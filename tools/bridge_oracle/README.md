# VERA20k bridge diagnostic fixtures

`schema.md` and `samples/` are legacy VERA20k bridge-domain diagnostic fixtures.
They document trace shapes and sample pathfinding cases; they are not a retail-game
runner, runtime capture backend, injector, or cross-engine comparison service.

These fixtures remain public because they predate the repository boundary and are
useful independently to VERA20k development.
