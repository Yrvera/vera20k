# VERA20k — Claude Code

@ENGINE.md

Use the shared contract above and relevant skills; no additional planning or approval
ceremony is required for work already authorized by the user.

Machine-local setup and Codex session recovery are in the primary checkout's `LOCAL.md`;
read it when needed. Keep durable preferences in memory, not stale progress claims.

When adapting an explicitly requested `/loop` prompt, preserve the invocation and the
user's stopping condition. Use the running host's available coordination tools.

Shared skill copies here are generated from `.agents/skills/`. Edit the canonical
source, then run `python tools/skill_sync.py --write`.