# Goal prompt examples

Illustrative only; adapt to actual scope and authority.

> Give persistent simulation state in the scenario-load path clear ownership,
> with app code coordinating loading. Follow ENGINE.md. Preserve behavior through
> representative load comparisons of deterministic hashes and snapshot bytes.
> Choose suitable Rust boundaries; an independent critic may inspect original
> evidence and challenge missing consumers. Finish when the selected defects and
> production comparisons are resolved. Record unrelated findings separately.

These comparisons prove refactor preservation, not gamemd parity.

> Resume in `<worktree>` on `<branch>` at `<HEAD>`. Checkpoint: `<path>`;
> `<unmerged work>` awaits `<review/validation>`. Reconcile current ownership/state,
> retain supported evidence and resume `<next action>`, preserving the governing
> goal and subsequent amendments below.
