# Iron Curtain — Parity Report

Compare rows per symbol. MATCH/DRIFT/MISSING/INTERNAL-ONLY. Observable-output drift only — internals can differ.

| Symbol | Verdict | Input | gamemd output | Rust output | Visibility | Citations |
|---|---|---|---|---|---|---|
| StartFidget_IronCurtain_Dispatch — deflect path (ImmuneToIronCurtain) | DRIFT | Fire IC at a unit whose type has `ImmuneToIronCurtain=yes` | Unit takes TakeDamage instakill (deflect), NOT invulnerability | Unit receives invulnerability (no immune check at dispatch, no deflect path exists) | High — player fires IC at immunized target and expects a kill; instead gets a protected unit | gamemd: manifest notes `TechnoTypeClass+0xd97` deflect check at 0x004deae4; Rust: `iron_curtain.rs` has no immune flag check; `object_type.rs` has no `immune_to_iron_curtain` field |
| StartFidget_IronCurtain_Dispatch — chrono warp detach | DRIFT | Fire IC at a unit currently in a chrono teleport (being_warped / ChronoDelay phase) | gamemd detaches chrono warp before applying IC timestamp | Rust `launch` does not clear `teleport_state`; entity keeps warp state while gaining invulnerability | Medium — visible as a unit simultaneously warping and glowing invulnerable; only triggers on chrono units mid-warp | gamemd: manifest notes chrono warp detach handling at 0x004deae4; Rust: `iron_curtain.rs:51-61` no teleport_state clear |
| StartFidget_IronCurtain_Dispatch — timestamp set (normal path) | MATCH | Fire IC at a normal ground vehicle | Sets IC apply-frame + duration timer on entity | `apply_invulnerability()` sets `start_frame=current_frame`, `duration_frames` | — | gamemd: `TechnoClass__IronCurtain` sets +0x18c/+0x194 (manifest notes); Rust: `invulnerability.rs:49-60` |
| ReadCombatDamage — IronCurtainDuration INI parse | MATCH | `IronCurtainDuration=N` in `[CombatDamage]` | Duration N frames (default 750) stored in RulesClass field | `rules.general.iron_curtain_duration` parsed from `[CombatDamage]`, default 750 | — | gamemd: manifest notes address 0x0066c63e, default 750 frames; Rust: `ruleset.rs` `iron_curtain_duration: combat_damage.and_then(|s| s.get_i32("IronCurtainDuration"))` default 750 |
| TechnoClass::IronCurtain — start_frame + duration storage | MATCH | Fire IC at any non-infantry non-immune unit at current_frame T, duration D | Sets TechnoClass+0x18c=T (start frame), TechnoClass+0x194=D (duration) | `apply_invulnerability` sets `start_frame=current_frame`, `duration_frames=D` — same semantics | — | gamemd: `disassemble_function 0x0070e2b0` confirms `+0x18c=g_CurrentFrameCounter`, `+0x194=duration`; Rust: `invulnerability.rs:49-60` |
| TechnoClass::IronCurtain — is_force_shield flag | MATCH | Fire ForceShield (is_force_shield=1) vs IronCurtain (is_force_shield=0) | Sets TechnoClass+0x1c4=1 (ForceShield) or 0 (IronCurtain) | Sets `kind = InvulnKind::ForceShield` or `InvulnKind::IronCurtain` | — | gamemd: `disassemble_function 0x0070e2b0` JZ branch sets +0x1c4; Rust: `invulnerability.rs:16-19` `InvulnKind` enum |
| TechnoClass::IronCurtain — TechnoClass+0x1a4 clear | INTERNAL-ONLY | IC applied to any unit | gamemd clears TechnoClass+0x1a4 to 0 on IC apply | Rust `apply_invulnerability` has no equivalent clear | Unknown field purpose — if +0x1a4 gates player-visible behavior, this becomes DRIFT; see struct decode task #10 | gamemd: `disassemble_function 0x0070e2b0` `XOR EAX,EAX; MOV [ECX+0x1a4],EAX`; Rust: `invulnerability.rs` no equivalent |
| TechnoClass::IronCurtain — source_house not stored | INTERNAL-ONLY | source_house param passed to IronCurtain | gamemd receives source_house but does NOT store it in TechnoClass fields (assembly confirmed) | Rust `apply_invulnerability` also has no source_house field | — | gamemd: `disassemble_function 0x0070e2b0` frame analysis — source_house at [X+8] never written to any field; Rust: `invulnerability.rs:49-60` |
| IsIronCurtainActive — active-check logic | MATCH | Unit with `start_frame=T, duration=D` queried at frame F | true if `F-T < D` AND `D-(F-T) > 0` (equivalent: `F-T < D`); false if apply_frame==-1 or elapsed≥duration | `is_invulnerable`: `elapsed = current_frame.saturating_sub(start_frame); elapsed < duration_frames` — same observable result | — | gamemd: `decompile_function 0x0041bf40`; Rust: `invulnerability.rs:39-45` |
| IsIronCurtainActive — never-applied sentinel | INTERNAL-ONLY | Query unit that never had IC applied | gamemd: `+0x18c == -1` → returns false | Rust: `Option::None` → returns false | — | Same observable output, different storage. gamemd: `decompile_function 0x0041bf40` `if (*(+0x18c) != -1)`; Rust: `invulnerability.rs:40-42` |
| IsIronCurtainActive — boundary (elapsed==duration) | MATCH | Query at exact expiry frame (F = T + D) | gamemd: `remaining = D - D = 0; 0 < 0` = false | Rust: `elapsed = D; D < D` = false | — | gamemd: `decompile_function 0x0041bf40` `CONCAT31` return on `0 < iVar1`; Rust: `invulnerability.rs:44` `elapsed < duration_frames`; confirmed by test at `invulnerability.rs:90` |
| IsIronCurtainActive — zero duration | MATCH | `IronCurtainDuration=0` in INI | gamemd: duration=0 → false (0 < 0) | Rust: `elapsed < 0` = false | — | gamemd: decompile logic; Rust: test at `invulnerability.rs:107-113` |
| IsIronCurtainActive — saturating_sub guard | INTERNAL-ONLY | `current_frame < start_frame` (impossible in normal play; 32-bit counter at 15fps takes ~9 years to wrap) | gamemd: no guard; signed subtraction would produce large positive elapsed → IC expired | Rust: `saturating_sub` → elapsed=0 → IC active | Not player-visible in practice; frame counter does not wrap during normal sessions | gamemd: `decompile_function 0x0041bf40` no underflow guard; Rust: `invulnerability.rs:43` |
| ReadAudioVisual — IronCurtainColor INI parse | MISSING | `IronCurtainColor=N` in `[AudioVisual]` | Packed int at RulesClass+0x18A8 read via `CCINIClass__ReadInt`; value used by rendering layer to tint IC'd units | No `iron_curtain_color` field exists in `RuleSet`; no color parsed; no tint applied in rendering | High — every IC'd unit and building shows the wrong tint (default engine color instead of the INI-configured IC gold shimmer) for the full IC duration every match | gamemd: `decompile_function 0x0066b844` param_1[0x62a]=RulesClass+0x18A8 via ReadInt; Rust: grep of `src/rules/` and `src/render/` found no `iron_curtain_color` |
| ReadGeneral — IronCurtainInvokeAnim INI parse | MATCH | `IronCurtainInvokeAnim=IRONBLST` in `[General]` | Reads AnimTypeClass* at RulesClass+0x348; uses it to spawn invoke animation at IC target cell | `rules.general.iron_curtain_invoke_anim` parsed from `IronCurtainInvokeAnim=` in `[General]`, default "IRONBLST"; `spawn_invoke_anim(sim, &anim_name, ...)` called in `launch()` | — | gamemd: manifest notes RulesClass+0x348 `ReadString`/`FindOrAllocate` call for InvokeAnim; Rust: `ruleset.rs` `iron_curtain_invoke_anim`, `iron_curtain.rs:34` `spawn_invoke_anim()` |
| SuperWeaponTypeClass — Type=IronCurtain dispatch | MATCH | `Type=IronCurtain` in SW section | SW type table at `0x008425c0` index 1 = "IronCurtain"; stored at `SuperWeaponTypeClass+0xb4`; routes IC handler | `SuperWeaponKind::from_ini_str("ironcurtain") = Some(IronCurtain)` case-insensitive; `kind` stored on `SuperWeaponType`; `launch()` called for `IronCurtain` variant | — | gamemd: `fn-SuperWeaponTypeReadINI-IC.md` verified table at `0x008425c0`, index 1 = "IronCurtain", field `+0xb4`; Rust: `superweapon_type.rs:58` `from_ini_str` match |
| TechnoTypeClass — Organic flag IC deflect | DRIFT | Fire IC at a unit whose type has `Organic=yes` (Dolphin, Giant Squid) | Checks `TechnoTypeClass+0xd97` (Organic=); if set, skips invulnerability and calls TakeDamage with C4Warhead (instakill) | No `Organic` field in any Rust source; `iron_curtain.rs:53` only checks `EntityCategory::Infantry`; Organic non-infantry units (Dolphins, Squids) receive invulnerability instead of being instakilled | High — fires every match on any water map when IC hits Dolphins or Giant Squids; wrong: IC kills them in gamemd, protects them in Rust | gamemd: `struct-TechnoTypeClass-IC-immune.md` verified `+0xd97` = `Organic=` via `TechnoTypeClass__ReadINI` decompile at `0x00712170`; Rust: grep of all `src/` confirms no `Organic` field or check |
| g_CurrentFrameCounter — IC frame base | INTERNAL-ONLY | IC applied at game tick T | `g_CurrentFrameCounter` (`0x00a8ed84`): incremented once per tick in `Main_Tick` (`0x0055de81`); stored as IC apply frame in `TechnoClass+0x18c` | `sim.tick as u32` in `iron_curtain.rs:31`; semantically equivalent — increments once per sim tick; passed to `apply_invulnerability` as `current_frame` | — | gamemd: `global-CurrentFrameCounter.md` verified `0x00a8ed84` write at `0x0055de81`; Rust: `iron_curtain.rs:31` |
| g_RulesClass_Instance — rules singleton access | INTERNAL-ONLY | IC applied (rules data accessed) | `g_RulesClass_Instance` (`0x008871e0`): global pointer dereferenced for C4Warhead (`+0xfa8`) and duration data | `&RuleSet` parameter passed to `launch()`; dereferenced directly for `iron_curtain_duration` and other rule fields | — | gamemd: `global-RulesClass-Instance.md` verified `0x008871e0` write at `0x007778ce`; Rust: `iron_curtain.rs:26` `rules.general.iron_curtain_duration` |
| TechnoClass+0x1c4 — spark color on blocked hit | DRIFT | Shoot at an IC'd unit (any); shoot at a Force-Shield'd unit | gamemd `TechnoClass__ReceiveDamage`: `+0x1c4 != 0` → spark_color=6 (Force Shield blue-white); `+0x1c4 == 0` → spark_color=1 (IC gold) | Rust `combat/mod.rs:2113-2117` skips damage when invulnerable but does NOT emit any spark color; comment "Flash-effect spawn deferred"; `InvulnKind` present but unused in damage path | High — player shoots at IC'd and FS'd units every match; wrong spark color (none vs gold/blue-white) is visible on every blocked hit | gamemd: `decompile_function 0x00701900` spark_color branch on `+0x1c4`; Rust: `combat/mod.rs:2113` `continue` with no spark |
| TechnoClass+0x18c apply-frame — storage format | INTERNAL-ONLY | Unit receives IC at frame T | gamemd: `i32 +0x18c = T`; `-1` = never-applied sentinel | Rust: `Option<InvulnerabilityState>` with `start_frame: u32`; `None` = never-applied | — | Covered by compare tasks #20 and #21 |
| TechnoClass+0x1a4 — XOR-clear on IC apply | INTERNAL-ONLY | IC applied to any unit | gamemd: `+0x1a4` cleared to 0 on IC apply; no reader found in IC, render, damage, AI, or save paths | Rust: no equivalent field | YELLOW — if a reader surfaces later and drives visible behavior, upgrade to DRIFT | gamemd: `disassemble_function 0x0070e2b0` `XOR EAX,EAX; MOV [ECX+0x1a4],EAX`; Rust: no equivalent |
| string IronCurtainDuration — INI key presence | MATCH | `IronCurtainDuration=N` in `[CombatDamage]` | gamemd: string at `0x0083b0b8` consumed by `RulesClass__ReadCombatDamage`; stored as `RulesClass+0xfe8`; default 750 | Rust: `iron_curtain_duration` parsed from `[CombatDamage]`, key string `"IronCurtainDuration"`, default 750 | — | gamemd: `string-IronCurtainDuration.md` verified; Rust: `ruleset.rs` — see compare row #10 |
| string IronCurtainInvokeAnim — INI key presence | MATCH | `IronCurtainInvokeAnim=IRONBLST` in `[General]` | gamemd: string at `0x0083cda0` consumed by `RulesClass__ReadGeneral`; stored as `AnimTypeClass*` at `RulesClass+0x348`; default `IRONBLST` | Rust: `iron_curtain_invoke_anim: String` parsed from `[General]`, key `"IronCurtainInvokeAnim"`, default `"IRONBLST"` | — | gamemd: `string-IronCurtainInvokeAnim.md` verified; Rust: `ruleset.rs` — see compare row #21 |
| string IronCurtainColor — INI key presence | MISSING | `IronCurtainColor=N` in `[AudioVisual]` | gamemd: string at `0x0083a1a4`; packed color int stored at `RulesClass+0x18a8`; applied as render tint | Rust: no `iron_curtain_color` field in `RuleSet`; key not parsed; tint not applied | High — see row #20 for full visibility assessment | gamemd: `string-IronCurtainColor.md` verified; Rust: absent — see compare row #20 |
| string "IronCurtain" typename — SW dispatch | MATCH | `Type=IronCurtain` in a SW INI section | gamemd: `"IronCurtain"` at `0x0081be54`; type-name table index 1; stored as enum int in `SuperWeaponTypeClass+0xb4` | Rust: `SuperWeaponKind::from_ini_str("ironcurtain")` case-insensitive; `IronCurtain` variant used for dispatch | — | gamemd: `string-IronCurtain-typename.md` verified; Rust: `superweapon_type.rs` — see compare row #22 |
| BuildingClass::IronCurtain — gate byte reset block (task #22) | INTERNAL-ONLY | IC applied to a building where `BuildingClass+0x6df` is non-zero (setter unidentified) | gamemd: gate check at `+0x6df`; if non-zero, clears it, sets `+0x528=g_CurrentFrameCounter`, writes stack garbage to `+0x52c` (vestigial), clears `+0x530` and `+0x540`, then calls TechnoClass::IronCurtain | Rust: no building-specific gate or reset block; all entities including buildings go through `apply_invulnerability()` directly | YELLOW — observable only if `+0x6df` setter fires during normal play; setter not yet identified. If the setter fires during building production or animation completion, upgrades to DRIFT | gamemd: `decompile_function 0x00457c90`; Rust: `iron_curtain.rs:53-61` no BuildingClass-specific path |
| BuildingClass::IronCurtain — normal delegate path (task #22) | MATCH | IC applied to a building (normal case, `+0x6df` == 0) | BuildingClass vtable slot 0x154 calls TechnoClass::IronCurtain immediately | Rust `launch()` calls `apply_invulnerability()` for buildings — same outcome: invulnerability set | — | gamemd: `decompile_function 0x00457c90`; Rust: `iron_curtain.rs:56-61` |
| InfantryClass::IronCurtain — instakill via warhead (task #23) | INTERNAL-ONLY | IC fired at infantry | gamemd: calls `TechnoClass::ReceiveDamage` (vtable+0x16c → `0x00517fa0`) with `damage = InfantryTypeClass.Strength` and `warhead = C4Warhead ptr` — C4-warhead kill effects (animation, sound) triggered at unit strength damage | Rust: `entity.health.current = 0; entity.dying = true` — unit dies but without C4-warhead-specific kill effects | Medium — fires every IC-on-infantry hit; if C4 warhead has distinct death animation/sound vs plain `dying=true`, player sees difference on every infantry IC kill | gamemd: `fn-InfantryClass-IronCurtain.md` `decompile_function 0x00522600`; Rust: `iron_curtain.rs:54` |
| BuildingClass IC struct fields — `+0x528`/`+0x530`/`+0x540` (task #30) | INTERNAL-ONLY | IC applied to any building | gamemd sets `BuildingClass+0x528 = current_frame`, clears `+0x530` and `+0x540` in gate-reset block | Rust `InvulnerabilityState.start_frame` covers `+0x528` semantics; no `+0x530`/`+0x540` equivalents | YELLOW — if `+0x530`/`+0x540` readers exist in building production/animation logic and affect visible output, upgrades to DRIFT | gamemd: `struct-BuildingClass-IC-fields.md`; Rust: `invulnerability.rs` |
| RulesClass IC config — all four fields summary (task #31) | — | — | `+0x348` InvokeAnim=IRONBLST (MATCH); `+0x18a8` IronCurtainColor (MISSING); `+0xfa8` C4Warhead (MISSING); `+0xfe8` IronCurtainDuration=750 (MATCH) | See individual rows above for IronCurtainDuration, IronCurtainInvokeAnim, IronCurtainColor, and InfantryClass C4Warhead instakill | — | gamemd: `struct-RulesClass-IC-config.md`; Rust: `ruleset.rs` |
