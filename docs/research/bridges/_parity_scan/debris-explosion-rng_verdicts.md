# Adversarial Verdicts — debris-explosion-rng

Facet: BlowUpBridge debris spawn + CollapseBridge hut-walker explosion spawn RNG draw order/arithmetic.
Auditor role: adversarial skeptic (default DRIFT only when burden of proof met; UNCERTAIN if gamemd side unconfirmable this session).

## Live verification performed this session

- `decompile_function 0x0047DD70` → confirmed `CellClass__BlowUpBridge` (decompiler returns that exact name + body 0x0047dd70–0x0047e036). Draw order (outer gate, jitterX ftol, jitterY ftol, metallic gate, [metallic slot], R(1,5) delay, explosion slot) reads exactly as finder reported.
- `disassemble_function 0x0047DD70` → full listing captured. Gate FCOMP chains at 0x0047de5d–0x0047de72 (outer), 0x0047df4c–0x0047df61 (metallic); jitter FILD/FMUL scale/FSUB 0.5/FMUL 50/FIADD/ftol at 0x0047decf–0x0047dee9 (X) and 0x0047df0d–0x0047df27 (Y); metallic slot CALL 0x0047df91 with NO count==0 test between gate-pass/alloc (0x0047df61/0x0047df74) and the slot CALL.
- `decompile_function 0x007C5F00` + `disassemble_function 0x007C5F00` → `Math__ftol` is FISTP with x87 default rounding = ROUND-to-nearest (banker's). Finder's "ftol=ROUND" claim VERIFIED.
- `read_memory` constants: `0x007e3570` = `0x3E00000000400000` = 4.656612877414201e-10 (scale; == 2^-31 + 2^-61, NOT exactly 1/2^31 nor 1/(2^31-1)); `0x007e4f58` = 0.95; `0x007e1738` = 0.5; `0x007e4f50` = 50.0. All VERIFIED.
- Boundary math recomputed from the exact decoded `scale` (not hand-arithmetic): largest outer-pass draw = 2_040_109_463 (next = 2_040_109_464 → 0.95000000014 → fail); largest metallic-pass draw = 1_073_741_822 (0x3FFFFFFF → exactly 0.5 → fail under strict <). Jitter offset: bin range [-25,+25], Rust floor range [-25,+24], diverges by 1 on most draws (e.g. 0x66666666 → bin 15 / rust 14).

## MCP outage caveat (D4, D6)

The Ghidra MCP server connection dropped after the BlowUpBridge + ftol + constants reads; `decompile_function`/`disassemble_function 0x00575BA0` could NOT be re-run live this session. CollapseBridge_NS_High evidence is therefore **doc-corroborated, not live-re-confirmed**: `fn-CollapseBridge-NS-High.md` (cites `decompile_function 0x00575BA0`, `get_function_by_address 0x00575BA0` → `MapClass__CollapseBridge_NS_High`) independently shows the inner 3-iteration perp loop with `Random__RandomRanged(0,0x7ffffffe); ftol()` for X and Y (same ROUND chain) and the `DAT_00abdc50` scratch-cell fallback for out-of-range cells.

---

D1: VERDICT=REAL — `decompile_function 0x0047DD70` + decoded scale `0x007e3570`/0.95 `0x007e4f58`: outer gate passes iff `(double)draw*scale < 0.95`, largest passing integer = 2_040_109_463. Rust const `BRIDGE_DEBRIS_OUTER_GATE_EXCLUSIVE = 2_040_109_466` (bridge_orchestrator.rs:373, used at :1182 `outer_draw >= ...continue`) passes for draw ≤ 2_040_109_465. Corrected delta: Rust must use **2_040_109_464** (pass iff draw ≤ 2_040_109_463). At draw ∈ {2_040_109_464, 2_040_109_465} Rust spawns full debris+explosion (6–7 extra draws); gamemd spawns nothing (1 draw) → hard lockstep desync.

D2: VERDICT=REAL — same decompile + 0.5 const `0x007e1738`: metallic gate passes iff `draw*scale < 0.5`; draw=0x3FFFFFFF (1_073_741_823) maps to exactly 0.5 → strict `<` FAILS. Rust const `BRIDGE_METALLIC_GATE_EXCLUSIVE = 0x4000_0000` (:374, used at :1200 `metallic_draw < ...`) passes for draw ≤ 0x3FFFFFFF. Corrected delta: Rust must use **0x3FFF_FFFF** (pass iff draw < 0x3FFFFFFF, i.e. ≤ 0x3FFFFFFE). At draw=0x3FFFFFFF Rust draws+spawns a metallic slot the binary does not → extra slot draw → lockstep desync.

D3: VERDICT=REAL — `disassemble_function 0x0047DD70` (0x0047decf–0x0047dee9) + `Math__ftol 0x007C5F00`=ROUND + constants scale/0.5/50.0: gamemd offset = `round((draw*scale - 0.5)*50)`, range [-25,+25]. Rust `bridge_jittered_subcell` (:1304) = `floor(draw*50/2^31) - 25`, range [-25,+24]. Diverges by 1 lepton on most draws (0x66666666 → bin 15 / rust 14; 0x7FFFFFFE → bin 25 / rust 24) and Rust never reaches +25. Render-only (no extra draws), every spawn of every collapse. Corrected delta: replace floor/2^31 with `round((draw*scale - 0.5)*50)` using scale=`0x007e3570` and round-to-nearest.

D4: VERDICT=REAL — Rust side fully confirmed from source: `spawn_bridge_explosion_effect` (:1263) calls the same `bridge_jittered_subcells` → inherits D3's floor/2^31 error verbatim. gamemd side corroborated by `fn-CollapseBridge-NS-High.md` (cites `decompile_function 0x00575BA0`), which shows the per-row debris loop using `Random__RandomRanged(0,0x7ffffffe); local_c=Math__ftol()` for X and Y — identical ROUND chain to BlowUpBridge. NOTE: NOT live-re-confirmed at 0x00575BA0 this session (MCP outage); resting on the cited decode doc, which is weaker than live Ghidra. Delta = same as D3, applied to every hut-collapse-walker explosion sprite.

D5: VERDICT=REAL — `disassemble_function 0x0047DD70`: between metallic-gate-pass (JZ 0x0047df61) / alloc-ok (JZ 0x0047df74) and the slot CALL 0x0047df91, the only intervening ops are `MOV EAX,[EDX+0x14c]` (count) / `DEC EAX` / `PUSH EAX` — there is NO `count==0` branch. gamemd unconditionally calls `RandomRanged(0, count-1)` once gate+alloc pass. Rust gates the slot draw on `metallic_pass && metallic_count > 0` (:1204). Confirmed static divergence: a modded empty `MetallicDebris=` (count 0) makes gamemd consume a `RandomRanged(0,-1)` draw (then index a 0-length array — UB) while Rust skips it → RNG-stream desync. Stock YR `MetallicDebris` is non-empty so never fires in stock play; binary's own behavior is UB there. REAL as a draw-count divergence; runtime crash/UB of the empty-array index not separately confirmed (static-only).

D6: VERDICT=UNCERTAIN — Rust side confirmed: `spawn_hut_walker_pre_destroy_effects` (:828) loops `[-1,0,1]`, and `step_axis` returns `None` only at u16 coord 0/65535 (:683–699), skipping both the spawn and its 4 draws for that perpendicular cell. gamemd side: `fn-CollapseBridge-NS-High.md` shows a fixed `iVar10=3` perp loop with the `DAT_00abdc50` scratch-cell fallback for out-of-range cells (implying all 3 iterations always run and consume draws), which supports the finder — BUT this is doc-corroborated only; the live `0x00575BA0` decompile/disassemble to confirm the perp loop is truly unconditional (and that the scratch-cell path still reaches the 4-draw AnimClass spawn rather than short-circuiting) could NOT be re-run this session (MCP outage). If the binary reading holds, this is REAL but unreachable in stock maps (collapsing bridge cell at absolute map coord 0 or 65535 only). Re-verify live at 0x00575BA0 before acting.

---

## MISS (new disparities the finder did not surface)

MISS-1 [LOW, render-only, every spawn]: anim Z-coord formula mismatch. gamemd BlowUpBridge (disasm 0x0047de78–0x0047dea6): `iStack_4 = (char)this->Level * DAT_0089e7c0 + DAT_0089e7b4` — a signed-byte level scaled by a global lepton-per-level constant PLUS a base offset, in leptons. Rust passes `z: deck_level` as a raw `u8` cell level (:1216/:1240), no `*scale + base` lepton conversion. The finder explicitly listed Z as UNCHECKED, so this is acknowledged-deferred rather than a true miss, but the disasm now shows the exact gamemd formula (`level*0x0089e7c0 + 0x0089e7b4` leptons) the Rust does not reproduce — surfacing it as a concrete delta. Belongs to a render-Z facet but is a real arithmetic divergence.

MISS-2 [LOW, lockstep-relevant if explosion list modded empty]: asymmetric empty-list guards. Rust early-returns the WHOLE `spawn_bridge_debris` when `explosion_count == 0` (:1173) — so with a non-empty `MetallicDebris=` but empty `BridgeExplosions=`, Rust draws ZERO (no outer gate, no metallic). gamemd gates only on `BridgeExplosions count > 0` at the TOP too (0x0047de33 JLE skip), so on empty BridgeExplosions gamemd also draws nothing — that part matches. HOWEVER gamemd's explosion-slot block has its own `operator_new`-fail guard (0x0047dfce JZ) but no `count==0` recheck after R(1,5); since the top gate already ensured count>0 this is moot in stock. No divergence proven — withdrawn. (Recorded for completeness; not a confirmed miss.)

MISS-3 [LOW, timing, every spawn — overlaps finder UNCHECKED]: `BRIDGE_EFFECT_FRAME_MS = 67` (:377) vs gamemd anim delay measured in 15fps logic frames (66.667 ms). Finder flagged this UNCHECKED; noting the delay arg path is `Random__RandomRanged(1,5)` → AnimClass__Constructor 3rd arg (0x0047dfe1 / push ESI 0x0047e01e), and the frame→ms unit conversion is the only divergence (the draw itself matches). Real sub-ms timing drift accumulating ~1.7ms at 5 frames; belongs to anim-timing facet.
