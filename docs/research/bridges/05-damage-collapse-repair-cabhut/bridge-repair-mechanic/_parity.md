# Bridge Repair Mechanic — Parity Report

Compare rows per symbol. MATCH/DRIFT/MISSING/INTERNAL-ONLY. Observable-output drift only — internals can differ.

| Symbol | Verdict | Input | gamemd output | Rust output | Visibility | Citations |
|---|---|---|---|---|---|---|
| `BuildingClass::Update` bridge branch — entry condition (health > 0 + C4 flag) | INTERNAL-ONLY | Building alive with C4 flag set | Timer checked, dispatch fires | Rust: `!e.dying` gate in `tick_c4_plants` Phase 2 (world_orders.rs:523) | None — same semantic | gamemd: doc §Behavioral Analysis step 2; Rust: world_orders.rs:523 |
| `BuildingClass::Update` bridge branch — timer comparison | INTERNAL-ONLY | C4 planted for N frames where N >= duration | Fires when `g_CurrentFrameCounter - start_frame >= duration` | Rust: `self.tick.saturating_sub(pending.plant_start_tick) < delay` → skip; fires otherwise | None — same elapsed-frame semantics | gamemd: doc §Decompilation Excerpt lines 52–56; Rust: world_orders.rs:548 |
| `BuildingClass::Update` bridge branch — `BridgeRepairHut` type check | INTERNAL-ONLY | Building is CABHUT vs normal building | Reads `BuildingTypeClass+0x16B6` directly | Rust: `rules.object(...).bridge_repair_hut` (world_orders.rs:771) | None — same observable branch selection | gamemd: doc §Struct Field Accesses; Rust: world_orders.rs:765–773 |
| `BuildingClass::Update` bridge branch — 5×5 scan ordering (X-major) | MATCH | 5×5 neighbourhood around hut cell | X outer (iVar12 -2..+2), Y inner (iVar3 -2..+2) — first low-bridge hit sets sentinel | Rust: `(-2..=2i32).flat_map(move |dx| (-2..=2i32)...)` — identical X-major order (bridge_orchestrator.rs:224) | None — first-hit sentinel would behave identically in both | gamemd: doc §5×5 scan §Behavioral Analysis; Rust: bridge_orchestrator.rs:222–235 |
| `BuildingClass::Update` bridge branch — low/high dispatch logic | MATCH | 5×5 neighbourhood contains low-bridge tile | Dispatches to `DestroyBridge_Low_OnHutDeath` | Rust: `choose_hut_bridge_family` → `HutBridgeFamily::Low` → `dispatch_bridge_collapse_from_hut` (bridge_orchestrator.rs:408–416) | None — same family routed to low collapse | gamemd: doc §Dispatch table; Rust: bridge_orchestrator.rs:408–426 |
| `BuildingClass::Update` bridge branch — low/high dispatch: high path | MATCH | 5×5 neighbourhood has no low-bridge tile | Dispatches to `DestroyBridge_High_OnHutDeath` | Rust: `HutBridgeFamily::High` → same `dispatch_bridge_collapse_from_hut` (bridge_orchestrator.rs:413–416) | None | gamemd: doc §Dispatch; Rust: bridge_orchestrator.rs:413–416 |
| `BuildingClass::Update` bridge branch — post-dispatch clear of C4 flag | MATCH | After bridge dispatch | Clears `this+0x6DF = 0` and `this+0x540 = 0` | Rust: sets `consumed_pending_marker: true`, clears `pending_c4_detonation` and attacker `c4_plant` (world_orders.rs:587–596) | None — C4 state is cleared, hut survives | gamemd: doc assembly 0x00440320/0x00440327; Rust: world_orders.rs:587–596 |
| `BuildingClass::Update` bridge branch — hut survives (not damaged) | MATCH | C4 timer expires on CABHUT | Hut HP not changed; only the bridge is destroyed | Rust: `apply_c4_damage_to_building` returns early on `target_bridge_hut=true` with `killed_building: false` (world_orders.rs:788) | Visible: hut remains on map after bridge collapses | gamemd: doc §Summary "BridgeRepairHut returns consumed_pending_marker"; Rust: world_orders.rs:760–792 |
| **`BuildingClass::Update` bridge branch — C4 detonation mechanism (DRIFT — HIGH)** | **DRIFT** | SEAL/Tanya clicks CABHUT enemy bridge repair hut | **gamemd: bridge collapses IMMEDIATELY at SEAL cell-arrival** (`InfantryClass::PerCellProcess` calls `ProcessBridgeDestruction_Low/High` directly at 0x00519630 step 3, with no timer. `BuildingClass::Update` timer path is for NORMAL C4 targets only, NOT BridgeRepairHut.); `BuildingClass+0x6DF` is never set for huts in gamemd | **Rust: SEAL sets `pending_c4_detonation` on CABHUT via `tick_c4_plants` Phase 1, bridge collapses after `c4_delay_ticks` frames** via `BuildingClass::Update`-equivalent timer path. No immediate collapse on arrival. | **Player-visible: bridge collapses after a delay in Rust vs. instantly on arrival in gamemd. In gamemd, clicking CABHUT triggers immediate bridge-drop; in Rust it fires after the C4 countdown delay.** | gamemd: fn-InfantryClass-PerCellProcess-C4Plant.md §Summary state machine (lines 254–259) — BridgeRepairHut branch fires ProcessBridgeDestruction immediately, never sets +0x6DF; Rust: world_orders.rs:423–606 timer path, bridge_orchestrator.rs:151–217 |
| **`BuildingClass::Update` bridge branch — C4-on-CABHUT silent-failure (DRIFT — HIGH, C4 cursor works but engine path wrong)** | **DRIFT** | SEAL with `c4_plant` targeting CABHUT, tick_c4_plants Phase 1 footprint check | gamemd: no footprint-entry step for BridgeRepairHut — `PerCellProcess` fires directly when SEAL cell == building cell (SEAL walks up, arrives, detonates immediately, no marker needed) | Rust: `tick_c4_plants` Phase 1 checks `target_footprint.contains(&attacker_cell)` (world_orders.rs:478). CABHUT `c4_base_foundation_cells` resolves via `foundation` string. If CABHUT foundation cells are impassable/no-enter, SEAL may never reach attacker_cell ∈ footprint, so `pending_c4_detonation` is never set and bridge never collapses. Cursor shows `Demolish` (app_cursor.rs:250–261, diagnostic log added at 236). | **Player-visible: in Rust, if SEAL can't physically enter CABHUT footprint cells, C4 does nothing permanently — confirmed by "C4 on CABHUT does nothing" report (project_c4_bridge_hut_followup.md). Root cause: wrong detonation mechanism (timer vs. immediate), causing footprint-entry requirement that doesn't exist in gamemd.** | gamemd: fn-InfantryClass-PerCellProcess-C4Plant.md §Full Logic (immediate call path); Rust: world_orders.rs:478, app_cursor.rs:233–261, app_context_order.rs:319–373 |
| `DestroyBridge_Low_OnHutDeath` — 5×5 scan order (column-major) | MATCH | CABHUT cell, any low-bridge neighbourhood | gamemd: outer dx (-2..+2), inner dy (-2..+2); first hit in column-major order calls `DestroyBridgeFromCell_Low` | Rust: `hut_destroy_5x5_scan`: `(-2..=2).flat_map(dx → (-2..=2).filter_map(dy))` — X-major, same column-major order (bridge_orchestrator.rs:222–235) | None | gamemd: fn-DestroyBridge-Low-OnHutDeath.md §Phase 1; Rust: bridge_orchestrator.rs:222–235 |
| `DestroyBridge_Low_OnHutDeath` — low overlay range [0x4A..0x65] | MATCH | Cell overlay subtype check | gamemd: `CellClass+0x44 ∈ (0x49, 0x66)` exclusive = [0x4A..0x65] | Rust: `BridgeRuntimeState::is_low_destroy_overlay`: `(0x4A..=0x65).contains(&overlay)` (walker.rs:34–36) | None | gamemd: fn-DestroyBridge-Low-OnHutDeath.md §Decompilation; Rust: src/sim/bridge_state/walker.rs:34–36 |
| `DestroyBridge_Low_OnHutDeath` — fast path: early return on first overlay match | INTERNAL-ONLY | 5×5 scan finds low-bridge overlay cell | gamemd: calls `DestroyBridgeFromCell_Low` on first hit and returns immediately | Rust: `find_destroy_overlay_seed` returns first match; `run_hut_collapse_bounded` starts from that seed (bridge_orchestrator.rs:190–207). Same observable: collapse initiated from first matching cell, no double-collapse | None — same observable outcome (collapse fires once from the first matching cell) | gamemd: fn-DestroyBridge-Low-OnHutDeath.md §Phase 1; Rust: bridge_orchestrator.rs:190–207 |
| `DestroyBridge_Low_OnHutDeath` — fallback: 8-direction flag walk (no overlay in 5×5) | INTERNAL-ONLY | No overlay [0x4A..0x65] found in 5×5 neighbourhood | gamemd: reads `CellClass+0x140 & 0x500` on hut cell; if zero walks 8 dirs up to 3 steps each for bridge-flagged cell | Rust: `build_hut_fallback_plan` + `run_hut_fallback_plan`; uses `HUT_FALLBACK_DIRS` (8 compass deltas) and `HUT_FALLBACK_STARTER_MASK` matching `0x500` semantics (bridge_orchestrator.rs:442–477) | None — same observable: bridge collapses from nearest bridge cell in both paths | gamemd: fn-DestroyBridge-Low-OnHutDeath.md §Phase 2; Rust: bridge_orchestrator.rs:354–406 |
| `DestroyBridge_Low_OnHutDeath` — post-destruction: zone rebuild | MATCH | After bridge destruction dispatch | gamemd: calls `UpdateBridgeZonesHelper` unconditionally (fn-DestroyBridge-Low-OnHutDeath.md §Phase 5) | Rust: `refresh_bridge_zones_if_dirty(sim, any_zones_dirty)` called from `apply_hut_bridge_execution` (bridge_orchestrator.rs:335). Conditioned on `any_zones_dirty` flag, which is set by any Collapsed outcome | None — same observable: zone graph rebuilt, pathfinding reflects new connectivity | gamemd: fn-DestroyBridge-Low-OnHutDeath.md §Phase 5; Rust: bridge_orchestrator.rs:289–338 |
| `DestroyBridge_Low_OnHutDeath` — post-destruction: adjacent bridges update | MATCH | After bridge destruction dispatch | gamemd: calls `UpdateAdjacentBridges_High` (even from low-bridge path) — verified via fn-UpdateAdjacentBridges-High.md callers | Rust: `update_adjacent_bridges(sim, &rim_cells)` called from `apply_hut_bridge_execution` (bridge_orchestrator.rs:333). `rim_cells` collected from Collapsed outcome `adjacent_bridges_dirty` field | None — same observable: bridge edge tiles updated | gamemd: fn-UpdateAdjacentBridges-High.md §Callers; Rust: bridge_orchestrator.rs:320–333 |
| `DestroyBridge_High_OnHutDeath` — high overlay range [0xCD..0xE8] | MATCH | Cell overlay subtype check | gamemd: `CellClass+0x44 ∈ (0xCC, 0xE9)` exclusive = [0xCD..0xE8] | Rust: `BridgeRuntimeState::is_high_destroy_overlay`: `(0xCD..=0xE8).contains(&overlay)` (walker.rs:38–40) | None | gamemd: fn-DestroyBridge-High-OnHutDeath.md §Stage 1; Rust: src/sim/bridge_state/walker.rs:38–40 |
| `DestroyBridge_High_OnHutDeath` — algorithm structure (same as low twin) | INTERNAL-ONLY | CABHUT cell, any high-bridge neighbourhood | gamemd: same 5×5 scan + fallback flag-walk + ramp-anchor derivation + walk as `DestroyBridge_Low_OnHutDeath`; only overlay band differs | Rust: same `dispatch_bridge_collapse_from_hut` path; `choose_hut_bridge_family` selects `HutBridgeFamily::High`; all downstream logic identical to low path (bridge_orchestrator.rs:151–217) | None — same observable: high bridge collapses from hut C4 | gamemd: fn-DestroyBridge-High-OnHutDeath.md; Rust: bridge_orchestrator.rs:408–416 |
| `DestroyBridgeFromCell_Low` — NS-overlay dispatch to CollapseBridge_EW_Low | INTERNAL-ONLY | Low-bridge NS-group overlay (`[0x4A..0x52] ∪ [0x5C..0x5F] ∪ {0x64}`) cell | gamemd: walks Y−1/Y−2 to find north-edge start cell, calls `CollapseBridge_EW_Low` (`0x00575220`) | Rust: `find_destroy_overlay_seed` → `canonicalize_hut_destroy_seed` probes Y−1/Y−2 (probe_axis=NS for EW physical_axis); dispatches to `run_hut_collapse_bounded` with `Axis::EW` (bridge_orchestrator.rs:258–287) | None — same observable: EW-axis collapse fires from canonical north start | gamemd: fn-DestroyBridgeFromCell-Low.md §4; Rust: bridge_orchestrator.rs:245–287 |
| `DestroyBridgeFromCell_Low` — EW-overlay dispatch to CollapseBridge_NS_Low | INTERNAL-ONLY | Low-bridge EW-group overlay (`[0x53..0x5B] ∪ [0x60..0x63] ∪ {0x65}`) cell | gamemd: walks X−1/X−2 to find west-edge start cell, calls `CollapseBridge_NS_Low` (`0x00575540`) | Rust: `find_destroy_overlay_seed` → `canonicalize_hut_destroy_seed` probes X−1/X−2 (probe_axis=EW for NS physical_axis); dispatches to `run_hut_collapse_bounded` with `Axis::NS` (bridge_orchestrator.rs:258–287) | None — same observable: NS-axis collapse fires from canonical west start | gamemd: fn-DestroyBridgeFromCell-Low.md §5; Rust: bridge_orchestrator.rs:245–287 |
| `DestroyBridgeFromCell_High` — NS/EW overlay routing and anchor probe | INTERNAL-ONLY | High-bridge cell, any overlay in [0xCD..0xE8] | gamemd: `DestroyBridgeFromCell_High` probes Y−1/Y−2 or X−1/X−2 to canonicalize start; dispatches to `CollapseBridge_EW_High` or `CollapseBridge_NS_High` | Rust: same `canonicalize_hut_destroy_seed` → `run_hut_collapse_bounded` path but with `HutBridgeFamily::High`; probe axis and dispatch axis are symmetric (bridge_orchestrator.rs:245–287) | None — same observable: high bridge collapses from canonical start | gamemd: fn-DestroyBridgeFromCell-High.md §Anchor selection; Rust: bridge_orchestrator.rs:245–287 |
| `CollapseBridge_EW_Low` / `CollapseBridge_NS_Low` — 4-iteration walker | MATCH | Any low-bridge collapse via `DestroyBridgeFromCell_Low` | gamemd: `param_1 = (int*)4; while (0 < (int)param_1) { ...; param_1-- }` — 4 iterations | Rust: `MAX_HUT_SWEEP_STEPS = 4`; `for _ in 0..MAX_HUT_SWEEP_STEPS` (bridge_orchestrator.rs:367, 751) | None | gamemd: fn-CollapseBridge-NS-Low.md §Phase 2; Rust: bridge_orchestrator.rs:367, 751 |
| `CollapseBridge_EW_Low` / `CollapseBridge_NS_Low` — 3-retry inner loop | MATCH | Any low-bridge cell advance within the walker | gamemd: `iVar4 = 0; do { DestroyBridge_Low(...); iVar4++; } while (iVar4 < 3)` | Rust: `MAX_HUT_ATTEMPTS_PER_STEP = 3`; `for _ in 0..MAX_HUT_ATTEMPTS_PER_STEP` (bridge_orchestrator.rs:368, 763) | None | gamemd: fn-CollapseBridge-NS-Low.md §Phase 2 (retry block); Rust: bridge_orchestrator.rs:368, 763 |
| `CollapseBridge_EW_Low` — terminal sentinel 0x64 (EW); `CollapseBridge_NS_Low` — 0x65 (NS) | MATCH | Walker step at destroyed-anchor cap cell | gamemd: EW_Low skips debris when `CellClass+0x44 == 0x64`; NS_Low when `== 0x65` | Rust: `hut_walker_terminal_cap(Low, EW) = 0x64`, `hut_walker_terminal_cap(Low, NS) = 0x65` — debris spawn skipped when `overlay == terminal_cap` (bridge_orchestrator.rs:837–844) | None | gamemd: fn-CollapseBridge-NS-Low.md §NS/EW twin table; Rust: bridge_orchestrator.rs:837–844 |
| `CollapseBridge_EW_Low` / `CollapseBridge_NS_Low` — 3 perpendicular debris anims per step | MATCH | Each walker step (non-sentinel cell) | gamemd: 3 `AnimClass::Constructor` calls at Y-1/Y/Y+1 (EW walker) or X-1/X/X+1 (NS walker) per step | Rust: `spawn_hut_walker_pre_destroy_effects` iterates `delta in [-1, 0, 1]` along perpendicular axis calling `spawn_bridge_explosion_effect` (bridge_orchestrator.rs:802–835) | None — 3 explosions per step, same perpendicular pattern | gamemd: fn-CollapseBridge-EW-Low.md §Main loop; Rust: bridge_orchestrator.rs:802–835 |
| `CollapseBridge_EW_Low` / `CollapseBridge_NS_Low` — RNG order: 4 calls per debris anim | MATCH | Each `AnimClass::Constructor` call for debris | gamemd: 4 `Random__RandomRanged` calls in fixed order: (1) X-jitter `[0, 0x7FFFFFFE]`, (2) Y-jitter `[0, 0x7FFFFFFE]`, (3) frame-delay `[1, 5]`, (4) anim-index `[0, count-1]` | Rust: `bridge_jittered_subcells(rng)` (2 calls: X+Y jitter `next_range_u32_inclusive(0, 0x7FFFFFFE)`), then `next_range_u32_inclusive(1, 5)` (delay), then `next_range_u32(len)` (index) — same 4-call order (bridge_orchestrator.rs:1250–1297) | None — RNG lockstep preserved for multiplayer determinism | gamemd: fn-CollapseBridge-EW-Low.md §RNG Lockstep; Rust: bridge_orchestrator.rs:1250–1297 |
| `CollapseBridge_NS_High` / `CollapseBridge_EW_High` — high sentinel 0xE8 (NS) / 0xE7 (EW) | MATCH | Walker step at high-bridge destroyed-anchor cap | gamemd: NS_High skips debris when `CellClass+0x44 == 0xE8`; EW_High when `== 0xE7` (fn-CollapseBridge-NS-High.md §7) | Rust: `hut_walker_terminal_cap(High, NS) = 0xE8`, `hut_walker_terminal_cap(High, EW) = 0xE7` (bridge_orchestrator.rs:839–843) | None | gamemd: fn-CollapseBridge-NS-High.md §7 + fn-CollapseBridge-EW-High.md §EW_High twin; Rust: bridge_orchestrator.rs:837–844 |
| `CollapseBridge_NS_High` / `CollapseBridge_EW_High` — 4-iteration walker, 3-retry inner loop, same RNG order | MATCH | Any high-bridge collapse | gamemd: same structure as low variants; `local_2c = 4`; inner `iVar10 < 3` retry; 4 RNG calls per anim | Rust: same constants `MAX_HUT_SWEEP_STEPS=4`, `MAX_HUT_ATTEMPTS_PER_STEP=3`; `spawn_bridge_explosion_effect` consumes same 4 RNG values per call | None | gamemd: fn-CollapseBridge-NS-High.md §4; Rust: bridge_orchestrator.rs:751–799, 1250–1297 |
| `UpdateBridgeZonesHelper` — zone rebuild called unconditionally after collapse | MATCH | After every bridge collapse (low or high) | gamemd: `MapClass::UpdateBridgeZonesHelper` called unconditionally at end of all 4 collapse walkers and both `*_OnHutDeath` functions | Rust: `refresh_bridge_zones_if_dirty(sim, any_zones_dirty)` where `any_zones_dirty` is set to `true` by any `StateOutcome::Collapsed` — fires on every real collapse (bridge_orchestrator.rs:303–336) | None — same observable: pathfinding zones reflect new bridge state after each collapse | gamemd: fn-UpdateBridgeZonesHelper.md §Active in YR (28 callers); Rust: bridge_orchestrator.rs:303–336 |
| `UpdateBridgeZonesHelper` — zone graph rebuild scope (full map) | INTERNAL-ONLY | Any bridge state change | gamemd: full flood-fill of all map cells + bridge-crossing edge insertion + passability matrix (5 phases) | Rust: `zone_map.rs` equivalent; `refresh_bridge_zones_if_dirty` triggers `rebuild_zone_map` covering same logical scope | None — same observable: zone connectivity correct after rebuild | gamemd: fn-UpdateBridgeZonesHelper.md §Behavioral Analysis; Rust: src/sim/pathfinding/zone_map.rs |
| `UpdateAdjacentBridges_High` — visual edge tile update after collapse | MATCH | After low or high bridge destruction (both call this) | gamemd: walks 8 directions for bridge-flagged neighbour, derives ramp anchor, calls `UpdateBridgeEdgeTiles_High` + `TacticalClass::DirtyScreenRect` | Rust: `update_adjacent_bridges` in bridge_orchestrator.rs — stub that updates span head records but full tile-visual edge update is render-side (bridge_orchestrator.rs:1034–1119). Bridge span collapse is noted via `notify_bridge_span_collapse` | None for sim correctness; visual edge tile rendering may differ — INTERNAL-ONLY for pathfinding parity, render fidelity depends on skirmish shell chrome | gamemd: fn-UpdateAdjacentBridges-High.md §Observable Effect; Rust: bridge_orchestrator.rs:1034–1119 |
| `IsBridgeRampTile` — theater-based ramp tile detection (6 configurations) | INTERNAL-ONLY | Bridge ramp cell during `*_OnHutDeath` fallback walk | gamemd: 6 checks of `CellClass+0x11A` sub-tile + runtime tile-type globals (2 single-tile pairs + 2 four-tile sets) to identify ramp cells | Rust: uses `is_bridge_transition_cell` / elevated bridge detection from map resolved terrain cell flags, not a runtime tile-type lookup (movement_step.rs:585–587). Same semantic: identifies cells that mark bridge entry/exit | None — same observable in bridge destruction path: ramp cells correctly identified during collapse | gamemd: fn-IsBridgeRampTile.md §Purpose; Rust: src/sim/movement/movement_step.rs:585–587 |
| `IsLowBridgeEndpointTile` — axis-discriminated terminal tile detection | INTERNAL-ONLY | Bridge walk in `*_OnHutDeath` fallback path | gamemd: `param_2` discriminator (2=NS, 4=EW), 6 tile-type globals + `CellClass+0x11A` sub-tile check | Rust: bridge span extent/endpoint detection uses overlay-band boundary check rather than tile-type globals; overlay leaving `[0x4A..0x65]` terminates walk (bridge_orchestrator.rs:789–795) | None — same observable: walk stops at bridge endpoint | gamemd: fn-IsLowBridgeEndpointTile.md §Behavioral Analysis; Rust: bridge_orchestrator.rs:789–795 |
| `BuildingTypeClass::ReadINI` — `BridgeRepairHut` field stored at `+0x16B6` | INTERNAL-ONLY | `rules(md).ini` load, `[CABHUT]` section | gamemd: `CCINIClass::ReadBool` → `BuildingTypeClass+0x16B6 = 1` for CABHUT | Rust: `rules.object(id).bridge_repair_hut` parsed from `BridgeRepairHut=yes` in INI section (object_type.rs and ruleset.rs) | None — same observable: CABHUT flagged as bridge repair hut in both | gamemd: fn-BuildingTypeReadINI-BridgeRepairHut.md §Key Findings; Rust: src/rules/object_type.rs, src/rules/ruleset.rs |
| `RulesClass::ReadAudioVisual` — `RepairBridgeSound` INI key parsed to `RulesClass+0x248` | MATCH | `[AudioVisual] RepairBridgeSound=` in `rules(md).ini`; engineer completes bridge repair | gamemd: `VocClass::FindByName` stores VocIndex at `RulesClass+0x248`; emitted in `InfantryClass::PerCellProcess` bridge-repair completion branch via `VocClass::PlayAt` at engineer position | Rust: `RepairBridgeSound` parsed from `[AudioVisual]` section (ruleset.rs:789–793), stored as `Option<String>`; `GameSoundEvent::BridgeRepaired { sound_id, .. }` emitted on repair completion at engineer position (audio/events.rs:158–161) | None — same observable: `BridgeRepaired` sound plays at engineer position when repair completes | gamemd: fn-ReadAudioVisual-RepairBridgeSound.md §Summary + §Sound-Emit Callsite; Rust: src/rules/ruleset.rs:789–793, src/audio/events.rs:158–161 |
| `BuildingClass` C4/Crewed-pending flag (`+0x6DF`) | INTERNAL-ONLY | Second SEAL clicks already-targeted CABHUT | gamemd: `BuildingClass+0x6DF != 0` early-return in C4-plant callsite (write: 0x0051A5A7 set=1, 0x00440320 clear=0); flag also doubles as Crewed-survivor spawn cooldown (0x00701F45 set=1) | Rust: `pending_c4_detonation: Option<PendingC4Detonation>` on building entity — `Some` ≡ flag set; Crewed survivor handled by a separate Rust mechanism. `tick_c4_plants` Phase 1 skips buildings with existing `pending_c4_detonation` | None — same observable: second SEAL trying to C4 an already-targeted building does nothing | gamemd: struct-BuildingClass-C4-fields.md §+0x6DF write-sites; Rust: src/sim/world/world_orders.rs |
| `BuildingClass` C4 frame stamp (`+0x528`, sentinel −1) | INTERNAL-ONLY | C4 planted; timer checked each frame | gamemd: `BuildingClass+0x528` = frame counter at plant time; sentinel −1 = not started; timer check: `g_CurrentFrameCounter − stamp >= duration` | Rust: `pending.plant_start_tick: u64` (tick counter); `Option` serves as sentinel — `None` ≡ not started; timer: `self.tick.saturating_sub(pending.plant_start_tick) < delay` → skip | None — same observable: C4 detonates after `c4_delay` frames from plant | gamemd: struct-BuildingClass-C4-fields.md §+0x528; Rust: src/sim/world/world_orders.rs:548 |
| `BuildingClass` C4 countdown ticks (`+0x530`, per-instance) | INTERNAL-ONLY | Any building C4 planted | gamemd: `BuildingClass+0x530` stores countdown ticks per instance (written once at plant time) | Rust: `rules.c4_delay_ticks` — global constant read from INI; same numeric value used for all buildings. No per-instance storage needed | None — same observable: detonation timing matches INI `C4Delay=` value | gamemd: struct-BuildingClass-C4-fields.md §+0x530; Rust: src/sim/world/world_orders.rs |
| `BuildingClass` C4 planter pointer (`+0x540`) | INTERNAL-ONLY | C4 planted by infantry; SEAL dies before detonation | gamemd: `BuildingClass+0x540` = raw `InfantryClass*`; cleared to 0 after bridge dispatch (0x00440327); not written in Crewed path | Rust: `pending.attacker_id: u64` entity ID; entity lookup handles dangling attacker gracefully (`get_unit_opt`) | None — same observable: if attacker dies before detonation, bridge collapse still fires; attacker credit may differ but bridge collapse output is same | gamemd: struct-BuildingClass-C4-fields.md §+0x540; Rust: src/sim/world/world_orders.rs |
| `BuildingClass` C4 hit-location Y (`+0x52C`, Crewed path only) | INTERNAL-ONLY | C4 detonates on Crewed-building CABHUT | gamemd: `BuildingClass+0x52C` = hit-location Y; written in Crewed-survivor spawn path; NOT written or read in bridge-destruction path | Rust: no equivalent stored for bridge C4 path; Crewed survivor handling uses separate mechanism without this field | None — bridge path: neither gamemd nor Rust uses hit-Y for bridge collapse. Observable bridge collapse output identical | gamemd: struct-BuildingClass-C4-fields.md §+0x52C note (Crewed path only); Rust: src/sim/world/world_orders.rs |
| `BuildingTypeClass` `CanC4` (`+0x1577`, default 1) | INTERNAL-ONLY | Any building checked for C4 cursor / targeting eligibility | gamemd: `BuildingTypeClass+0x1577` = byte; constructor writes `MOV BYTE [ESI+0x1577], 1` (0x0045E063); INI key `CanC4` at 0x0081ADFC overrides | Rust: `can_c4: bool`; default `true` for `ObjectCategory::Building`, `false` otherwise (object_type.rs); INI override same key | None — same observable: C4 cursor shows on all buildings by default; `CanC4=no` disables it | gamemd: struct-BuildingTypeClass-BridgeFields.md §+0x1577; Rust: src/rules/object_type.rs |
| `BuildingTypeClass` `BridgeRepairHut` (`+0x16B6`, default 0) | INTERNAL-ONLY | CABHUT vs any other building targeted with C4 | gamemd: `BuildingTypeClass+0x16B6` = byte; default 0 in constructor; `ReadBool "BridgeRepairHut"` at 0x00460E8D stores to `+0x16B6` | Rust: `bridge_repair_hut: bool`; default `false` (object_type.rs); parsed from `BridgeRepairHut=yes` | None — same observable: only CABHUT routes to bridge-collapse path; all other buildings use standard C4 damage | gamemd: struct-BuildingTypeClass-BridgeFields.md §+0x16B6; Rust: src/rules/object_type.rs |
| `BuildingTypeClass` `InvisibleInGame` (`+0x1701`, default 0) | INTERNAL-ONLY | CABHUT render / selection check | gamemd: `BuildingTypeClass+0x1701` = byte; default 0 in constructor; `ReadBool "InvisibleInGame"` at 0x0081A8CC | Rust: `invisible_in_game: bool`; default `false`; parsed from `InvisibleInGame=yes` (object_type.rs) | None — same observable: no stock building with this flag set; CABHUT is visible in both | gamemd: struct-BuildingTypeClass-BridgeFields.md §+0x1701; Rust: src/rules/object_type.rs |
| `ObjectTypeClass` `Immune` (`+0x233`) — gate investigation | INTERNAL-ONLY | CABHUT targeted by C4; CABHUT targeted by weapon fire | gamemd: `Immune` gates `ObjectClass::ReceiveDamage` (early-out if Immune=1); NOT checked in C4 plant path (`InfantryClass::PerCellProcess` dispatches on `BridgeRepairHut` flag, not `Immune`). CABHUT has `Immune=yes` in rulesmd.ini → takes zero weapon damage, but C4 fires unconditionally | Rust: `Immune` field not implemented in `src/rules/object_type.rs`. C4 path (`tick_c4_plants`, `world_orders.rs`) does not check `Immune` — matches gamemd's behavior on the C4 branch. Weapon ReceiveDamage path would need `Immune` but is outside bridge mechanic scope | None for bridge C4 path — both gamemd and Rust skip `Immune` when processing C4-on-CABHUT. Weapon-fire immunity for CABHUT is out-of-scope for this parity report | gamemd: struct-ObjectTypeClass-GateFields.md §Gate Investigation Conclusion + §+0x233; Rust: src/rules/object_type.rs (field absent), src/sim/world/world_orders.rs (no immune check) |
| `ObjectTypeClass` `Bombable` (`+0x22E`), `LegalTarget` (`+0x231`), `Insignificant` (`+0x232`) | INTERNAL-ONLY | Any building; CABHUT targeting / spy-bomber / EVA | gamemd: `Bombable` = spy-bomber targeting eligibility; `LegalTarget` = click-to-attack eligibility (subclass constructors set 1 for buildings); `Insignificant` = suppresses "unit destroyed" EVA. All three are read by combat/targeting code, not by bridge mechanic functions | Rust: none of the three fields are implemented in `src/rules/object_type.rs`. Bridge mechanic code paths do not reference them in either gamemd or Rust | None for bridge mechanic — these fields affect combat/targeting/EVA only, not C4 bridge collapse. Targeting/EVA parity is out-of-scope for this report | gamemd: struct-ObjectTypeClass-GateFields.md §+0x22E, §+0x231, §+0x232; Rust: src/rules/object_type.rs (fields absent) |
| `CellClass + 0x44` — overlay type index (bridge band detection) | INTERNAL-ONLY | 5×5 scan cell; any cell in collapse walker path | gamemd: `*(int *)(cell + 0x44)` = overlay type index; ranges `(0x49, 0x66)` (low) and `(0xCC, 0xE9)` (high) identify bridge body cells | Rust: `bridge_facts.overlay_id: Option<u8>` in `BridgeCellFacts`; range checks performed in `is_low_destroy_overlay` / `is_high_destroy_overlay` (bridge_state/walker.rs:34–40). Same numeric ranges, different storage (u8 vs int) | None — same observable: bridge body cells correctly identified in both engines | gamemd: struct-CellClass-BridgeFields.md §+0x44; Rust: src/map/bridge_facts.rs:56, src/sim/bridge_state/walker.rs:34–40 |
| `CellClass + 0x140` — bridge flags bitmask (0x80/0x100/0x400/0x800) | INTERNAL-ONLY | Bridge walk anchor resolution and direction selection | gamemd: `*(uint *)(cell + 0x140)` masks: `0x80`=self-anchor, `0x100`=body, `0x400`=bridgehead, `0x800`=direction. Composite `0x500` = bridge-present gate | Rust: `bridge_facts.raw_flags: u32` with constants `BRIDGE_FLAG_ANCHOR_SELF=0x80`, `BRIDGE_FLAG_STRUCTURAL=0x100`, `BRIDGE_FLAG_DESTROYED_OR_RAMP=0x400`, `BRIDGE_FLAG_DIRECTION_ZERO=0x800`. `HUT_FALLBACK_STARTER_MASK = 0x100|0x400 = 0x500` (bridge_orchestrator.rs:364). Bitmask values identical; naming diverges (`DESTROYED_OR_RAMP` vs gamemd's "bridgehead") | None — bitmask values identical; walk branching logic produces same result | gamemd: struct-CellClass-BridgeFields.md §+0x140; Rust: src/map/bridge_facts.rs:3–7, src/sim/world/bridge_orchestrator.rs:364 |
| `CellClass + 0x24` — cell grid coordinate (packed short×2) | INTERNAL-ONLY | Walk anchor extraction and cell indexing | gamemd: `*(short **)(cell + 0x24)` = packed `(x: short, y: short)` as anchor for bridge walk start | Rust: `(rx: u16, ry: u16)` fields on `ResolvedTerrainCell` — unpacked at map build time. Bridge orchestrator accesses cell by `(u16, u16)` pair, not by pointer walk | None — same observable: walk starts from correct cell coordinate | gamemd: struct-CellClass-BridgeFields.md §+0x24; Rust: src/map/resolved_terrain.rs:75–76 |
| `CellClass + 0x2C` — neighbor cell pointer (bridgehead anchor) | INTERNAL-ONLY | Bridge body cells without `0x80` flag; anchor resolution | gamemd: `*(CellClass **)(cell + 0x2C)` = pointer to associated bridgehead cell; anchor coord read from neighbor's `+0x24` | Rust: `bridge_facts.anchor: Option<BridgeAnchorRelation>` stores `anchor: (u16, u16)` pre-resolved at map load; no pointer indirection at runtime (bridge_facts.rs:29–35) | None — same observable: walk anchor resolves to same bridgehead cell coordinate | gamemd: struct-CellClass-BridgeFields.md §+0x2C; Rust: src/map/bridge_facts.rs:29–35 |
| `CellClass + 0x38` — tile type index (ramp/endpoint detection) | INTERNAL-ONLY | `IsBridgeRampTile` and `IsLowBridgeEndpointTile` callers | gamemd: `*(int *)(cell + 0x38)` = theater tile-type index; subtracted from runtime tile-set base globals for relative ramp/endpoint checks | Rust: `final_tile_index: i32` on `ResolvedTerrainCell`; bridge ramp detection uses `is_wood_bridge_repair_tile: bool` pre-resolved at map load (resolved_terrain.rs:83–84); endpoint/transition uses `bridge_transition: bool` flag | None — same observable: ramp and endpoint cells correctly classified during bridge walk | gamemd: struct-CellClass-BridgeFields.md §+0x38; Rust: src/map/resolved_terrain.rs:83–84, 155 |
| `CellClass + 0x11A` — sub-tile index (ramp orientation byte) | INTERNAL-ONLY | `IsBridgeRampTile` — 6 sub-tile orientation checks | gamemd: `*(signed char *)(cell + 0x11A)` = frame within tile type's sheet; sub-tile values `0x0C, 0x04, 0x08, 0x02` identify ramp orientation | Rust: `final_sub_tile: u8` on `ResolvedTerrainCell` (resolved_terrain.rs:80); ramp orientation classification pre-resolved into `canonical_ramp: Option<RampDirection>` at map build (resolved_terrain.rs:118) | None — same observable: ramp orientation correctly classified; bridge walk uses same semantic | gamemd: struct-CellClass-BridgeFields.md §+0x11A; Rust: src/map/resolved_terrain.rs:78–80, 118 |
| `InfantryTypeClass + 0xEC2` — `C4=` flag | INTERNAL-ONLY | SEAL/Tanya selected, enemy building targeted | gamemd: `InfantryTypeClass+0xEC2` = byte; INI key `C4=` in infantry section; `yes` for SEAL and Tanya; gates C4-plant branch in `InfantryClass::PerCellProcess` | Rust: `c4: bool` on `ObjectType` (object_type.rs:639); parsed via `section.get_bool("C4").unwrap_or(false)` (object_type.rs:1084); used in C4 cursor display and `tick_c4_plants` Phase 1 | None — same observable: SEAL and Tanya show Demolish cursor and trigger C4 plant; other infantry do not | gamemd: struct-InfantryTypeClass-C4-Engineer.md §+0xEC2; Rust: src/rules/object_type.rs:639, 1084 |
| `InfantryTypeClass + 0xEC3` — `Engineer=` flag | INTERNAL-ONLY | Engineer enters CABHUT for bridge repair | gamemd: `InfantryTypeClass+0xEC3` = byte; INI key `Engineer=` in infantry section; gates bridge-repair branch in `InfantryClass::PerCellProcess` | Rust: `engineer: bool` on `ObjectType` (object_type.rs:495); parsed via `section.get_bool("Engineer").unwrap_or(false)` (object_type.rs:1022) | None — same observable: only Engineer-flagged infantry can repair bridges | gamemd: struct-InfantryTypeClass-C4-Engineer.md §+0xEC3; Rust: src/rules/object_type.rs:495, 1022 |
| `InfantryTypeClass + 0xEBE` — aggregate enter-building gate (C4 or Engineer) | INTERNAL-ONLY | Any infantry attempting to enter a building | gamemd: `+0xEBE` is set by post-INI merge pass if `C4=yes` OR `Engineer=yes`; used as primary enter-building gate in `PerCellProcess` before checking specific action | Rust: no pre-computed aggregate field; `c4` and `engineer` are checked independently at each call site in sim logic | None — same observable: same infantry types enter buildings (same INI flags drive the gate) | gamemd: struct-InfantryTypeClass-C4-Engineer.md §+0xEBE; Rust: src/rules/object_type.rs (no aggregate field) |
| `RulesClass__ReadCombatDamage` — `BridgeStrength` stored at `RulesClass + 0x1740` | INTERNAL-ONLY | Bridge tile struck by weapon fire | gamemd: `CCINIClass::ReadInt` reads `[CombatDamage] BridgeStrength=` and stores to `RulesClass+0x1740`; default 1500; controls bridge tile HP vs weapon damage | Rust: `rules.bridge_rules.strength: u16` (ruleset.rs:738), parsed from `[CombatDamage] BridgeStrength=` with default 1500 (ruleset.rs:773–777); used in `bridge_re.rs` damage gating | None — same observable: bridge tiles survive weapon fire up to 1500 accumulated damage in both | gamemd: fn-ReadCombatDamage-BridgeStrength.md §Key Findings; Rust: src/rules/ruleset.rs:738, 773–777 |
| `InfantryClass::PerCellProcess` — C4 plant guards (IronCurtain check, already-planted check) | INTERNAL-ONLY | SEAL targets IronCurtained building; second SEAL targets already-claimed building | gamemd: IronCurtain guard via `building.vtable+0x160` (`IsIronCurtained`) → C4 not planted if active; already-planted guard via `BuildingClass+0x6DF != 0` → infantry reroutes | Rust: IronCurtain guard via `invulnerability.is_some()` on entity (world_orders.rs:443–453); already-claimed guard via `pending_c4_detonation.is_some()` (world_orders.rs:469–476) | None — same observable: IronCurtained buildings can't be C4'd; second SEAL on claimed target is no-op | gamemd: fn-InfantryClass-PerCellProcess-C4Plant.md §Step 1–2; Rust: src/sim/world/world_orders.rs:443–476 |
| **`InfantryClass::PerCellProcess` — BridgeRepairHut branch: immediate bridge collapse vs. timer (DRIFT — HIGH, same root as rows 9–10)** | **DRIFT** | SEAL/Tanya arrives at CABHUT cell (Mission_Sabotage + C4=yes + BridgeRepairHut building) | **gamemd: skips `+0x6DF` write and all timer fields; calls `ProcessBridgeDestruction_Low` or `ProcessBridgeDestruction_High` IMMEDIATELY on cell arrival; then `building.KillAnim()`. No timer, no plant marker.** | **Rust: `tick_c4_plants` Phase 1 sets `pending_c4_detonation` and routes CABHUT through same timer path as normal buildings. Bridge collapse fires after `c4_delay_ticks` frames — not immediately. Root cause confirmed: Rust has no `InfantryClass::PerCellProcess`-equivalent immediate dispatch for the BridgeRepairHut branch.** | **Player-visible: in gamemd bridge collapses the frame the SEAL arrives; in Rust it is delayed by the C4 countdown timer. Underlying cause of the confirmed "C4 on CABHUT does nothing" bug (project_c4_bridge_hut_followup.md).** | gamemd: fn-InfantryClass-PerCellProcess-C4Plant.md §Summary state machine lines 254–259, §Bridge-Specific Path; Rust: src/sim/world/world_orders.rs:423–517 (no immediate BridgeRepairHut branch) |
| `InfantryClass::PerCellProcess` — normal C4 target: plant timer writes to building | INTERNAL-ONLY | SEAL targets normal building (not BridgeRepairHut) | gamemd: sets `BuildingClass+0x6DF=1`, `+0x528`=frame stamp, `+0x52C`=coord_y, `+0x530`=countdown, `+0x540`=infantry ptr; then stop-moving + route to plant position | Rust: sets `pending_c4_detonation = Some(PendingC4Detonation { plant_start_tick: self.tick, attacker_id })` (world_orders.rs:494–498); stops movement (world_orders.rs:501–505); plays C4Planted sound (world_orders.rs:510–516) | None — same observable for normal buildings: C4 planted, SEAL stops and arms, building dies after delay | gamemd: fn-InfantryClass-PerCellProcess-C4Plant.md §Step 3–5; Rust: src/sim/world/world_orders.rs:492–517 |
| INI string `"RepairBridgeSound"` at `0x0083A7FC` — single xref, `[AudioVisual]` section | MATCH | `rules(md).ini` load | gamemd: string at `0x0083A7FC`, single xref from `RulesClass::ReadAudioVisual`; section `[AudioVisual]`; stock value `BridgeRepaired` (rulesmd.ini:721) | Rust: `section.get_str("RepairBridgeSound")` in `BridgeRules::from_ini` — same INI key, same section, same stock value (ruleset.rs:789–793) | None — same INI key name, same section, same default | gamemd: string-RepairBridgeSound.md §String Verification + §INI Read Callsite; Rust: src/rules/ruleset.rs:789–793 |
| INI string `"BridgeStrength"` at `0x0083AD90` — single xref, `[CombatDamage]` section | MATCH | `rules(md).ini` load | gamemd: string at `0x0083AD90`, single xref from `RulesClass::ReadCombatDamage`; section `[CombatDamage]`; stock value 1500 (rulesmd.ini:816) | Rust: `section.get_i32("BridgeStrength")` in `BridgeRules::from_ini` — same INI key, same section, stock default 1500 (ruleset.rs:773–777) | None — same INI key name, same section, same default | gamemd: string-BridgeStrength.md §String Verification + §INI Read Callsite; Rust: src/rules/ruleset.rs:773–777 |
| `ProcessBridgeDestruction_Low` — 5×5 scan order (row-major: dy outer, dx inner) | MATCH | Engineer enters CABHUT; repair-path 5×5 scan | gamemd: `for (dy=-2; dy<3; dy++) { for (dx=-2; dx<3) { ... } }` — dy outer, dx inner (row-major); first hit dispatches to `RepairBridge_Low` | Rust: `cells_in_5x5_scan`: `(-2..=2i32).flat_map(move |dy| (-2..=2i32).filter_map(move |dx| ...))` — dy outer, dx inner (bridge_state/mod.rs:1631–1641) | None — same observable: repair dispatches from first matching cell in identical row-major order | gamemd: fn-ProcessBridgeDestruction-Low.md §Phase 1; Rust: src/sim/bridge_state/mod.rs:1629–1642 |
| `ProcessBridgeDestruction_Low` — low-bridge overlay fast path (fast-path to `RepairBridge_Low`) | INTERNAL-ONLY | 5×5 scan finds low-bridge overlay `(0x49, 0x66)` in engineer neighbourhood | gamemd: calls `RepairBridge_Low(coord + (dx,dy))` and returns immediately on first match | Rust: `repair_bridge_low_from_scan` iterates scan cells, returns on first `is_low_repair_overlay` hit via `repair_bridge_low` (walker.rs:83–98). Same observable: repair fires from first matching cell | None — same observable outcome | gamemd: fn-ProcessBridgeDestruction-Low.md §Phase 1 fast path; Rust: src/sim/bridge_state/walker.rs:83–98 |
| `ProcessBridgeDestruction_Low` — fallback flag walk (no overlay in 5×5) | INTERNAL-ONLY | No low-bridge overlay found in 5×5 neighbourhood | gamemd: reads `CellClass+0x140 & 0x500`; 8-direction walk up to 3 steps; branch by 0x100/0x400/0x80/0x800 flags | Rust: `repair_bridge_from_engineer_scan` outer dispatch: if no `is_low_repair_outer_candidate` found, falls through to high-repair scan (walker.rs:71–80). Fallback walk within repair walkers uses span-start finders. | None — same observable: repair finds anchor cell and fires even without overlay in immediate neighbourhood | gamemd: fn-ProcessBridgeDestruction-Low.md §Phase 2; Rust: src/sim/bridge_state/walker.rs:65–80 |
| `ProcessBridgeDestruction_Low` — zone rebuild after repair (`ValidateBridgeZones` → `UpdateBridgeZonesHelper`) | MATCH | After any bridge repair completes | gamemd: `if (ValidateBridgeZones_result) UpdateBridgeZonesHelper()` | Rust: `outcome.zones_dirty` gate in `tick_bridge_repair_orders` triggers zone rebuild (world_orders.rs:383); `repair_bridge_from_engineer_scan` sets `zones_dirty` on successful repair | None — same observable: zone graph rebuilt after repair, pathfinding reflects repaired bridge | gamemd: fn-ProcessBridgeDestruction-Low.md §Phase 4; Rust: src/sim/world/world_orders.rs:383 |
| ``ProcessBridgeDestruction_High`` — 5x5 scan order (row-major: dy outer, dx inner) | MATCH | Engineer enters CABHUT; repair-path 5x5 scan for high bridge | gamemd: iVar10 (dy) outer loop -2..+2, iVar9 (dx) inner loop -2..+2 in decompilation; first hit dispatches to RepairBridge_High | Rust: same cells_in_5x5_scan: (-2..=2).flat_map(move |dy| (-2..=2).filter_map(move |dx| ...)) — dy outer, dx inner (bridge_state/mod.rs:1631-1641) | None — same observable: repair dispatches from first matching cell in identical row-major order | gamemd: fn-ProcessBridgeDestruction-High.md §Entry Scan; Rust: src/sim/bridge_state/mod.rs:1629-1642 |
| ``ProcessBridgeDestruction_High`` — high-bridge overlay fast path ([0xCC..0xE9] -> RepairBridge_High) | INTERNAL-ONLY | 5x5 scan finds high-bridge overlay in engineer neighbourhood | gamemd: (0xcc < cell+0x44) && (cell+0x44 < 0xe9) -> MapClass__RepairBridge_High + return | Rust: is_high_repair_overlay checks (0xCD..=0xE8).contains(&overlay) -> repair_bridge_high (walker.rs:100-115). Same range, different dispatch mechanism | None — same observable: repair fires from first matching high-bridge cell | gamemd: fn-ProcessBridgeDestruction-High.md §Entry Scan; Rust: src/sim/bridge_state/walker.rs:100-115 |
| ``ProcessBridgeDestruction_High`` — zone rebuild after repair | MATCH | After any high bridge repair completes | gamemd: ValidateBridgeZones -> if non-zero UpdateBridgeZonesHelper; then if cells queued RecalcCellsAndRebuildZones | Rust: outcome.zones_dirty gate in tick_bridge_repair_orders triggers zone rebuild (world_orders.rs:383) | None — same observable: zone graph rebuilt after repair | gamemd: fn-ProcessBridgeDestruction-High.md §Zone Rebuild Sequence; Rust: src/sim/world/world_orders.rs:383 |
| ``RepairBridge_Low`` — low-bridge overlay axis split (NS [0x4A..0x52]+[0x5C..0x5F]+{0x64} vs EW [0x53..0x5B]+[0x60..0x63]+{0x65}) | MATCH | Engineer repair; axis identification from overlay byte | gamemd: NS case overlay in [0x4A..0x52] OR [0x5C..0x5F] OR == 0x64; EW case overlay in [0x53..0x5B] OR [0x60..0x63] OR == 0x65; unclassified [0x5C..0x5F] gap falls through | Rust: is_ns_walker_overlay_low: (0x4A..=0x52) || (0x5C..=0x5F) || == 0x64; is_ew_walker_overlay_low: (0x53..=0x5B) || (0x60..=0x63) || == 0x65 (walker.rs:609-618). Identical ranges | None — same observable: same overlay bytes route to same axis walkers | gamemd: fn-RepairBridge-Low.md §Overlay Band Analysis; Rust: src/sim/bridge_state/walker.rs:609-618 |
| ``RepairBridge_Low`` — walker start-cell anchor detection (3-case: Y-1 absent -> Y+1; Y-2 present -> Y-1; else Y) | INTERNAL-ONLY | Low bridge NS repair; finding canonical span-start cell | gamemd: checks (X,Y-1) overlay band; if absent -> walker starts at Y+1; else checks (X,Y-2); if present -> walker at Y-1; else walker at Y | Rust: find_walker_start_low_ns (walker.rs:553-572) implements same 3-case: north_off -> ry+1; north2_on -> ry-1; else ry. EW mirror in find_walker_start_low_ew (walker.rs:574-592) | None — same observable: walker always starts at canonical span anchor cell regardless of which cell was first hit | gamemd: fn-RepairBridge-Low.md §NS Branch + §EW Branch; Rust: src/sim/bridge_state/walker.rs:553-592 |
| ``RepairBridge_High`` — high-bridge overlay axis split (NS [0xCD..0xD5]+[0xDF..0xE2]+{0xE7} vs EW [0xD6..0xDE]+[0xE3..0xE6]+{0xE8}) | MATCH | Engineer repair; axis identification from overlay byte on high bridge | gamemd: NS sub-range [0xCD..0xD5]|[0xDF..0xE2]|0xE7; EW sub-range [0xD6..0xDE]|[0xE3..0xE6]|0xE8 (decompile_function 0x0057F440 verified) | Rust: is_ns_walker_overlay_high: (0xCD..=0xD5) || (0xDF..=0xE2) || == 0xE7; is_ew_walker_overlay_high: (0xD6..=0xDE) || (0xE3..=0xE6) || == 0xE8 (walker.rs:597-607). Identical ranges | None — same observable: same overlay bytes route to same axis walkers for high bridges | gamemd: fn-RepairBridge-High.md §Overlay Sub-Range to Direction Mapping; Rust: src/sim/bridge_state/walker.rs:597-607 |
| ``RepairBridge_High`` — walker start-cell anchor detection (3-case NS/EW, parallel to Low) | INTERNAL-ONLY | High bridge NS/EW repair; finding canonical span-start cell | gamemd: checks (X,Y-1) for NS and (X-1,Y) for EW; 3 cases as per RepairBridge_Low but high-band overlay range; FUN_00588C60 fallback for EW X-2 | Rust: find_walker_start_high_ns (walker.rs:509-530) and find_walker_start_high_ew (walker.rs:532-551) implement same 3-case logic for high range (0xCD..=0xE8) | None — same observable: walker starts at canonical span anchor cell | gamemd: fn-RepairBridge-High.md §Logic - Direction Detection and Dispatch; Rust: src/sim/bridge_state/walker.rs:509-551 |
| ``DestroyBridge_Low`` — full low-bridge overlay range [0x4A..0x65] and NS/EW axis split | MATCH | Any bridge cell struck by weapon fire or collapse walker | gamemd: overlay ∉ [0x4A..0x65] -> return 0 (not-bridge signal); NS split [0x4A..0x52]+[0x5C..0x5F]+{0x64}; EW split [0x53..0x5B]+[0x60..0x63]+{0x65} | Rust: is_low_destroy_overlay: (0x4A..=0x65); is_ns/ew_walker_overlay_low same sub-ranges as gamemd (walker.rs:34-36, 609-618) | None — same observable: same overlay bytes classified as NS or EW low bridge | gamemd: fn-DestroyBridge-Low.md §Overlay Range Summary; Rust: src/sim/bridge_state/walker.rs:34-36, 609-618 |
| ``DestroyBridge_Low`` — walker start-cell anchor detection (3-case: X/Y-1 absent -> +1; X/Y-2 present -> -1; else self) | INTERNAL-ONLY | Low bridge cell struck; canonical span-start required | gamemd: NS path checks Y-1, Y-2; EW path checks X-1, X-2; same 3-case as RepairBridge_Low but for destruction | Rust: reuses same find_walker_start_low_ns / find_walker_start_low_ew (walker.rs:553-592) for both repair and destroy paths | None — same observable: walker always starts at canonical span anchor | gamemd: fn-DestroyBridge-Low.md §NS path + §EW path; Rust: src/sim/bridge_state/walker.rs:553-592 |
| ``DestroyBridge_Low`` — not-bridge return signal (overlay ∉ [0x4A..0x65] -> return 0) | INTERNAL-ONLY | CollapseBridge walker hits non-bridge cell during retry | gamemd: returns uVar4 & 0xFFFFFF00 = 0 when overlay out-of-band; signals CollapseBridge to retry up to 3 times | Rust: destroy_bridge_low returns StateOutcome::NoChange when is_low_destroy_overlay false (walker.rs:487-491); collapse walker handles retry internally via separate retry counter | None — same observable: collapse walker retries correctly when hitting non-bridge cells | gamemd: fn-DestroyBridge-Low.md §Return value + §Summary; Rust: src/sim/bridge_state/walker.rs:487-491 |
