# Inactive high-bridge restamp: bounded retail witness

The increment repairs a stock `Deadman.mmx` fresh-load omission: inactive high-bridge gap cells now receive native400/800 flags, so the empty clear witness `(57,42)` rejects ore growth through the existing production gate. A separate synthetic restamp fixture exercises committed building-placement rejection. The pre-fix evidence checkpoint is `4f72210f`, based on `6c7ccf92`. Validated source/evidence checkpoint `8b147e5e` is recorded below; this is **not complete native startup or Phase3 parity**.

## Retail selection and Rust result

`.local/restamp-retail-map-hashes.json` contains actual SHA256 receipts for four map inputs. `BayOPigs.mmx` produced two active records, `Hills.mmx` three active records, extracted `all01umd.map` zero records. `Deadman.mmx` (SHA256 `8d17f1937e7215ae2bd210506640172e954cc00efa2e620a64717b4da132890d`) produced four inactive non-Tube high records:

- `(54,41) -> (62,41)`
- `(71,41) -> (83,41)`
- `(54,90) -> (62,90)`
- `(71,90) -> (79,90)`

All have group0 and active=false. The ordinary in-grid diagnostic found 20 interior cells missing0x100 and 80 distinct transverse cells. This diagnostic deduplicates candidate cells and does not model native write order/aliases/dummy behavior.

Selected witness `(57,42)` is inside the playfield: Clear tile0/subtile0, land0, level0, slope0, flags0, no overlay, no Techno occupancy, no terrain object, ground-walkable. At the pre-fix checkpoint, production `can_place_new_tiberium`, with actual rules/terrain/overlay/occupancy, returns true. The diagnostic conservatively excludes every terrain-object cell from new placement; since this witness is empty, the stricter set does not weaken its positive result.

Export `.local/restamp-deadman-export.json` has9035 allocated scalar cells, source Size/LocalSize, actual838-entry theater tile AllowTiberium table, bridge bases, land Buildable values and complete records. Parent `.local/restamp-retail-gate-provenance.json` binds `[TileSet0000] AllowTiberium=true` and `[Clear] Buildable=yes` to extracted retail bytes and original reader stores. No invalid-tile bypass is used.

## Original executable comparison

[`bridge_restamp_retail.py`](../../tools/spatial_oracle/bridge_restamp_retail.py) runs hash-checked original56D6E0 (including clear; adequate record backing supplied afterward), retains its output records in place, runs586BF0, then4838E0 on the empty witness. Native producer output exactly matches the four record semantic fields above; raw record bytes remain unchanged through restamp. There are80 native flag writes and80 changed cells. At `(57,42)` the flag change is0→0xC00. Native4838E0 returns AL1 before restamp and AL0 afterward; pre-fix Rust admission is true. The retained [native receipt](../../tools/spatial_oracle/bridge_restamp_retail.json) can be checked with `python -B -m tools.spatial_oracle.bridge_restamp_retail --check` after generating the scalar export at evidence checkpoint `4f72210f`; no retail map bulk is committed. The repaired loader intentionally exports post-restamp state, so it must not regenerate this pre-fix golden. Preserve the original export or regenerate it from that checkpoint. The current diagnostic separately asserts every native post-state flag and production ore rejection.

Saved result/log: `.local/restamp-native-deadman.json` and `.local/restamp-native-deadman.log`, actual exit0. No executable patches or substituted returns. The actual original playfield and fixed-cell lookup execute. Ground object list is empty and therefore legitimately bypasses virtual object predicates.

**Boundary:** scalar input comes from the current Rust retail loader, not native loader execution. Native684C30's intervening connectivity and all-zone-level calls are omitted. A fresh independent preservation audit establishes the relevant boundary: 56C510/56CB90 write separate zone/adjacency/work allocations; record loop56C6E6 reads active+8 and56C6EF skips these inactive records. 581F50→581F90(2,1,0)→42C1C0 write separate hierarchy/pathfinder stores; loop582346 skips582D70 for inactive records. Flood5824A0 writes hierarchy/temp edges and calls578460 for Cell reads. Thus, under normal disjoint allocations, record endpoints/type/activity and real Cell100/400/800 survive the omitted calls. Dummy coordinates may change; this is not full-zone execution or terminal-dummy startup parity. This retail probe exercises horizontal OR0xC00 on ordinary allocated cells. The separate bounded fixture corpus below covers vertical clearing, retained dummy and write order; persistence has a Rust lifecycle regression and original raw-body evidence. The script's map hash is linked to the separate actual-file receipt; its scalar export is directly hashed.

## Production and persistence boundary

`bridge_state/gap_restamp.rs` owns the reverse-record, packed-word, fixed-cell transaction. `scenario_post_map::finalize_scenario_post_map` publishes it once at the fresh-load tail before particle/twinkle creation. Generic navigation rebuilding and snapshot restore do not replay it. The existing real-cell value plane now retains1D80 (1180 plus400/800), including saved zero values. Its historical field/type names remain for continuity; snapshot141 rejects older layouts lacking the new semantic authority. Runtime Mark/Destroy setters preserve the same authority:400 depends on `set==0`, while800 independently depends on `direction==0`. A fresh critic caught and corrected a conditional that lost800 for destruction in direction0; both the retained writer and full `BridgeCellFacts` writer agree.

Real-cell serialization evidence: original CellSave483C10 calls AbstractSave410320, whose virtual size query resolves through Cell vtable7E4EEC+30 to487E70 (`B8 48 01 00 00 C3`, size148). Raw DWORD140 is included. CellLoad4839F0 calls410380 for that raw body; its tail resets34/4C/FC and swizzles pointers without replacing140. This is allocated-Cell body evidence, not full native save execution or global-dummy enrollment. Rust's process dummy retains400/800 live and hashes them; native Resize reconstruction resets it. Its existing exclusion from serialized Scenario payload remains deliberate.

[`bridge_gap_flags.py`](../../tools/spatial_oracle/bridge_gap_flags.py) / [golden](../../tools/spatial_oracle/bridge_gap_flags.json) execute20 original586BF0 cases: both axes and decreasing walks, reverse overlap order, preserved full real bits, structural skips, repeated shared-dummy writes/identity, fixed aliases and transverse WORD wrapping. An additional48 original setter anchor prefixes cover both families, three directions, both set values and four initial words; they stop immediately after the flag store before conditional bridge blowup. Neighbor/collapse lifecycle is not claimed from those prefixes. Non-axis malformed loops and dummy bits outside retained1D80 remain outside this corpus.

Rust checks exercise ordered native observations, actual committed building-placement rejection after restamp, real saved flags and saved zeros across complete map-authority restore, canonical hash changes, retained dummy reset and later setters. The retail ignored check compares80 native post-state cells and the actual ore admission gate. These are bounded delivery checks, not a broad map-loader or placement-policy certification.

## Actual validation receipts

Both diagnostic commands used `cargo test -p vera20k --lib sim::movement::movement_bridge_retail_tests::bridge_restamp_retail_probe::retail_inactive_high_record_restamp_inventory -- --ignored --exact --nocapture` with a process-idle check. Four-map default:1passed0failed8721filtered,3.67s run/3m58 compile, exit0 (`restamp-retail-inventory.log`). Frozen extended export with `VERA20K_RESTAMP_MAPS=Deadman.mmx`:1passed0failed8721filtered,0.90s run/3m48 compile, exit0 (`restamp-deadman-export.log`). Existing58 lib-test warnings. No full suite or Clippy run for this research-only diagnostic.

Fresh independent critic reviewed the selector and original composition and completed the relevant omitted-middle preservation proof. The20-case native corpus was independently reproduced with exit0. The revised48 setter prefixes generated and independently checked successfully with exit0. Candidate focused command `cargo test -p vera20k --lib gap -- --nocapture` passed35/failed0/ignored2/filtered8688 in0.01s after4m10s compilation, exit0 (`.local/restamp-focused-v1.log`). It includes all three new bridge-state tests and the committed-placement test. The exact ignored retail command above, with `VERA20K_RESTAMP_MAPS=Deadman.mmx` and a distinct `VERA20K_RESTAMP_OUTPUT=.local/restamp-deadman-postfix.json`, passed1/failed0/filtered8724 in0.88s, exit0 (`.local/restamp-deadman-postfix.log`); all80 post-state comparisons and ore rejection passed. Both runs emitted58 existing lib-test warnings. On unchanged source checkpoint `8b147e5e`, `cargo test -p vera20k --lib` passed8605/failed0/ignored120 in22.43s, actual exit0 (`.local/restamp-full-v1.log`). `cargo clippy -p vera20k --lib` completed in44.20s, actual exit0 with1144 warnings (`.local/restamp-clippy.log`); the existing warning backlog was not rebaselined. Each command followed an idle Cargo/rustc check. Neither row46 nor Phase3 is complete.
