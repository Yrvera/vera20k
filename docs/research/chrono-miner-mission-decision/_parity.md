# Chrono Miner Mission-Decision — Parity Report

Generated by: rust-comparer (task #23 — compare-fn-mission-harvest)
Decode doc: `fn-mission-harvest.md` (PROOFED, confidence 100)
Rust source: `src/sim/miner/miner_system.rs`, `src/sim/miner/mod.rs`

---

## Summary table

| # | Symbol | Verdict | Visibility |
|---|--------|---------|------------|
| 1 | Mission_Harvest tick-rate | DRIFT | Every match tick |
| 2 | State 0 SCAN — ore scan radius (chrono) | DRIFT | Every CMIN ore search |
| 3 | State 2 RETURN — distance reference point (refinery center vs NW) | DRIFT | Every return trip |
| 4 | State 2 RETURN — War Miner close-radio path missing | DRIFT | Every HARV return within 5 cells |
| 5 | State 2 RETURN — zone-bypass trick missing | DRIFT | Zone-separated miner edge case |
| 6 | State 1 HARVEST — multi-bale extraction per tick | DRIFT | Every harvest tick |
| 7 | Chrono warp effect — arrival shimmer spawned (should not be) | DRIFT | Every CMIN inbound warp |
| 8 | State machine structure (5→8 states) | INTERNAL-ONLY | — |
| 9 | HARVEST wait counter gate | INTERNAL-ONLY | — |
| 10 | Slave miner early-exit | INTERNAL-ONLY | — |

Generated by: rust-comparer (task #24 — compare-fn-set-destination)
Decode doc: `fn-set-destination.md` (PROOFED, confidence 100)
Rust source: `src/sim/world/world_commands.rs`, `src/sim/movement/teleport_movement.rs`

---

## Summary table (task #24)

| # | Symbol | Verdict | Visibility |
|---|--------|---------|------------|
| 11 | Set_Destination — non-harvester Teleporter to building cell | DRIFT | Any non-harvester Teleporter=yes unit commanded to a building cell |
| 12 | Set_Destination — NavCom guard missing | DRIFT | Teleporter unit receiving move while already moving |
| 13 | Set_Destination — refinery unload anim clear | DRIFT | Every time a new move order is issued while in Harvest mission |
| 14 | Set_Destination — harvester teleport-vs-drive decision | INTERNAL-ONLY | — |

---

---

## Parity rows

### Row 1 — Mission_Harvest tick-rate

| Field | gamemd | Rust |
|-------|--------|------|
| **Verdict** | DRIFT | — |
| **Input** | Any miner in any state | — |
| **gamemd output** | `Mission_Harvest` returns `MissionTimerEntry + Random(0,2)` ticks; the function is not called again for that many ticks. Per the timer table, this is typically O(10) frames between invocations. | — |
| **Rust output** | `process_miner` is called every sim tick (every frame) from `tick_miners`. No frame-skip mechanism. | — |
| **Player-visible consequence** | Rust runs state-machine transitions (scan, harvest timer, return decision) every frame instead of every ~10 frames. A miner in `SearchOre` re-evaluates its target every tick; gamemd re-evaluates every mission-timer period. This changes retarget responsiveness and how many times per second harvest/return decisions fire. | — |
| **Citations** | gamemd: `0x0073E5E0` (`fn-mission-harvest.md` §Summary, §Decompilation line 116) | Rust: `src/sim/miner/miner_system.rs:150` (`process_miner` called per tick in loop) |
| **Required evidence to downgrade** | Algebraic proof that calling the state machine every tick vs every N ticks produces identical observable outcomes for all state transitions. Not plausible given timer-dependent states. |

---

### Row 2 — State 0 SCAN — ore scan radius for chrono miner

| Field | gamemd | Rust |
|-------|--------|------|
| **Verdict** | DRIFT | — |
| **Input** | Chrono miner (Teleporter=yes) in SCAN state, teleport locomotor active | — |
| **gamemd output** | Calls `FootClass__Search_For_Tiberium_And_Move` with radius from `Rules+0x177C` (short-scan INI key, different from `Rules+0x1778`). | — |
| **Rust output** | `handle_search_ore` calls `search_local_ore` with `config.long_scan_radius` = `general.tiberium_long_scan` (sourced from `Rules+0x1778`). | — |
| **Player-visible consequence** | Chrono miner scans a different radius for ore than gamemd. If `Rules+0x177C` ≠ `Rules+0x1778` (two distinct INI offsets confirmed in `fn-mission-harvest.md` §Globals), CMIN may target ore at a different distance than gamemd. | — |
| **Citations** | gamemd: `0x0073E5E0` state 0 teleport branch, `Rules+0x177C` (`fn-mission-harvest.md` §Globals) | Rust: `src/sim/miner/miner_system.rs:378` (`config.long_scan_radius`), `src/sim/miner/mod.rs:227` (`long_scan_radius: general.tiberium_long_scan`) |
| **Required evidence to downgrade** | Confirm `Rules+0x177C` and `Rules+0x1778` parse the same INI key (i.e., they are the same field). If so, INTERNAL-ONLY. If different INI keys: DRIFT stands. |

---

### Row 3 — State 2 RETURN — distance reference point

| Field | gamemd | Rust |
|-------|--------|------|
| **Verdict** | DRIFT | — |
| **Input** | Any miner in RETURN state with a found refinery | — |
| **gamemd output** | Distance computed between miner's `GetCoords` (vtable `+0x48`) and refinery's `GetCoords` (vtable `+0x48`). For buildings, `GetCoords` = `Location + ((w-1)*128, (h-1)*128)` — foundation-center in leptons. For GAREFN (4×3 at NW (10,10)): center = (10×256+384, 10×256+256) = (2944, 2816) leptons. | — |
| **Rust output** | `return_exceeds_too_far_threshold` computes miner position as `rx*256 + sub_x` and refinery position as `refinery.position.rx*256 + refinery.position.sub_x`. `rx` is the NW-corner cell. For GAREFN at NW (10,10): refinery position = (2560+sub_x, 2560+sub_y) ≠ (2944, 2816). | — |
| **Player-visible consequence** | The close/far threshold boundary is misaligned by up to `(w-1)*128` cells east and `(h-1)*128` cells south. For GAREFN (4×3): 1.5 cells east + 1 cell south offset. A miner 4 cells from refinery NW could be 5.5 cells from foundation center — Rust says "close" when gamemd says "far", taking the wrong path. | — |
| **Citations** | gamemd: `GetCoords` at vtable `+0x48`, `BuildingClass::GetCoords` `0x00447AC0`, formula in `fn-mission-harvest.md` §"State 2 RETURN: chrono close/far split" | Rust: `src/sim/miner/miner_system.rs:54–62` (`return_exceeds_too_far_threshold`) |
| **Required evidence to downgrade** | Proof that `refinery.position.rx/ry` stores foundation-center cell rather than NW cell, or that `sub_x/sub_y` encodes the center offset. Based on coordinate conventions in CLAUDE.md and codebase, `rx/ry` is NW corner — DRIFT stands. |

---

### Row 4 — State 2 RETURN — War Miner close-radio path missing

| Field | gamemd | Rust |
|-------|--------|------|
| **Verdict** | DRIFT | — |
| **Input** | War Miner (Teleporter=no) in RETURN state, distance ≤ `HarvesterTooFarDistance` (default 5 cells) from refinery | — |
| **gamemd output** | Sends radio msg `0x02` (accept dock) via vtable `+0x278`; on success (return 1) → substate 3 (DOCK). This is the close-radio path that transitions HARV to DOCK immediately when within 5 cells. | — |
| **Rust output** | `try_begin_close_return_radio` returns `false` immediately for `MinerKind::War` (`src/sim/miner/miner_system.rs:919`). HARV uses adjacency/`is_within_close_enough` contact detection in `handle_return` instead. No radio-distance threshold gate. | — |
| **Player-visible consequence** | A War Miner within 5 cells of its refinery uses adjacency contact (1 cell) or `CloseEnough` rather than the 5-cell radio-accept distance. HARV will continue pathfinding to adjacency range instead of docking when 2–5 cells away. Observable: War Miner takes longer to begin unloading than it should; dock timing differs from gamemd. | — |
| **Citations** | gamemd: `0x0073E5E0` state 2, non-chrono branch, `Rules+0xD78` threshold, `fn-mission-harvest.md` §"State 2 RETURN" | Rust: `src/sim/miner/miner_system.rs:919` (`if snap.miner.kind != MinerKind::Chrono { return false; }`) |

---

### Row 5 — State 2 RETURN — zone passability bypass missing (g_MapEditorMode trick)

| Field | gamemd | Rust |
|-------|--------|------|
| **Verdict** | DRIFT | — |
| **Input** | Miner in a different movement zone from its refinery (zone-separated by cliffs/water) | — |
| **gamemd output** | Far path bumps `g_MapEditorMode +1` before second `FindFirstBuilding`, then `-1` after. This suppresses zone check inside `FindFirstBuilding` so a zone-separated refinery is still found. | — |
| **Rust output** | `find_nearest_refinery` has no equivalent zone bypass. A miner zone-separated from all refineries returns `None` from `find_nearest_refinery` → transitions to `WaitNoOre`. | — |
| **Player-visible consequence** | A miner cliffed off from its refinery (but refinery nominally accessible via long path) will idle instead of navigating. In gamemd it would still find the refinery and navigate. Trigger frequency: low — requires specific map topology. | — |
| **Citations** | gamemd: `0x0073E5E0` state 2 far path, `g_MapEditorMode` bump described in `fn-mission-harvest.md` §"g_MapEditorMode trick in state 2 far path" | Rust: `src/sim/miner/miner_system.rs:1121–1169` (`find_nearest_refinery`, no zone flag) |

---

### Row 6 — State 1 HARVEST — multi-bale extraction per tick

| Field | gamemd | Rust |
|-------|--------|------|
| **Verdict** | DRIFT | — |
| **Input** | Miner in HARVEST state, timer expired, ore cell with density ≥ 2 levels AND miner has ≥ 2 empty bale slots | — |
| **gamemd output** | `UnitClass__Harvest_Ore_Tick` (`0x0073D450`) extracts one bale per call. After one bale: resets step timer, returns; next bale on next mission-tick. | — |
| **Rust output** | `extract_bales_max` extracts `min(empty_capacity, density_levels)` bales in one call — potentially many bales at once. | — |
| **Player-visible consequence** | Rust drains an ore cell much faster than gamemd when the miner has spare capacity. A high-density cell (12 levels) with an empty Chrono Miner (20 slots) drains in one Rust tick vs 12 gamemd mission-ticks. This changes ore depletion speed, harvest timing pips, and economy balance. | — |
| **Citations** | gamemd: `UnitClass__Harvest_Ore_Tick 0x0073D450` (out-of-scope, referenced in `fn-mission-harvest.md` §"State 1 HARVEST") | Rust: `src/sim/miner/miner_system.rs:535` (`extract_bales_max`), `src/sim/miner/miner_system.rs:813–865` (implementation drains `min(empty, density)` per call) |

---

### Row 7 — Chrono warp effect spawned at arrival cell (should not be)

| Field | gamemd | Rust |
|-------|--------|------|
| **Verdict** | DRIFT | — |
| **Input** | Chrono Miner initiating an inbound warp (far-return teleport) | — |
| **gamemd output** | `WarpOut` SHP animation plays **only at the depart cell** (ore field). No arrival-cell shimmer. (Per project memory `feedback_chrono_miner_no_arrival_shimmer`: confirmed by user observation.) | — |
| **Rust output** | `spawn_warp_effects` iterates over `[depart, arrive]` and spawns a `WorldEffect` at both cells (`src/sim/miner/miner_system.rs:1077`). | — |
| **Player-visible consequence** | A WarpOut shimmer appears at the refinery pad when the miner arrives, which is not present in gamemd. Visible every time CMIN completes an inbound warp. | — |
| **Citations** | gamemd: confirmed user observation (project memory `feedback_chrono_miner_no_arrival_shimmer`) | Rust: `src/sim/miner/miner_system.rs:1077` (`for (rx, ry, z) in [depart, arrive]`) |

---

### Row 8 — State machine structure (INTERNAL-ONLY)

| Field | gamemd | Rust |
|-------|--------|------|
| **Verdict** | INTERNAL-ONLY | — |
| **Evidence** | gamemd uses a 5-substate machine (0–4). Rust uses 8 named states (SearchOre, MoveToOre, Harvest, ReturnToRefinery, Dock, Unload, WaitNoOre, ForcedReturn). The Rust split of gamemd's SCAN(0) into SearchOre+MoveToOre is purely internal: both execute the same scan and produce the same next-cell target. The Rust WaitNoOre and ForcedReturn states have no explicit gamemd counterpart but produce the same observable behavior (miner stays still; eventually rescans or returns). | — |

---

### Row 9 — HARVEST wait counter gate (INTERNAL-ONLY)

| Field | gamemd | Rust |
|-------|--------|------|
| **Verdict** | INTERNAL-ONLY | — |
| **Evidence** | gamemd: `UnitClass+0xF8` counter must reach ≥9 steps (each step = `HarvesterLoadRate` frames). Rust: `harvest_timer` initialized to `HarvesterLoadRate * 9` frames and counts to 0. Algebraically identical: both produce a `HarvesterLoadRate * 9`-frame wait before first bale extraction. | — |

---

### Row 10 — Slave miner early-exit (INTERNAL-ONLY)

| Field | gamemd | Rust |
|-------|--------|------|
| **Verdict** | INTERNAL-ONLY | — |
| **Evidence** | gamemd early-returns to `SlaveManagerClass__HandleReturnedSlaves` for slave miners. Rust filters `MinerKind::Slave` in `tick_miners` Phase 1 before the state machine runs. Both produce the same observable output: slave miners do not run the harvest loop. | — |
