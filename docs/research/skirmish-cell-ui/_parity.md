# Skirmish Cell UI — Parity Report

| Symbol | Verdict | Input | gamemd output | Rust output | Visibility | Citations |
|---|---|---|---|---|---|---|
| FUN_006ACEE0 / AI scan | INTERNAL-ONLY | Start pressed, N AI rows active | Scans 7 CB_GETCURSEL+CB_GETITEMDATA calls, writes count to DAT_00A8B274 | `launch_session` counts `opponents.iter().filter(is_active())` | Not visible to player | gamemd: `decompile_function 0x006ACEE0` §AI-Type Combo Scan; Rust: `src/ui/skirmish_shell/state/launch.rs:87-99` |
| FUN_006ACEE0 / Gate 1 map capacity | INTERNAL-ONLY | Start pressed, map start slots < player count | StringTable error 0x437/0x438 shown | `LaunchValidationError::MapCapacityExceeded` | Same error shown | gamemd: §Start Button Validation Gate 1; Rust: `launch.rs:93-98` |
| FUN_006ACEE0 / Gate 2 min 2 players | INTERNAL-ONLY | Start pressed, no active AI | Error 0x43F/0x440 (total < 2) | `LaunchValidationError::NoEnabledOpponent` | Same error shown | gamemd: §Gate 2; Rust: `launch.rs:100-102` |
| FUN_006ACEE0 / Gate 3 team conflict auto-repair | DRIFT | Start pressed, team conflict exists | Loops rows, calls FUN_004E5940(-1) to auto-fix each conflict, retries FUN_004E6030 twice; only shows error 0x457/0x458 if repair fails | Checks only `player_team >= 0 AND all-AI-on-same-team`; returns `SameExplicitTeam` error without attempting repair | Player-visible: gamemd auto-repairs certain conflict layouts silently; Rust rejects them with an error dialog instead | gamemd: §Gate 3 `decompile_function 0x006ACEE0`; Rust: `launch.rs:103-115` |
| FUN_006ACEE0 / Gate 4 AlliesAllowed | MISSING | Start pressed, mode without AlliesAllowed | Calls `SelectedMode vtable+0x14` (AlliesAllowed check), shows error 0x469 if fails | No AlliesAllowed vtable call present | Player-visible if AlliesAllowed=false mode selected: error never shown in Rust | gamemd: §Gate 4 `DAT_00A8B23C` + vtable+0x14; Rust: `launch.rs:117-120` (mode lookup only) |
| FUN_006ACEE0 / Color combo ownership | DRIFT | Player selects a color already taken by another slot | gamemd FUN_004E4C20 maintains 9-row global ownership table; color already owned by another slot is skipped / shown as unavailable | `combos.rs:282-284` offers 8 color indices with no ownership check; `apply_action` cycles `(color+1)%8` ignoring other slots | Player-visible: in gamemd two players cannot have the same color; in Rust both can pick the same color index | gamemd: §Control-ID → Dispatch Table 0x522/0x6A2 → FUN_004E4C20; Rust: `src/ui/skirmish_shell/state/combos.rs:282-284`, `src/ui/skirmish_shell/state/hit_test.rs:289-298` |
| FUN_006ACEE0 / Session serialization on Start | INTERNAL-ONLY | Successful Start | Writes to globals DAT_00A8B274/27C/29C/2BC/2DC/2FC etc., calls ProcessRandomAssignments, sets DAT_00A8B260=1 | Returns `SkirmishLaunchSession` struct | Not visible to player; same data available to game launch | gamemd: §Session Data Serialization; Rust: `launch.rs:78-162` |
| FUN_006ACEE0 / Map-selector change (0x5AA) | MISSING | Map-selector combo changes selection | Reloads map data, adjusts row visibility, re-validates, calls FUN_006ADDF0 to cascade row-count change | Map selection handled via `SkirmishShellAction::SelectMap` / `apply_action` selecting `state.selected_map_idx`; no row-count cascade from map change | Player-visible: in gamemd changing the map immediately hides/reveals AI rows to match new start-slot count; Rust does not cascade row enable/disable on map change | gamemd: §Map-Selector Change; Rust: `src/ui/skirmish_shell/state/hit_test.rs:281-300` |
| FUN_006ADC20 / Row enable/disable on AI-type change | INTERNAL-ONLY | AI-type combo changes to None / active AI | gamemd: CB_GETCURSEL+CB_GETITEMDATA; item-data 0/1/2 → EnableWindow(4 sibling controls, TRUE); else EnableWindow(sibling controls, FALSE) | Rust: `combo_enabled` returns `opponent.is_active()` for Side/Color/Start/Team rows; rendering skips disabled combos | Same visible result: inactive rows grayed/hidden | gamemd: `decompile_function 0x006ADC20` §Item-Data Test; Rust: `src/ui/skirmish_shell/state/combos.rs:369-384` |
| FUN_006ADC20 / Country/color/start sentinels on deactivation | INTERNAL-ONLY | Row deactivated (AI-type → None) | FUN_004E49A0(-2), FUN_004E3F70(-2), FUN_004E5480(-2) write –2 into per-row combo item-data | Rust clears no sentinel — combos for inactive rows simply aren't populated/shown | No player-visible difference: both show nothing in those combos when row is inactive | gamemd: §Sentinel writers called on inactive row; Rust: `combos.rs:456-466` (row_type set, no sentinel write) |
| FUN_006ADC20 / Team sentinel AlliesAllowed gate | INTERNAL-ONLY | Row deactivated; AlliesAllowed true/false | FUN_004E5ED0 writes 3 (AlliesAllowed=true) or -2 (false) to team combo; reads `*(DAT_00A8B23C+0x3C)` | `inactive_ai_team_default` returns 3 or -2 based on `state.selected_mode_allies_allowed`; applied at `combos.rs:457` on AI-type change | No player-visible difference: same sentinel value set | gamemd: §Team-Sentinel Propagation Loop; Rust: `src/ui/skirmish_shell/state/player_name.rs:333-339`, `combos.rs:456-466` |
| FUN_006ADC20 / Team-sentinel propagation across all 7 rows on map change | MISSING | Map selection changes → reveal/hide row callers trigger FUN_006ADC20 | Propagates team-sentinel (3 or -2) across all 7 rows in a loop when called from row reveal/hide | No equivalent full-row propagation on map selection change | Player-visible if AlliesAllowed changes across maps: team values of revealed rows not updated to match mode | gamemd: §Team-Sentinel Propagation Loop (iVar2=1, iVar5=8 path); Rust: no equivalent in `apply_action` / map-change path |
| FUN_006AE2C0 / Skirmish launcher (dialog create + pump + teardown) | INTERNAL-ONLY | Player selects Skirmish from main menu | Creates Win32 dialog 0x102 via CreateDialogIndirectParamA, runs message pump, tears down; returns true on Start, false on Back | winit event-loop with `skirmish_shell_state`; `handle_skirmish_shell_action` for Start/Back; no modal Win32 dialog | Same observable UI: dialog visible, Start proceeds to game, Back returns to menu | gamemd: `decompile_function 0x006AE2C0` §Behavioral Analysis; Rust: `src/app.rs` `handle_skirmish_shell_action`, `start_selected_skirmish`, `close_native_skirmish_shell` |
| FUN_006AE2C0 / House-type setup before dialog | MISSING | Launcher called | Calls FUN_006722F0+FUN_00672440+per-house vtable+0x64 to populate house-type data before combos are created | No equivalent house-type setup call at dialog-launch time found in Rust skirmish shell | Player-visible if house/side data depends on per-launch setup: combo country list may be static vs. dynamic | gamemd: §1 House-type data setup `decompile_function 0x006AE2C0`; Rust: `src/app.rs` `start_selected_skirmish` (no pre-dialog house setup call) |
| FUN_006AE3F0 / DlgProc message routing | INTERNAL-ONLY | Any UI event in skirmish dialog | Win32 DlgProc callback routes WM_COMMAND/WM_PAINT/WM_NOTIFY/0x497 to sub-handlers | winit event handlers `handle_skirmish_shell_mouse_down`, `handle_skirmish_shell_key_input`, etc. | Same net routing — user interactions dispatched to same logical handlers | gamemd: `decompile_function 0x006AE3F0` §Message routing table; Rust: `src/app.rs` event handler methods |
| FUN_006AE3F0 / WM_PAINT start-position markers on mini-map | MISSING | Dialog painted; `DAT_00AC1154 != 0` (map loaded) | Calls DrawStartPositions(0x00640710) to paint numbered start-position indicators on control 0x468 (mini-map) | No equivalent start-position marker draw on map preview found in skirmish shell | Player-visible: in gamemd the mini-map shows numbered start position dots overlaid; not rendered in Rust shell | gamemd: §WM_PAINT path; Rust: `src/app_skirmish_shell_render/preview.rs` (no start-position marker) |
| FUN_006AE3F0 / WM_NOTIFY AI-type tooltip strings | INTERNAL-ONLY | Hover over AI-type combo item | Reads CB_GETITEMDATA, maps to string table indices 0x87(None), 0x89(Easy), 0x8B(Normal), 0x8D(Hard) | `status_help_key_for_ai_row_type` maps to `STT:PlayerNone`, `STT:PlayerDumbAI`, `STT:PlayerSmartAI`, `STT:PlayerGeniusAI` | Same string displayed (both read from the same CSF string table, different lookup keys) | gamemd: §WM_NOTIFY AI-type combos; Rust: `src/ui/skirmish_shell/state/hit_test.rs:206-213` |
| FUN_006AE3F0 / WM_NOTIFY tooltip dispatch mechanism | INTERNAL-ONLY | Hover over any cell | gamemd: Windows tooltip control via `FUN_007b6880`; WM_NOTIFY/WM_MOUSEMOVE path through `FUN_00622B50` | Rust: `set_status_help_text` → status bar text at bottom of shell | Same observable: tooltip text shown on hover | gamemd: §WM_NOTIFY tooltip dispatch; Rust: `src/ui/skirmish_shell/state/hit_test.rs:99-144` |
| FUN_006ADDF0 / Row show/hide on map change — delta-drive | MISSING | Map-selector combo changes to map with different start count | Computes delta (new_count - old_count), calls FUN_006ADF00 (reveal) or FUN_006AE080 (hide) per-row, propagates AlliesAllowed team-sentinels across all 7 rows | `apply_action SelectMap(idx)` only updates `state.selected_map_idx`; no row reveal/hide, no team-sentinel propagation | Player-visible: rows do not appear/disappear when map is changed; rows for newly-unavailable start slots stay visible | gamemd: `decompile_function 0x006ADDF0`; Rust: `src/ui/skirmish_shell/state/hit_test.rs:281-300` |
| FUN_006ADF00 / Reveal AI rows (ShowWindow + enable-state) | MISSING | Map supports more start positions → show more rows | ShowWindow(SW_SHOW=5) on each of 6 row controls, then calls FUN_006ADC20 for enable-state | No per-row ShowWindow call; all rows always visible in Rust shell | Player-visible: in Rust all 7 AI rows are always shown regardless of map capacity | gamemd: `decompile_function 0x006ADF00`; Rust: no equivalent show/hide of row controls |
| FUN_006AE080 / Hide AI rows (sentinel + ShowWindow) | MISSING | Map supports fewer start positions → hide excess rows | Selects sentinel item-data=-1 in AI-type combo, calls FUN_006ADC20, calls ShowWindow(SW_HIDE=0) on 6 controls per row | No per-row hide; all 7 AI rows always rendered | Player-visible: same as FUN_006ADF00 — row count never adjusts to map capacity in Rust | gamemd: `decompile_function 0x006AE080`; Rust: no equivalent |
| FUN_006ACD60 / Team combo enable based on AlliesAllowed | DRIFT | Mode selected with AlliesAllowed=false | Disables all 8 team combos via EnableWindow(FALSE) | Rust: `combo_enabled` for Team rows returns `opponent.is_active()` regardless of `selected_mode_allies_allowed`; team combo stays enabled even when AlliesAllowed=false | Player-visible: in gamemd team combos are grayed/disabled when AlliesAllowed=false; in Rust they remain enabled | gamemd: `decompile_function 0x006ACD60`; Rust: `src/ui/skirmish_shell/state/combos.rs:369-384` (`combo_enabled` for Team rows) |
| FUN_004E3320 / Flag-static control-ID lookup | INTERNAL-ONLY | Row show/hide called | Returns control IDs 0x6DA..0x6E1 for flag/picture static per row; used by FUN_006ADF00/FUN_006AE080 | No equivalent Win32 control ID lookup; Rust renders flag images directly via `push_flag_entry_native_clipped_centered` | Same observable: flag image shown per row | gamemd: `decompile_function 0x004E3320`; Rust: `src/app_skirmish_shell_render.rs` flag rendering |
| FUN_004E3560 / Side item-data to flag PCX mapping | MATCH | Country combo changes selection | Maps item-data -3..9 to PCX filenames (obsi, rani, usai, japi, frai, geri, gbri, djbi, arbi, lati, rusi, yrii) | `flag_pcx_for_side_item_data` in `app_skirmish_shell_render.rs:127-141` maps same item-data values to same PCX filenames | Same flag images shown for same country | gamemd: `decompile_function 0x004E3560` (string table `0x00844AE8`); Rust: `src/app_skirmish_shell_render.rs:127-141` |
| FUN_004E3690 / Country combo CBN_SELCHANGE handler | INTERNAL-ONLY | Country combo selection changes | Reads CB_GETITEMDATA, calls flag-PCX update + owner-draw invalidation; calls vtable+0x28 on SelectedMode if item-data out of range | `apply_combo_selection` for `Side` combo; calls `opponent.country = country` or `country_random = true`; flag re-rendered next frame | Same observable: flag updates on country change | gamemd: `decompile_function 0x004E3690`; Rust: `src/ui/skirmish_shell/state/combos.rs:450-515` |
| FUN_004E37D0 / Country combo control-ID lookup | INTERNAL-ONLY | Any caller needs country combo ID | Returns control IDs 0x6A1 (slot 0) + 0x510/0x513/0x51E/0x514/0x51F/0x520/0x521 (slots 1-7) | No equivalent ID table; Rust uses `SkirmishComboId::Side(row)` directly | No player-visible difference | gamemd: `decompile_function 0x004E37D0`; Rust: `src/ui/skirmish_shell/state/combos.rs:18-26` |
| FUN_004E3830 / Country combo control-ID reverse lookup | INTERNAL-ONLY | WM_COMMAND fires for country control ID; need row index | Maps control ID 0x6A1/0x510/0x513/0x51E/0x514/0x51F/0x520/0x521 back to row index 0-7 | No equivalent ID→row reverse map; Rust dispatches by `SkirmishComboId::Side(row)` carrying row index directly | No player-visible difference | gamemd: `decompile_function 0x004E3830`; Rust: `src/ui/skirmish_shell/state/combos.rs:18-26` |
| FUN_004E3B90 / Country combo loader (all 8 rows) — normal path | INTERNAL-ONLY | Dialog init: populate country combos | Iterates 8 rows; normal path calls FUN_004E3A00 to populate combo with Random(-2) + countries 0-9 | `combo_items` for `Side` returns `[Random, ...SkirmishCountry::ALL]` (10 items + 1 sentinel) | Same visible combo contents for normal skirmish | gamemd: `decompile_function 0x004E3B90`; Rust: `src/ui/skirmish_shell/state/combos.rs:274-281` |
| FUN_004E3B90 / Country combo loader — spectator/observer path | MISSING | Dialog init when session is spectator mode (`iVar2 == DAT_00AC11B4`) | Sets country combo to single "Observer" entry (string 0x135, item-data -3) per row | `combo_items` for `Side` always returns full country list; no spectator-only Observer-only path | Player-visible if spectator mode is supported: combos should show only Observer for spectator slots | gamemd: `decompile_function 0x004E3B90` §Spectator branch; Rust: `src/ui/skirmish_shell/state/combos.rs:274-281` |
| FUN_004E3CE0 / Country selection restore from session globals | MISSING | Dialog init; restores prior-session country per row | Reads DAT_00A8B3BC + DAT_00A8B3C0 (-2=random, -3=observer) to determine target; scans combo items for match; selects via CB_SETCURSEL | Rust initializes country to defaults (first country / random flag); no read from session globals DAT_00A8B3BC/B3C0 | Player-visible: in gamemd the country selection from a prior session is restored; Rust always starts at default | gamemd: `decompile_function 0x004E3CE0`; Rust: `src/ui/skirmish_shell/state/player_name.rs` (SkirmishShellState default fields) |
| FUN_004E3F70 / Country combo selection setter (explicit value) | INTERNAL-ONLY | Row-enable state machine or init needs to set country combo to specific value | Hides combo, scans items for matching item-data, selects via CB_SETCURSEL, re-shows; calls flag-PCX update | `selected_combo_item` + `combo_items` resolve current country; no explicit scan-and-set needed — state drives rendering | Same observable: combo shows correct selection | gamemd: `decompile_function 0x004E3F70`; Rust: `src/ui/skirmish_shell/state/combos.rs:317-367` |
| FUN_004E4170 / Country item-data reader with range guard | INTERNAL-ONLY | Need current country item-data from combo | CB_GETCURSEL + CB_GETITEMDATA; out-of-range returns vtable+0x28 result or -2 | Rust reads `state.player_country` / `opponent.country` directly; no Win32 message round-trip | No player-visible difference | gamemd: `decompile_function 0x004E4170`; Rust: `src/ui/skirmish_shell/state/combos.rs:317-355` |
| FUN_004E41D0 / Color combo control-ID lookup | INTERNAL-ONLY | Any caller needs color combo control ID | Returns IDs 0x6A2 (row 0) + 0x522..0x528 (rows 1-7) | No equivalent; Rust uses `SkirmishComboId::Color(row)` directly | No player-visible difference | gamemd: `decompile_function 0x004E41D0`; Rust: `src/ui/skirmish_shell/state/combos.rs:22-23` |
| FUN_004E43C0 / Color table label and swatch initializer | INTERNAL-ONLY | Dialog init: load color label strings + swatch RGB | Loads 9 strings (IDs 0x1DB-0x1E3) into global array `0x8B4034`, copies 9 swatch RGB DWORDs from `0x8316A8`; stride-12 layout | Rust reads color names from CSF string table and swatch colors from rules.ini/palette at startup | Same observable: named colored swatches in color combo | gamemd: `decompile_function 0x004E43C0`; Rust: `src/app_skirmish_shell_render.rs` color swatch rendering |
| FUN_004E45A0 / Color combo population helper (per-row, ownership filter) | INTERNAL-ONLY | Dialog init or color change: populate one color combo | Filters color table entries: only adds colors whose slot_owner == this_row OR slot_owner == -1 (unclaimed); this enforces color exclusivity | `combo_items` for `Color` offers `[ColorSentinel(-2), Color(0)..Color(7)]` with no ownership filter; ownership DRIFT already logged at FUN_006ACEE0 | Ownership DRIFT already captured; per-item filtering INTERNAL-ONLY vs DRIFT | gamemd: `decompile_function 0x004E45A0`; Rust: `src/ui/skirmish_shell/state/combos.rs:282-284` (see also FUN_006ACEE0 / Color combo ownership row) |
| FUN_004E4770 / Color combo sentinel loader (closed/spectator slot) | MISSING | Slot is closed or spectator (game mode 3/4, slot owner = local player) | Resets color combo to single "Random Color" entry (string 0x237, item-data -2, swatch -1); row shows locked sentinel | Rust: `combo_items` for `Color` always returns full 9-item list; no sentinel-only mode for closed/spectator slots | Player-visible if spectator/observer mode is supported: closed slots should show locked color sentinel | gamemd: `decompile_function 0x004E4770`; Rust: `src/ui/skirmish_shell/state/combos.rs:282-284` |
| FUN_004E4820 / Color combo init loop (spectator branch selector) | INTERNAL-ONLY | Dialog init: populate all 8 color combos | Loops 8 rows; calls FUN_004E4770 (sentinel) for spectator+local+closed slots; calls FUN_004E45A0 (normal) otherwise | `combo_items` for `Color` is called for all 8 rows with same full list; no per-slot branch | Internal-only for normal skirmish; spectator MISSING logged at FUN_004E4770 row | gamemd: `decompile_function 0x004E4820`; Rust: `src/ui/skirmish_shell/state/combos.rs:282-284` |
| FUN_004E48E0 / Color combo selection restore (session-saved color) | MISSING | Dialog init: restore prior-session color per row | Reads DAT_00A8B3B4 (saved color index); overrides with -2 if row was closed, 8 if spectator+local+closed; passes to FUN_004E49A0 | Rust initializes color to defaults; no read from session globals DAT_00A8B3B4/B3B8 | Player-visible: in gamemd the color selection from a prior session is restored; Rust always starts at default | gamemd: `decompile_function 0x004E48E0`; Rust: `src/ui/skirmish_shell/state/player_name.rs` (SkirmishShellState default fields) |
| FUN_004E49A0 / Color table ownership update + combo refresh | INTERNAL-ONLY | Color selection changes (init restore or row deactivation) | Phase 1: clears prior color ownership in DAT_008B4040; Phase 2: finds target item by CB_GETITEMDATA scan, sets CB_SETCURSEL, writes new ownership (unless -2); Phase 3: refreshes all 8 color combos | `apply_combo_selection` for `Color` sets `player_color_index` / `opponent.color_index` directly; no ownership table; combos re-rendered from state | Ownership DRIFT already captured at FUN_006ACEE0 row; per-combo refresh mechanism INTERNAL-ONLY | gamemd: `decompile_function 0x004E49A0`; Rust: `src/ui/skirmish_shell/state/combos.rs:489-497` |
| FUN_004E4C20 / Color combo CBN_SELCHANGE handler (live selection) | INTERNAL-ONLY | User changes color combo selection | Phase 1: clears prior slot ownership from color table; Phase 2: reads CB_GETCURSEL+CB_GETITEMDATA of new selection, writes new ownership; Phase 3: refreshes all 8 color combos | `apply_combo_selection` for `Color` sets `color_index` directly; state-driven re-render covers all rows next frame | Ownership DRIFT already captured; per-combo refresh via Win32 vs state-driven render INTERNAL-ONLY | gamemd: `decompile_function 0x004E4C20`; Rust: `src/ui/skirmish_shell/state/combos.rs:489-497` |
| FUN_004E4E20 / Color item-data reader (no range guard) | INTERNAL-ONLY | WM_COMMAND or DlgProc needs current color combo item-data | CB_GETCURSEL then CB_GETITEMDATA; result discarded (void) — likely a Ghidra artifact; return value consumed by caller via register | No equivalent Win32 call; Rust reads `state.player_color_index` / `opponent.color_index` directly | No player-visible difference | gamemd: `decompile_function 0x004E4E20`; Rust: `src/ui/skirmish_shell/state/combos.rs:339-356` |
| FUN_004E4E60 / Start-position combo control-ID lookup | INTERNAL-ONLY | Any caller needs start-position combo ID | Returns IDs 0x6A3 (row 0) + 0x6A4..0x6A8, 0x6AA, 0x6AB (rows 1-7); note gap: 0x6A9 skipped | No equivalent; Rust uses `SkirmishComboId::Start(row)` directly | No player-visible difference | gamemd: `decompile_function 0x004E4E60`; Rust: `src/ui/skirmish_shell/state/combos.rs:22-24` |
| FUN_004E4F50 / Start-position label table initializer | INTERNAL-ONLY | Dialog init: build start-pos label pointer table | Writes 9 ASCII label string pointers ("0"-"8") + clears 9 ownership fields into global table at 0x8B3F30 | `combo_items(Start)` generates `StartPosition::Position(i)` items dynamically from map waypoint count | Same visible result: start-position items 0-8 available | gamemd: `decompile_function 0x004E4F50`; Rust: `src/ui/skirmish_shell/state/combos.rs:285-300` |
| FUN_004E4FC0 / Start-position availability mask setter | INTERNAL-ONLY | Map loaded; set which start positions are available | Writes 0xFFFFFFFF (unavailable) to entries < param_1 and 0xFFFFFFFE (available) to entries >= param_1 in start-pos table | `combo_items(Start)` reads `map.multiplayer_start_waypoints.len()` and generates only available positions | Same result: positions beyond map capacity not shown | gamemd: `decompile_function 0x004E4FC0`; Rust: `src/ui/skirmish_shell/state/combos.rs:285-300` |
| FUN_004E5310 / Start-position combo population loop (all 8 rows) | INTERNAL-ONLY | Dialog init or map change: populate all 8 start-pos combos | Iterates 8 rows; spectator+local+closed → sentinel (FUN_004E5260); else → normal population (FUN_004E50C0) | `combo_items(Start)` called per-row dynamically; no spectator sentinel path | Spectator MISSING covered by color sentinel row; normal path INTERNAL-ONLY | gamemd: `decompile_function 0x004E5310`; Rust: `src/ui/skirmish_shell/state/combos.rs:285-300` |
| FUN_004E53D0 / Start-position sentinel dispatcher (spectator slot) | MISSING | Dialog init; spectator mode, slot is local player AND closed | Routes to position 8 (locked) for spectator+local+closed slot; otherwise passes 0xFFFFFFFE (available) | No spectator branch in start-position logic; Rust treats all slots uniformly | Player-visible if spectator/observer mode is supported: spectator slot start position not locked | gamemd: `decompile_function 0x004E53D0`; Rust: `src/ui/skirmish_shell/state/combos.rs:285-300` |
| FUN_004E5480 / Start-position selection restore + assignment table update | INTERNAL-ONLY | Init restore or row deactivation: apply target start position | Phase 1: clears old slot claim from DAT_008B3F38; Phase 2: scans items for matching item-data, CB_SETCURSEL, writes new claim; Phase 3: refreshes all 8 combos | `selected_start_position` reads from state; `start_position_taken_by_other_row` enforces exclusivity in `combo_items(Start)` | Same result: start positions are exclusive across slots | gamemd: `decompile_function 0x004E5480`; Rust: `src/ui/skirmish_shell/state/combos.rs:285-300`, `396-410` |
| FUN_004E5700 / Start-position CBN_SELCHANGE handler | INTERNAL-ONLY | User changes start-position combo selection | Phase 1: clears old claim; Phase 2: CB_GETCURSEL+CB_GETITEMDATA, writes new claim if not -2; Phase 3: refreshes all 8 combos | `apply_combo_selection(Start)` updates `start_position`; `combo_items(Start)` re-filters on next render | Same observable: selected start pos exclusive; other slots update immediately | gamemd: `decompile_function 0x004E5700`; Rust: `src/ui/skirmish_shell/state/combos.rs:498-503` |
| FUN_004E5900 / Generic combo item-data reader | INTERNAL-ONLY | WM_COMMAND or DlgProc needs item-data from any combo | CB_GETCURSEL (if param_3=-1) then CB_GETITEMDATA; result in EAX | No equivalent; Rust reads state fields directly for all combo types | No player-visible difference | gamemd: `decompile_function 0x004E5900`; Rust: `src/ui/skirmish_shell/state/combos.rs:317-365` |
| FUN_004E5940 / Team combo control-ID lookup | INTERNAL-ONLY | Any caller needs team combo ID | Returns IDs 0x76D (row 0) + 0x76E..0x774 (rows 1-7); contiguous range | No equivalent; Rust uses `SkirmishComboId::Team(row)` directly | No player-visible difference | gamemd: `decompile_function 0x004E5940`; Rust: `src/ui/skirmish_shell/state/combos.rs:24-25` |
| FUN_004E5AC0 / Team combo string table initializer | INTERNAL-ONLY | Dialog init: load team name strings | Loads 4 team label strings (IDs 0x437-0x43A) into global table at 0x8B3FC0; stride-12 layout + fallback pointer | Rust reads team names from CSF string table at render time; no static global table | Same observable: team combo shows named team options | gamemd: `decompile_function 0x004E5AC0`; Rust: `src/app_skirmish_shell_render.rs` team combo render |
| FUN_004E5D60 / Team combo population loop (all 8 rows) | INTERNAL-ONLY | Dialog init or mode change: populate all 8 team combos | Iterates 8 rows; spectator+local+closed → sentinel (FUN_004E5CB0); else → normal (FUN_004E5B60); in offline skirmish spectator gate is always false | `combo_items(Team)` returns [-2,0,1,2,3] or [0,1,2,3] based on `selected_mode_must_ally`; no spectator path | Spectator MISSING covered by color sentinel row; normal INTERNAL-ONLY | gamemd: `decompile_function 0x004E5D60`; Rust: `src/ui/skirmish_shell/state/combos.rs:302-314` |
| FUN_004E5E20 / Per-slot team sentinel dispatcher (spectator+AND condition) | MISSING | Dialog init; spectator mode, slot is absent AND closed | Passes sentinel value 4 to FUN_004E5ED0 when spectator+absent+closed; otherwise passes -1 | No spectator+AND team sentinel path in Rust | Player-visible if spectator/observer mode supported: team combo sentinel not set for spectator slots | gamemd: `decompile_function 0x004E5E20`; Rust: `src/ui/skirmish_shell/state/combos.rs:302-314` |
| FUN_004E5ED0 / Team combo selection setter + observer hide | INTERNAL-ONLY | Row-enable or init: set team combo to target value | Hides combo, scans for target item-data via CB_GETCOUNT/CB_GETITEMDATA, CB_SETCURSEL, writes ownership to DAT_008B3FC8; observer mode (g_GameMode==4) additionally hides combo + control 0x794 | `apply_combo_selection(Team)` sets `opponent.team` directly; no team assignment table write; no observer-mode hide | Observer-mode hide is MISSING; normal path INTERNAL-ONLY | gamemd: `decompile_function 0x004E5ED0`; Rust: `src/ui/skirmish_shell/state/combos.rs:505-512` |
| FUN_004E6030 / Team combo item-data reader | INTERNAL-ONLY | WM_COMMAND needs current team item-data | CB_GETCURSEL (if -1) then CB_GETITEMDATA; result in EAX | Rust reads `state.player_team` / `opponent.team` directly | No player-visible difference | gamemd: `decompile_function 0x004E6030`; Rust: `src/ui/skirmish_shell/state/combos.rs:350-356` |
| OwnerDraw_ComboBox_00617250 / Custom combo WndProc (owner-draw rendering) | INTERNAL-ONLY | Any combo needs paint, input, or item-data | Custom WndProc: WM_PAINT with AlphaBlend + BitFont + color swatch; WM_LBUTTONDOWN arrow detection; CB_SETCURSEL/CB_GETCURSEL from registration struct; 0x498 swatch store; dropdown creation/destruction | `push_combo_instances` + `combo_dropdown_*` functions in `app_skirmish_shell_render/controls.rs`; fully custom Rust renderer; no Win32 WndProc | Same observable: named combos with colored swatches and truncated text | gamemd: `decompile_function 0x00617250`; Rust: `src/app_skirmish_shell_render/controls.rs:358` |
| SessionClass__ReadSkirmishSettings / INI session restore | MISSING | Dialog init: restore prior skirmish session from RA2MD.INI | Reads `[Skirmish]` section from RA2MD.INI: GameMode, ScenIndex, GameSpeed, Credits, UnitCount, booleans, per-slot triples (type/country/color) for slots 1-7 via FUN_00477440 | No INI-based session restore; Rust skirmish shell initializes to defaults on every open | Player-visible: in gamemd re-opening Skirmish restores prior settings (map, opponents, colors); Rust resets every time | gamemd: `decompile_function 0x00697F10`; Rust: `src/ui/skirmish_shell/state/player_name.rs` (SkirmishShellState default init) |
| FUN_00477440 / INI slot string parser (3-int tokenizer) | INTERNAL-ONLY | Dialog init (via session restore): parse per-slot INI string | Reads INI key string, tokenizes by delimiter, atoi-parses up to 3 integers into out-params; skips if key absent | No INI tokenizer needed; Rust has no INI-based slot restore | Covered by SessionClass__ReadSkirmishSettings MISSING | gamemd: `decompile_function 0x00477440`; Rust: no equivalent |
| FUN_0069ADF0 / Session RandMap_Sed validator | INTERNAL-ONLY | WM_COMMAND / dialog init: check if session is random-map type | Compares session+0x58 to "RandMap_Sed" literal; returns bool | No session type check in Rust skirmish shell; random map is not supported | Not player-visible in standard offline skirmish (no random map) | gamemd: `decompile_function 0x0069ADF0`; Rust: no equivalent session field |
| FUN_0069B540 / Session slot array initializer | INTERNAL-ONLY | Dialog init: clear session slot arrays | Zeroes +0x24, clears 8-element arrays at +0x4C and +0x6C to -1 | `SkirmishShellState` default initializes all fields via Rust struct init; no manual -1 fill of session memory | Same observable: slots start unassigned | gamemd: `decompile_function 0x0069B540`; Rust: `src/ui/skirmish_shell/state/player_name.rs` (SkirmishShellState derive Default) |
| FUN_0069B760 / Session country writer (random + fixed paths) | INTERNAL-ONLY | WM_COMMAND: commit country selection to session object | param_2=-2 → randomize if flag else clear; picks random 0-9 via Random__RandomRanged; writes to +0x184/+0x188 primary + +0x174/+0x178 mirror | `apply_combo_selection(Side)` sets `player_country` / `opponent.country` / `country_random` directly in state | Same observable: country stored for session launch | gamemd: `decompile_function 0x0069B760`; Rust: `src/ui/skirmish_shell/state/combos.rs:467-488` |
| FUN_0069B7E0 / Session color writer (random with collision avoidance) | INTERNAL-ONLY | WM_COMMAND: commit color selection to session object | Random branch: picks candidate 0-7, checks conflict vs slot list (+0x2840) AND used-colors array (+0x84), retries; writes to +0x17C/+0x180 + +0x15C/+0x160 mirror | `apply_combo_selection(Color)` sets `color_index` directly; no random-color collision-avoidance loop | Color DRIFT (no exclusive ownership) already captured at FUN_006ACEE0 row; random collision avoidance INTERNAL-ONLY for already-noted DRIFT | gamemd: `decompile_function 0x0069B7E0`; Rust: `src/ui/skirmish_shell/state/combos.rs:489-497` |
| FUN_006040B0 / Global tooltip text dispatcher | INTERNAL-ONLY | Cursor hovers over any skirmish dialog control | Win32 HWND+GetDlgCtrlID dispatch; returns pointer to STT string for the matched control ID (e.g. 0x6A0→`STT:SkirmishEditPlayer`, 0x50B-0x521→`STT:SkirmishComboAIPlayer`, 0x6DA-0x6E1→`STT:SkirmishPictureFlag`, etc.); NULL for unknown IDs | `status_help_key_for_hover` (hit_test.rs:99-143) dispatches by `SkirmishHoverTarget` enum variant; returns same STT key strings for matched targets | Same STT keys returned for same hover targets; dispatch mechanism differs (Win32 HWND vs Rust enum-based hit-test) | gamemd: `decompile_function 0x006040B0`; Rust: `src/ui/skirmish_shell/state/hit_test.rs:99-143` |
| FUN_006040B0 / Flag-static tooltip (STT:SkirmishPictureFlag) | MISSING | Cursor hovers over country-flag static (control IDs 0x6DA..0x6E1) | Returns `STT:SkirmishPictureFlag` for all 8 flag static controls | `SkirmishHoverTarget` enum has no flag-static variant; `hovered_shell_control` does not test flag image rects; hovering flag images produces no tooltip | Player-visible: in gamemd hovering the country flag image shows a tooltip; in Rust no tooltip appears on the flag image | gamemd: `decompile_function 0x006040B0` control IDs 0x6DA-0x6E1; Rust: `src/ui/skirmish_shell/state/hit_test.rs:40-96` (no flag rect test) |
| SessionClass__ProcessRandomAssignments / Random resolution on Start | DRIFT | Start pressed; any slot still has random country or color | Resolves all random (-2) country/color selections to concrete values using RNG + collision-avoidance; observer slots get country=-3, color=8; slot-0 color mirrored to DAT_00A8B394 | `launch_session` returns `LaunchValidationError::RandomSelectionUnverified{slot}` if `player_country_random` or `opponent.country_random` is true; no resolution pass; no observer branch | Player-visible: in gamemd "Random" country/color is silently resolved when Start is clicked; in Rust clicking Start with any random selection shows a validation error | gamemd: `decompile_function 0x0069B8C0`; Rust: `src/ui/skirmish_shell/state/launch.rs:123,136` (`launch_country_from_menu` returning `Err` on `random=true`) |
| FUN_005E6520 / Selected-map player/start-point count | INTERNAL-ONLY | Map selected; need start-slot count for row show/hide | Opens map INI at runtime, counts `[Waypoints]` keys 0-7 with non-(-1) values; falls back to `[RandomMap] NumPlayers`; hard default = 8 | `MapMenuEntry.multiplayer_start_waypoints.len()` — built at map-list scan time from `multiplayer_start_waypoints(&parse_waypoints(ini))` filtering waypoints 0..=7; `[RandomMap] NumPlayers` fallback absent (handled by `min_players` field on scenario records) | Same waypoint count produced; mechanism differs (runtime INI open vs pre-parsed Vec); no player-visible difference in normal skirmish | gamemd: `decompile_function 0x005E6520`; Rust: `src/app_list_maps.rs:321` + `src/map/waypoints.rs:54-63` |
| STT:SkirmishComboAIPlayer / Tooltip string | INTERNAL-ONLY | Cursor hovers over AI-type combo (slots 1-7) | Static string `"STT:SkirmishComboAIPlayer"` at `0x008353E4`; returned by tooltip dispatcher for control IDs 0x50B/0x50E/0x516/0x51A-0x51D | `status_help_key_for_combo` returns `Some("STT:SkirmishComboAIPlayer")` for `SkirmishComboId::AiType(_)` | Same STT key string used; storage differs (binary data segment vs Rust `&'static str`) | gamemd: `read_memory 0x008353E4`; Rust: `src/ui/skirmish_shell/state/hit_test.rs:198` |
| STT:SkirmishPictureFlag / Tooltip string | MISSING | Cursor hovers over flag static (control IDs 0x6DA..0x6E1) | Static string `"STT:SkirmishPictureFlag"` at `0x00835400`; returned by tooltip dispatcher | `status_help_key_for_hover` has no flag-static branch; no `SkirmishHoverTarget` variant for flag images | Player-visible: flag-image hover tooltip absent in Rust (captured also at FUN_006040B0 / Flag-static tooltip row) | gamemd: `read_memory 0x00835400`; Rust: `src/ui/skirmish_shell/state/hit_test.rs:107` (no flag branch) |
| STT:SkirmishComboCountry / Tooltip string | INTERNAL-ONLY | Cursor hovers over country combo (any row) | Static string `"STT:SkirmishComboCountry"` at `0x00835418`; returned for control IDs 0x6A1/0x510/0x513/0x51E/0x514/0x51F/0x520/0x521 | `status_help_key_for_combo` returns `Some("STT:SkirmishComboCountry")` for `SkirmishComboId::Side(_)` | Same STT key string used | gamemd: `read_memory 0x00835418`; Rust: `src/ui/skirmish_shell/state/hit_test.rs:199` |
| STT:SkirmishComboColor / Tooltip string | INTERNAL-ONLY | Cursor hovers over color combo (any row) | Static string `"STT:SkirmishComboColor"` at `0x00835434`; returned for control IDs 0x6A2/0x522-0x528 | `status_help_key_for_combo` returns `Some("STT:SkirmishComboColor")` for `SkirmishComboId::Color(_)` | Same STT key string used | gamemd: `read_memory 0x00835434`; Rust: `src/ui/skirmish_shell/state/hit_test.rs:200` |
| STT:SkirmishEditPlayer / Tooltip string | INTERNAL-ONLY | Cursor hovers over player-name edit control (slot 0, control ID 0x6A0) | Static string `"STT:SkirmishEditPlayer"` at `0x008355C8`; returned by tooltip dispatcher | `status_help_key_for_hover` returns `Some("STT:SkirmishEditPlayer")` for `SkirmishHoverTarget::PlayerName0x6a0` | Same STT key string used | gamemd: `read_memory 0x008355C8`; Rust: `src/ui/skirmish_shell/state/hit_test.rs:102` |
| FUN_005E2EF0 / Control 0x6EC custom-message sender (guarded) | INTERNAL-ONLY | Dialog init or WM_COMMAND: update unknown control 0x6EC | If `param_2 != 0`: GetDlgItem(0x6EC), calls `FUN_007B7140()` for lParam, sends `WM_USER+0xB2` (0x4B2) to control | No Win32 SendMessage to control 0x6EC; control 0x6EC not present in Rust shell (unknown purpose — likely a custom status-bar or map-label control not ported) | Not player-visible for identified control types; would be MISSING if 0x6EC is a visible text/label control | gamemd: `decompile_function 0x005E2EF0`; Rust: no equivalent `SendMessageA(0x6EC, 0x4B2, ...)` call |
| FUN_005E2F60 / Control 0x5A8 custom-message sender (unconditional) | INTERNAL-ONLY | Dialog init or WM_COMMAND: initialize unknown control 0x5A8 | Unconditionally GetDlgItem(0x5A8), SendMessageA(hWnd, 0x4B2, 0, 0xA8B322); no guard flag; lParam is hardcoded address in Skirmish dialog global block | No Win32 SendMessage to control 0x5A8; control 0x5A8 is not in any known control ID set; Rust initializes its owner-draw combo renderer directly without WM_USER+N messages | Same functional result: combo/control initialized before user interaction; Win32 plumbing vs Rust direct init | gamemd: `decompile_function 0x005E2F60`; Rust: `src/app_skirmish_shell_render/controls.rs` (direct combo renderer init) |
| SessionClass slots-slice / Slot entry struct (DAT_00A8DA90[i], +0x6B closed flag) | INTERNAL-ONLY | Dialog iterates slot entries to test closed/spectator state | 8-pointer array at DAT_00A8DA90; `slot_entry+0x6B == -1` = closed/spectator; used by color/start-pos/team combo population loops | `opponent.is_active()` / `opponent.row_type != None`; no raw pointer array; closed-slot sentinel is enum variant `SkirmishAiRowType::None` | Same observable: inactive/closed slots not populated with combos | gamemd: `decompile_function 0x004E49A0` (slot_entry+0x6B); Rust: `src/ui/skirmish_shell/state/combos.rs` `is_active()` |
| SessionClass slots-slice / Player entry struct (+0x53 startpos, +0x57 random flag, +0x5B color, +0x63 team) | INTERNAL-ONLY | WM_COMMAND Start: package per-slot data into 0x85-byte entries | Allocates 0x85-byte structs per active player; writes startpos at +0x53, random flag at +0x57, color at +0x5B, team at +0x63, stored in SessionClass+0x2840 list | `SkirmishLocalSlot` and `SkirmishAiSlot` structs in `skirmish_launch.rs`; typed Rust fields (`country`, `color_index`, `start_position`, `team`); no raw byte layout | Same data available for session launch; representation differs | gamemd: `decompile_function 0x006ACEE0` (operator_new(0x85)); Rust: `src/skirmish_launch.rs:80-120` |
| SessionClass slots-slice / Persistence array (DAT_00A8B3EC, PlayerType/Country/StartPos per 7 AI slots) | MISSING | WM_COMMAND Start: write AI slot data; dialog re-open: restore AI slot data | FUN_006ACEE0 writes 7×3-dword array at DAT_00A8B3EC on Start; FUN_006AE6E0 reads it back on re-open to restore AI-type combos | No persistence array; Rust shell always starts from defaults on open | Player-visible: in gamemd re-opening Skirmish restores prior AI opponent settings; Rust resets every time (covered also at SessionClass__ReadSkirmishSettings row) | gamemd: `decompile_function 0x006ACEE0` (write to DAT_00A8B3EC); Rust: no equivalent restore in `SkirmishShellState` init |
| DAT_00A8B274 / active_ai_count global | INTERNAL-ONLY | WM_COMMAND Start pressed: count active AI rows | FUN_006ACEE0 writes active AI count to `DAT_00A8B274` during Start validation; read by ScenarioClass setup functions (Post_Map_Init, Create_Houses, Gather_Start_Positions) | `launch_session` counts `opponents.iter().filter(is_active()).count()` inline; no persistent global; same count passed to `app_skirmish.rs` slot setup | Same AI count used; no player-visible difference | gamemd: `get_xrefs_to 0x00A8B274`; Rust: `src/ui/skirmish_shell/state/launch.rs:87-99`, `src/app_skirmish.rs` |
| ColorTableBase / 9-entry color table (DAT_008B4040, stride-12) | INTERNAL-ONLY | Dialog init and color-selection change: populate and update color combos | Static 9-entry table at 0x008B4034; each entry has swatch_rgb, flags, label_ptr; ownership field at stride base used for collision detection; populated at runtime by FUN_004E43C0 | Color swatch RGB values from `house_colors.rs` / rules palette; color names from CSF string table; no stride-12 ownership table; color ownership DRIFT already captured at FUN_006ACEE0 row | Swatch colors and names visible in combo; ownership DRIFT already logged | gamemd: `get_xrefs_to 0x008B4040`; Rust: `src/rules/house_colors.rs` + `src/app_skirmish_shell_render.rs` |
| FUN_006AE6E0 / Dialog init — combo population (Win32 CB_RESETCONTENT + CB_SETITEMDATA) | INTERNAL-ONLY | Dialog opens: populate all 7 AI-type combos with Closed/Easy/Medium/Human items | CB_RESETCONTENT + 4 CB_SETITEMDATA calls per slot (item-data -1/2/1/0 for Closed/Easy/Medium/Human); slot 0 excluded from AI-type loop | `combo_items(AiType)` generates `[None, Easy, Normal, Hard]` dynamically per render call; no Win32 messages | Same observable: 4 AI-type options shown per row | gamemd: `decompile_function 0x006AE6E0` §Phase 2; Rust: `src/ui/skirmish_shell/state/combos.rs` `combo_items(AiType)` |
| FUN_006AE6E0 / Dialog init — session restore from DAT_00A8B3F0 array | MISSING | Dialog opens after prior session: restore AI-type + country + color per slot | Reads 7-slot × 3-dword array at DAT_00A8B3F0 (type/country/color per AI slot); maps type-code to item-data and selects combo; restores country via FUN_004E3F70; restores color via FUN_004E49A0; disabled rows get -2 sentinels + EnableWindow(FALSE) | `SkirmishShellState` always initializes from defaults; no read from DAT_00A8B3F0 or any equivalent | Player-visible: in gamemd AI opponent settings (type/country/color) are restored from prior session; Rust resets every time (compound of session-restore MISSING rows) | gamemd: `decompile_function 0x006AE6E0` §Phase 4; Rust: `src/ui/skirmish_shell/state/player_name.rs` (default init) |
| FUN_006AE6E0 / Dialog init — row visibility + AlliesAllowed team enable | MISSING+DRIFT | Dialog opens: show/hide rows per map capacity; set team combos per AlliesAllowed | Phase 7: sets team combo to item-data 3 (AlliesAllowed) or -2 (not allowed) for all 7 slots; Phase 8: calls FUN_006ADDF0 to show/hide rows per map start count | No row visibility adjustment on init; no AlliesAllowed team-enable loop on init — team enable only responds to AI-type changes (DRIFT logged at FUN_006ACD60 row) | Player-visible: both already captured in separate rows (row show/hide MISSING, team AlliesAllowed DRIFT) | gamemd: `decompile_function 0x006AE6E0` §Phase 7-8; Rust: `src/ui/skirmish_shell/state/combos.rs:369-384` |
| ColorTableEntry struct (stride-12, swatch_rgb / flags / label_ptr) | INTERNAL-ONLY | Color combo shows 9 color swatches with names; swatch color and label are per-entry data | 12-byte struct: +0x00 swatch_rgb (DWORD), +0x04 flags (0xFFFFFFFF sentinel), +0x08 label_ptr (StringTable result); 9 entries at 0x8B4034; ownership sub-table at DAT_008B4040 for row-index ownership | No `ColorTableEntry` struct in Rust; color data sourced from rules INI (`HOUSE_COLOR_COUNT = 8`) at runtime; swatch colors from render pipeline, labels from CSF strings; ownership conflicts enforced implicitly via `combo_items()` exclusion logic | INTERNAL-ONLY: same 9-color combo UX observable; Win32 ownership table replaced by Rust item-exclusion | gamemd: struct-colortableentry.md; Rust: `src/skirmish_launch.rs:12`, `src/ui/skirmish_shell/state/combos.rs:282-284` |
| DAT_00A8B23C / SelectedMode_AlliesAllowed_Ptr — AlliesAllowed flag gate | INTERNAL-ONLY | Team combos reflect AlliesAllowed mode setting | Pointer global; `*(ptr + 0x3C)` = AlliesAllowed byte from game-mode object; written by dialog init from FUN_005E2F80; read by FUN_006AE6E0 and FUN_006ACE80 to gate team combo init and enable | `selected_mode_allies_allowed: bool` field in `SkirmishShellState`; set by `repair_teams_for_selected_mode()` from `mode.allies_allowed`; used by `inactive_ai_team_default()` and team `combo_enabled()` logic | INTERNAL-ONLY: pointer-to-struct[0x3C] vs typed bool field — same AlliesAllowed gate behavior | gamemd: global-selectedmode-alliesallowed-ptr.md; Rust: `src/ui/skirmish_shell/state/player_name.rs:226,334,346` |
