# Pathfinding — Parity Report

| Symbol | Verdict | Input | gamemd output | Rust output | Visibility | Citations |
|---|---|---|---|---|---|---|
| `FootClass__Find_Path` — locomotor guard | MATCH | Unit has no locomotor attached | Clears path head (FootClass+0x5E0=-1), returns 0 immediately; locomotor check at vtable+0x2CC | `find_move_path` returns `None` when `ctx.path_grid` is `None`; movement target is not created without a locomotor | Player-invisible: unit never begins moving | gamemd: fn-find_path.md Block 1; Rust: movement_path.rs:200 `let grid = ctx.path_grid?` |
| `FootClass__Find_Path` — close_enough distance metric | DRIFT | Blocked unit at cell (10,10) with goal at (10,12) (2 cells away, 512 leptons Euclidean = Manhattan) — or diagonal goal (10,12)+(2,0) = 2 cells away, Euclidean ≈ 512 leptons but Manhattan = 1024 leptons | gamemd: `Sqrt_Approx(dx²+dy²)` — Euclidean distance in leptons (Block 2); unit stops only when Euclidean dist < CloseEnough (576 leptons ≈ 2.25 cells) | Rust: `(dx.abs() + dy.abs()) * 256` — Manhattan distance (movement_blocked.rs:88-90); for a diagonal blocked scenario the Rust distance is ~41% larger than gamemd's, so unit may fail to stop when gamemd would stop | Units blocked near diagonal goals stop prematurely or too late; visible as minor pathing/stop-distance difference on diagonal approaches (fires on any blocked path with diagonal offset to goal) | gamemd: fn-find_path.md Block 2 `Sqrt_Approx`; Rust: movement_blocked.rs:88-90 |
| `FootClass__Find_Path` — water/building dest remap | MATCH | Destination cell is water (loco-type 6) and unit is not naval | Both call `Find_Nearby_Passable_Cell` / `resolve_requested_move_goal` to redirect to nearest passable cell | Same goal-redirect behaviour: movement_path.rs `resolve_requested_move_goal` + `nearest_move_goal`, gated on `movement_zone.is_water_mover()` for naval units | Player-visible if destination set on water tile; unit goes to nearest land cell | gamemd: fn-find_path.md Block 3; Rust: movement_path.rs:60-118 |
| `FootClass__Find_Path` — building-occupied dest remap | MATCH | Destination cell contains a building (loco-type 7) | Calls `Find_Nearby_Passable_Cell` to find adjacent passable cell | `resolve_requested_move_goal` calls `nearest_move_goal` for any non-walkable goal | Unit redirected to adjacent passable cell | gamemd: fn-find_path.md Block 3; Rust: movement_path.rs:120-142 |
| `FootClass__Find_Path` — occupancy mark/unmark around A* | DRIFT | Any path request by any unit | gamemd: calls `vtable+0x124` (Mark) before `Run_AStar`, `vtable+0x124` (Unmark) after — prevents other units pathing into occupied cells during search (fn-find_path.md Block 4 lines 144-159) | Rust: no occupancy mark/unmark wrapping `find_move_path` calls; entity_block_map is passed as soft-block cost penalty, not a hard lock during search | Rare: two units simultaneously pathing to same cell in the same tick could produce conflicting paths; in serial Rust tick loop the effect is minimal but the hard-exclusion contract is absent | gamemd: fn-find_path.md Block 4; Rust: movement_path.rs:199-329 (no Mark/Unmark call) |
| `FootClass__Find_Path` — path step ring buffer (24-step cap) | MATCH | Path longer than 24 steps | gamemd: copies at most 24 steps into ring buffer at FootClass+0x5E0 (Block 4: `0x18` = 24 max steps) | Rust: `MAX_PATH_SEGMENT_STEPS` cap applied via `truncate_layered_path` in `find_move_path`; auto-segment repath when path exhausted | Player-invisible: long paths are broken into 24-step segments on both sides | gamemd: fn-find_path.md Block 4; Rust: movement_path.rs:238-266 + core.rs `MAX_PATH_SEGMENT_STEPS` |
| `FootClass__Find_Path` — last_path_frame / last_dest_coord stamps | MISSING | Any successful path request | gamemd: stamps `FootClass+0x640` (last_path_frame = g_CurrentFrameCounter), `FootClass+0x644` (last_dest_coord = dest), `FootClass+0x648` (path_retry_wait = 0) after every `Run_AStar` call (Block 5) | Rust: no corresponding `last_path_frame`, `last_dest_coord`, or `path_retry_wait` fields on `MovementTarget` or `GameEntity`; `path_delay_ticks` throttles retries differently via `movement_delay` counter | Throttling behaviour differs: gamemd uses a timestamp+wait-interval for path-retry gating; Rust uses a decrementing counter. Fires every path request — common in any match with moving units | gamemd: fn-find_path.md Block 5; Rust: components.rs `MovementTarget` (no these fields) |
| `FootClass__Find_Path` — convoy chain re-path | MISSING | Unit on MISSION_MOVE with a convoy follower linked at FootClass+0x6C8 | gamemd: after successful path, follows convoy-next linked list and recursively calls `Find_Path` for each convoy follower (Block 6) | Rust: no convoy-chain data structure or recursive re-path; `bump_crush.rs:91` only mentions convoy deadlock prevention as a comment | Convoy-linked units do not automatically re-path when leader moves; visible in any convoy scenario — but convoy formation does not appear to be wired at all in Rust yet | gamemd: fn-find_path.md Block 6; Rust: bump_crush.rs:91 (comment only), no convoy link field found |
| `FootClass__Find_Path` — path failure scatter for non-player units | MISSING | Non-player-controlled unit, `Run_AStar` returns null, unit not close enough to dest | gamemd: checks if not player-controlled + multiplayer → calls `FUN_00500200` to report blocked unit; calls `vtable+0x1E8` (Scatter) for non-player units (Block 7) | Rust: `handle_blocked_tick` triggers repath-urgency escalation and eventually `path_stuck_counter` exhaustion → removes movement_target; no player-ownership-gated scatter call and no `FUN_00500200` equivalent | AI/enemy units blocked near unreachable goals may fail to scatter or report blockage; affects skirmish AI pathing when paths genuinely fail | gamemd: fn-find_path.md Block 7; Rust: movement_blocked.rs:154-183 |
| `FootClass__Find_Path` — path failure stop + loco halt | MATCH | `Run_AStar` returns null and unit is very close to dest (≤1 cell) or on bridge | gamemd: calls `vtable+0x480` (SetDestCell) and `vtable+0x3C8` (Stop) to halt the locomotor (Block 7) | Rust: when repath fails exhaustively (`path_stuck_counter == 0` or close_enough), removes `movement_target` via `finished_entities.push` — unit stops | Unit stops when path fails near goal; observable stop behaviour matches | gamemd: fn-find_path.md Block 7; Rust: movement_blocked.rs:160-173 |
| `FootClass__Run_AStar` — function structure (thin bridge) | MATCH | Any path request | gamemd: thin bridge; gets destination coords via vtable+0x4C, guards on zero goal cell, calls `Path_walk_directions_to_cell` on existing path buffer, then dispatches to `AStar_pathfind_search` on global `PathfinderClass` singleton | Rust: no separate `run_astar` function; behavior is directly inlined in `find_move_path` → `find_path_zoned_marker` → `astar_search`. Same single-codepath dispatch. | Player-invisible structural detail; output is path to destination | gamemd: fn-run_astar.md; Rust: movement_path.rs, zone_search.rs, core.rs `astar_search` |
| `FootClass__Run_AStar` — lepton-to-cell floor conversion | MATCH | Negative destination lepton coords (e.g., unit near map edge with sub-cell offset) | gamemd: `(x_lepton + (x_lepton >> 31 & 0xFF)) >> 8` — sign-correct arithmetic floor shift (fn-run_astar.md §Step 2) | Rust: `position.rx` / `position.ry` are already cell-grid integers (stored as cells, not leptons); sub-cell is separated into `position.sub_x/sub_y`; cell is always correct without a floor-shift | INTERNAL-ONLY: Rust stores cell + sub-cell separately, so the floor-shift is implicitly correct by construction. Any valid lepton coord maps to the same cell. No input space exists where they could diverge. | gamemd: fn-run_astar.md §Step 2; Rust: components.rs `Position` (rx: u16, ry: u16, sub_x: SimFixed, sub_y: SimFixed) |
| `FootClass__Run_AStar` — zero goal cell guard | MATCH | Path request to the null/invalid cell (packed cell == 0) | gamemd: returns 0 immediately if `param_4 == 0` (goal packed cell is zero sentinel — fn-run_astar.md §Step 3) | Rust: `find_move_path` path returns `None` when goal is off-grid; `astar_search` returns `None` when goal is not walkable; zero cell (0,0) is only a valid cell on tiny maps, not a sentinel in Rust | DRIFT (MINOR): gamemd uses packed cell index 0 as an explicit "invalid destination" sentinel; Rust has no equivalent guard on cell (0,0) specifically. On maps where cell (0,0) is a valid passable cell, Rust would attempt to path there where gamemd would silently abort. Triggers only when a unit's locomotor has no destination (unusual; locomotor dest defaults to current pos) | gamemd: fn-run_astar.md §Step 3; Rust: zone_search.rs:143-172 (no sentinel zero-cell guard) |
| `FootClass__Run_AStar` — `Path_walk_directions_to_cell` pre-processing | MISSING | Unit with an existing path direction buffer at `FootClass+0x5E0` | gamemd: before each A* call, converts existing path step-direction buffer into cell waypoints via `Path_walk_directions_to_cell`, passing these as context (start-node hint) to `AStar_pathfind_search` | Rust: no equivalent pre-processing step; the prior path (`MovementTarget::path`) is simply discarded when a repath fires; A* starts fresh from current position with no hint from prior path | A* is initialized without any prior-path warm-start. gamemd can resume near-goal using a step buffer hint when re-pathing mid-path; Rust always runs from scratch. For long paths with mid-path repath this may produce slightly different paths when a hint would have guided the search. Fires on every repath during movement. | gamemd: fn-run_astar.md §Step 4 (`Path_walk_directions_to_cell` @ `0x00429780`); Rust: movement_path.rs (no call) |
| `FootClass__Run_AStar` — `PathfinderClass` singleton and zone exclusion args | MATCH | Any A* call with zone exclusions == 0xFFFFFFFF | gamemd: passes `-1, -1` as zone exclusion args to `AStar_pathfind_search`; these are the "no zone filter" sentinel values — the search is run without zone restrictions | Rust: `AStarOptions` has no explicit zone-exclusion arg; zone corridor is passed via `corridor: Option<...>` field which is `None` for unconstrained searches (equivalent to "no filter") | INTERNAL-ONLY: both sides run A* with no zone exclusion when no corridor is set; the parameter encoding differs but the search space is identical for `None` / `-1` | gamemd: fn-run_astar.md §Step 5 (0xFFFFFFFF zone exclusions); Rust: zone_search.rs `AStarOptions::corridor = None` |
| `AStar_pathfind_search` — retry count (4 vs 5) | MATCH | Targeted move to specific cell (param_6 != -1) vs scatter/no-target | gamemd: 4 retries for specific-target move, 5 for scatter/no-target (fn-astar_pathfind_search.md Phase 5) | Rust: `MAX_CORRIDOR_RETRIES = 5` constant used for all calls in zone_search.rs:36; no distinction between targeted vs scatter retry counts | DRIFT: for targeted-cell moves gamemd limits to 4 retries; Rust always uses 5. This means Rust performs one extra corridor-retry attempt before giving up when a specific cell is unreachable. Fires whenever a unit targets a specific cell that requires corridor search (cross-zone move). | gamemd: fn-astar_pathfind_search.md Phase 5 (`iStack_14 = 4 or 5`); Rust: zone_search.rs:36 `MAX_CORRIDOR_RETRIES = 5` |
| `AStar_pathfind_search` — zone unreachability guard (cross-zone, HS on) | MATCH | Source and destination in different hierarchical zones with HS enabled | gamemd: returns 0 immediately when zones differ AND HS is on (fn-astar_pathfind_search.md Phase 4, line `return 0` at zone mismatch) | Rust: `find_path_zoned_marker_inner`: if `!zone_precheck_passed` and zones differ → returns `None` immediately (zone_search.rs:359-389) | Unit stops immediately rather than running full A* on an unreachable destination; correct behavior | gamemd: fn-astar_pathfind_search.md Phase 4; Rust: zone_search.rs:358-388 |
| `AStar_pathfind_search` — same-zone fallthrough to unrestricted A* | MATCH | Source and destination in same hierarchical zone | gamemd: zone-ID equality check passes, calls `AStar_main_loop` without corridor restriction | Rust: `start_zone == goal_zone && start_zone != ZONE_INVALID` → calls `find_path_with_costs_marker` directly (no corridor) at zone_search.rs:411-425 | INTERNAL-ONLY: both run unconstrained A* on same-zone pairs; corridor wrapping is only for cross-zone | gamemd: fn-astar_pathfind_search.md Phase 4; Rust: zone_search.rs:411-425 |
| `AStar_pathfind_search` — PathfinderClass global singleton | MATCH | Any path request | gamemd: A* state held in `g_PathfinderClass_Singleton` at `0x0087e8b8` — single global instance reused per search | Rust: thread-local `AStarScratch` buffers (core.rs:34-60); per-call `came_from` arrays allocated in `astar_search`; no global singleton in Rust | INTERNAL-ONLY: both approaches clear state before each search; Rust's thread-local scratch reuse is functionally equivalent but without single-instance serialization. In a lockstep sim there's only one search active at a time. | gamemd: fn-astar_pathfind_search.md Phase 1; Rust: core.rs:34-60 `AStarScratch` |
| `AStar_pathfind_search` — bridge-aware coord snap (MapClass__ResolvePathCoord_BridgeAware) | MISSING | Path request when source or destination cell is on bridge deck | gamemd: calls `MapClass__ResolvePathCoord_BridgeAware` on both source and destination before A* (fn-astar_pathfind_search.md Phase 2 @ `0x00583180`) | Rust: no equivalent `ResolvePathCoord_BridgeAware` pre-snap; start/goal are passed directly as `(rx, ry)` from entity position; bridge layer selection is handled inside `astar_search` via `start_layer` param and `MovementLayer::Bridge` cell walkability | DRIFT: for units on bridge cells the search start/goal snap to the bridge-snapped coord in gamemd; Rust uses raw cell coords and handles bridge layer via `start_layer`. If the bridge-snap would redirect start/goal to a different cell index than the raw cell, paths may differ. Fires whenever a unit on a bridge requests a path. | gamemd: fn-astar_pathfind_search.md Phase 2 `MapClass__ResolvePathCoord_BridgeAware @ 0x00583180`; Rust: movement_path.rs `find_move_path` (no snap step) |
| `AStar_pathfind_search` — HS-capable flag per unit (FootClass+0x3D5) | MISSING | Non-HS-eligible unit (e.g., chrono-locked, air-speed, or flag not set) | gamemd: per-unit `FootClass+0x3D5` flag gates whether hierarchical search is used; chrono-locked check at vtable+0x320 also disables HS (fn-astar_pathfind_search.md Phase 4) | Rust: no per-entity HS-eligible flag; zone hierarchy is used for all units that have `blocker_neighbor_counts` available (zone_search.rs:280-282); chrono-lock check absent | For chrono-locked or HS-ineligible units, Rust runs corridor hierarchy where gamemd would run flat A*. This can produce different paths (hierarchy may exclude zones that flat A* would cross). Fires for any unit under chrono lock or that matches the other HS-disable conditions. | gamemd: fn-astar_pathfind_search.md Phase 4 (`FootClass+0x3D5`, vtable+0x320); Rust: zone_search.rs:280-282 (no per-unit HS gate) |
| `AStar_pathfind_search` — chrono-infantry pre-pass | MISSING | Infantry unit with teleporter flag (`TechnoType+0xD94 != 0`) | gamemd: creates COM locomotor via `CoCreateInstance`, queries move target, overrides `param_7 = SPEED_Foot = 7` before A* (fn-astar_pathfind_search.md Phase 3) | Rust: no chrono-infantry teleporter COM pre-pass in pathfinding; chrono miner handling is separate and only covers CHRONMINI inbound warp | Chrono infantry path-request does not get the SPEED_Foot override; movement zone may differ for chrono infantry units. Fires on any chrono infantry move order. | gamemd: fn-astar_pathfind_search.md Phase 3; Rust: teleport_movement.rs (no COM locomotor path step) |
| `AStar_pathfind_search` — UpdateHierarchicalEdges on retry failure | MATCH | Path fails in a corridor retry attempt | gamemd: calls `PathfinderClass__UpdateHierarchicalEdges` after each failed `AStar_main_loop` attempt to invalidate blocked zone edges (fn-astar_pathfind_search.md Phase 5) | Rust: `exclude_corridor_edges` accumulates failed corridor zone edges into `excluded_edges: BTreeSet<ZoneEdge>` and excludes them from the next Dijkstra run (zone_search.rs:458-463) | INTERNAL-ONLY: both progressively exclude failed zone edges across retries; mechanism differs (Rust accumulates per-call, gamemd mutates persistent PathfinderClass state) but observable output — progressive exclusion of the same failing zones — is equivalent | gamemd: fn-astar_pathfind_search.md Phase 5 `PathfinderClass__UpdateHierarchicalEdges`; Rust: zone_search.rs:428-464 |
| `AStar_main_loop` — iteration limit | DRIFT | Map with a long, winding path requiring more than 10,000 node expansions | gamemd: expansion loop terminates when `local_34 == 10000` (fn-astar_main_loop.md Block 9 line `local_34 != 10000`; verified via `decompile_function 0x00429A90`); returns null path (no route found) | Rust: `MAX_SEARCH_NODES = 65_527` (core.rs:107); comment claims "Original engine uses 65,527 (0xFFF7)" — contradicts the decode doc value of 10000. For paths requiring 10,001–65,527 expansions: gamemd fails (returns no path); Rust succeeds and returns a path | DRIFT: gamemd abandons searches exceeding 10,000 expansions and the unit stops; Rust continues up to 65,527. On maps with extremely convoluted paths or large exclusion zones, Rust will find routes that gamemd would never produce — a path vs. no-path observable difference. The Rust comment's claim of 65,527 being the gamemd value is unverified and contradicts the decode doc. | gamemd: fn-astar_main_loop.md Block 9 (`local_34 != 10000`, verified `decompile_function 0x00429A90`); Rust: core.rs:104-107 `MAX_SEARCH_NODES = 65_527` |
| `AStar_main_loop` — closed-list reopen tolerance (1.009) | DRIFT | Path through cells with g-cost difference near 1.009 — e.g., unit on open terrain where two routes cost 1.0 apart | gamemd: neighbor skipped only if `existing_g < current_g + 1.009` (constant at `0x007E37C0`, verified via `read_memory`); cells with existing_g between current_g and current_g+1.009 are NOT re-expanded (fn-astar_main_loop.md Block 7) | Rust: hard bool closed lists — `if bridge_closed[n_idx] { continue; }` and `if ground_closed[n_idx] { continue; }` (core.rs:864-875, 991-996); a cell once closed is never reconsidered regardless of g-cost; code comment at line 988-990 explicitly defers 1.009 tolerance | DRIFT: gamemd's 1.009 tolerance permits minor path refinement in dense cost landscapes where two routes are within 1.009 cost of each other; Rust never revisits. For typical terrain (integer costs in range 1–10000) the tolerance fires at the boundary of equal-cost expansions — potentially affecting tie-breaking between two routes. Fires on any search where two path routes have g-costs within the 1.009 window. | gamemd: fn-astar_main_loop.md Block 7 (`0x007E37C0` = 1.009, `read_memory 0x007E37C0`); Rust: core.rs:864-875, 988-996 (hard bool, no tolerance) |
| `AStar_main_loop` — direction tie-breaker epsilon values | MATCH | Two candidate paths with equal integer base cost to the same cell | gamemd: adds float epsilons `[0.001, 0.005, 0.002, 0.006, 0.003, 0.007, 0.004, 0.008]` by direction from `g_DirEpsilonTable @ 0x0081872C` (fn-astar_main_loop.md Block 6, read_memory verified) | Rust: adds integers `[1, 5, 2, 6, 3, 7, 4, 8]` at 10000× scale (core.rs:369-378 `DIR_TIEBREAK`); same N<NE<E<SE ordering; algebraically equivalent scaling of the same relative order | INTERNAL-ONLY: both produce identical direction-preference ordering (N preferred over NE over E …). The scale ratio 1000× is within the integer cost range; no input produces a different tie-break winner. Algebraic proof: for any two directions d1 < d2, the Rust tiebreak d1 < d2 iff the gamemd epsilon[d1] < epsilon[d2] — same ordering preserved. | gamemd: fn-astar_main_loop.md Block 6 `g_DirEpsilonTable @ 0x0081872C`; Rust: core.rs:364-378 `DIR_TIEBREAK` |
| `AStar_main_loop` — direction-8 tube edge handling | MATCH | Unit in an underground tube (direction 8 neighbor) | gamemd: direction 8 uses Chebyshev cost (no epsilon applied), calls `AStar_compute_edge_cost` only for directions 0–7; tube edge gets flat cost (fn-astar_main_loop.md Block 5 direction-8 branch) | Rust: tube neighbors handled via `TUBE_DIR_TIEBREAK = 9` (greater than all compass values 1-8), no terrain/entity cost applied for tube jumps (core.rs:382-383, tube direction skips `compute_code2_multiplier` and `apply_search_marker_cost`) | MATCH: tube edges get distinct (higher) tiebreak value in both; edge cost is not run for tube direction in either | gamemd: fn-astar_main_loop.md Block 5 direction-8; Rust: core.rs:380-395 `TUBE_DIR_TIEBREAK` |
| `AStar_main_loop` — blocked-goal fallback (impassable destination) | MATCH | Destination cell is blocked/impassable but start height and goal height are close | gamemd: if impassable neighbor IS the destination AND `abs(start_height - dest_height) < 2`, accepts the current expansion node as path terminus (fn-astar_main_loop.md Block 9 `0x0042a17d`) | Rust: `if (nx, ny) == goal && start_height.abs_diff(goal_height) <= 1` — accepts current node as path endpoint (core.rs:1025-1037); condition `<= 1` is equivalent to `< 2` | MATCH: same height tolerance, same acceptance condition; unit reaches closest reachable cell to blocked goal | gamemd: fn-astar_main_loop.md Block 9 (blocked-goal fallback); Rust: core.rs:1021-1037 |
| `AStar_main_loop` — hierarchical zone gate + blocker-neighbor exception | MATCH | Cell in a zone not marked in hierarchy expansion set, but with non-zero blocker-neighbor refcount (`CellClass+0x122 != 0`) | gamemd: when hierarchy flag is set, only expands cells with level-0 zone marked in Pathfinder+0x40; exception: cells with `CellClass+0x122 != 0` (blocker-neighbor refcount) are also expanded regardless (fn-astar_main_loop.md Block 8) | Rust: `options.hierarchy_gate.allows(nx, ny)` — returns true if `marked_level0.contains(&zone) || blocker_neighbor_counts.count_at(x, y) != 0` (core.rs:358-361); same two-condition gate | MATCH: same zone-marked-OR-blocker-refcount expansion rule; paths through zone boundaries with blocker neighbors are handled identically | gamemd: fn-astar_main_loop.md Block 8; Rust: core.rs:354-361 `HierarchyGate::allows` |
| `AStar_main_loop` — post-processing pipeline (reconstruct + smooth + optimize) | MATCH | Any successful path found by A* | gamemd: on success calls `AStar_reconstruct_path` → `Path_smooth_corners` → `Path_optimize_straight_segments` (fn-astar_main_loop.md Block 9 callees, verified via `get_function_callees 0x00429A90`) | Rust: `reconstruct_path_dual` → `smooth_corners` → `optimize_straight_segments` within `astar_search` (core.rs post-loop processing) | MATCH (structural): same 3-stage pipeline — reconstruct parent chain, smooth corners, merge straight segments. Parity of individual stages is covered by compare-astar_reconstruct_path and compare-path_smooth_corners tasks | gamemd: fn-astar_main_loop.md Block 9 callees; Rust: core.rs reconstruct + post-processing section |
| `AStar_compute_edge_cost` — base cost table (codes 0–7) | MATCH | Cells with each `Can_Enter_Cell` return code 0–7 | gamemd: `g_AStar_EdgeCost_BaseTable @ 0x0081870C` = `[1.0, 1000.0, 1.0, 1.0, 60.0, 20.0, 8.0, 10000.0]` (fn-astar_compute_edge_cost.md §Base cost table, verified via `read_memory 0x0081870C`) | Rust: code 0=`STEP_COST`(=1.0 base), code 1=1000 via `entity_blocks` hard-block, code 2=`compute_code2_multiplier`, code 4=60 from `terrain_costs` water cost, code 5=20 cliff via `CLIFF_COST_MULTIPLIER×5`, code 6=8 via `CODE6_MULT_STATIONARY_ALLY`, code 7=10000 buildings (core.rs:118-138) | MATCH: all 8 cost codes produce the same numeric multipliers; encoding differs (Rust splits some codes into separate lookup paths) but the values are identical | gamemd: fn-astar_compute_edge_cost.md §Base cost table `g_AStar_EdgeCost_BaseTable @ 0x0081870C`; Rust: core.rs:116-138 cost constants |
| `AStar_compute_edge_cost` — code-2 entity prediction (urgency 0/1/2) | MATCH | Friendly moving unit (code 2) at each of the 3 urgency levels | gamemd: urgency 0 = chain-walk up to 10 hops (clear→1.0, jam→4.0), urgency 1 = 4.0, urgency 2 = 1000.0 (fn-astar_compute_edge_cost.md §Code-2 block, verified via `decompile_function 0x00429830`) | Rust: `compute_code2_multiplier`: urgency≥2→`CODE2_MULT_ROUTE_AROUND=1000`, urgency==1→`CODE2_MULT_JAM=4`, urgency==0→chain walk up to `CODE2_CHAIN_MAX_HOPS=10` returning 1 or 4 (core.rs:1888-1930) | MATCH: same 3-branch urgency logic, same chain-walk hop limit (10), same output values (1, 4, 1000) | gamemd: fn-astar_compute_edge_cost.md §Code-2 prediction; Rust: core.rs:118-126, 1888-1930 `compute_code2_multiplier` |
| `AStar_compute_edge_cost` — 0x40000 temporary search marker (×4 cost) | MATCH | Cell flagged with `CellClass+0x140 & 0x40000` during A* search | gamemd: cell flagged with bit `0x40000` in `CellClass+0x140` → edge cost multiplied by 4.0 (constant verified at `0x007E37BC` = 4.0f, fn-astar_compute_edge_cost.md §0x40000 marker block) | Rust: `SearchMarkerOverlay` models the same XOR-toggled per-search overlay; `apply_search_marker_cost` multiplies by `SEARCH_MARKER_COST_MULTIPLIER = 4` (core.rs:134-137, 1933-1943) | MATCH: same ×4 multiplier, same XOR toggle lifecycle modeled; Rust overlay is search-scoped rather than mutating persistent CellClass bits but produces identical cost output per-search | gamemd: fn-astar_compute_edge_cost.md §0x40000 marker; Rust: core.rs:134-137 `SEARCH_MARKER_COST_MULTIPLIER`, 1933-1943 `apply_search_marker_cost` |
| `AStar_compute_edge_cost` — bridge flank multiplier (Pathfinder+0x01 gate) | DRIFT | Unit pathing alongside a bridge flank (bridge is neighbor but not traversed) | gamemd: if `PathfinderClass+0x01` flag set → apply bridge-flank multiplier: no-bridge-flank=10.0, one-bridge=1.0, both-bridge=2.0 (fn-astar_compute_edge_cost.md §Bridge flank block, verified via `decompile_function 0x00429830`) | Rust: `bridge_flank_multiplier` function defined with correct constants (core.rs:1946-1953) but never called in `astar_search`; comment at core.rs:141 says "runtime wiring is blocked until PathfinderClass+0x01 lifecycle is verified" — the multiplier is dead code | DRIFT: when `Pathfinder+0x01` would be set (bridge-adjacent paths), gamemd applies a flank penalty (no-bridge side = ×10) discouraging flanking; Rust applies no flank penalty. Units pathing along bridge sides may choose routes that are 10× cheaper in Rust than in gamemd. Fires any time a unit paths near a bridge. | gamemd: fn-astar_compute_edge_cost.md §Bridge flank multiplier; Rust: core.rs:139-145 `BRIDGE_FLANK_*` constants, 1946-1953 `bridge_flank_multiplier` (dead code) |
| `AStar_create_node` — heuristic function (Euclidean) | MATCH | Any A* node expansion where neighbor is not at goal | gamemd: `Sqrt_Approx(dx² + dy²)` — Euclidean distance in cell units, float (fn-astar_create_node.md §Heuristic computation, callee `Sqrt_Approx @ 0x004CAC40`) | Rust: `euclidean_heuristic(ax, ay, bx, by)` = `isqrt((dx²+dy²) × 1_000_000) as i32` — scaled integer Euclidean; at STEP_COST=1000 this equals `sqrt(dx²+dy²) × 1000` (core.rs:1864-1884) | INTERNAL-ONLY: both compute Euclidean distance; Rust scales by STEP_COST (1000) to stay in integer domain matching the scaled integer edge costs. For any pair (ax,ay,bx,by), Rust output = gamemd output × 1000 — same relative ordering, same effect on path selection. No input produces different tie-break winner. | gamemd: fn-astar_create_node.md §Heuristic computation (`Sqrt_Approx @ 0x004CAC40`); Rust: core.rs:1864-1884 `euclidean_heuristic` |
| `AStar_create_node` — bridge layer height selection (ground→bridge ascend condition) | DRIFT | Unit on flat ground entering a bridge cell where `abs(neighbor_level - parent_height) <= 1` (e.g., parent_height=5, neighbor ground_level=4) | gamemd: ascend to bridge layer when parent is on ground AND `abs(neighbor_level - parent_height) <= 1` (fn-astar_create_node.md §Path height computation, third branch: `abs(neighbor_ground - parent_h) <= 1` → `height = level + 4`) | Rust: `compute_neighbor_height` Case 3: ascend only when `parent_height - neighbor.ground_level == 4 AND neighbor.transition` (core.rs:436-444) — requires exact diff of 4 AND the bridgehead transition flag, not abs-diff≤1 | DRIFT: gamemd promotes to bridge layer for near-flat ground transitions (parent and neighbor at same terrain level); Rust requires exact +4 height difference AND transition flag. Any bridge entry from flat terrain (same ground level) that gamemd would ascend, Rust would stay on ground layer. Fires on any bridge approach from flat terrain. Affects which bridge layer a unit is routed through — visible if the unit then traverses differently on the bridge or misses bridge-only walkable cells. | gamemd: fn-astar_create_node.md §Path height computation third branch (`abs(neighbor_level - parent_h) <= 1`); Rust: core.rs:436-444 `compute_neighbor_height` Case 3 |
| `AStar_create_node` — node pool allocation (internal memory management) | MATCH | Any node creation during A* | gamemd: fixed pre-allocated pools at `Pathfinder+0x0C` (CellInfo, 12 bytes/entry) and `Pathfinder+0x10` (Node, 16 bytes/entry) with bump-pointer allocation (fn-astar_create_node.md §Pool allocation) | Rust: `BinaryHeap<Reverse<AStarNode>>` and `came_from: Vec<usize>` arrays pre-allocated in `AStarScratch` (core.rs:40-68) — no separate CellInfo pool; node data inlined into `AStarNode` struct | INTERNAL-ONLY: different memory layout; both allocate one node per expansion. Rust's inline struct is more cache-friendly; gamemd uses two separate pool arrays. No observable difference in path result. | gamemd: fn-astar_create_node.md §Pool allocation; Rust: core.rs:34-68 `AStarScratch` |
| `AStar_reconstruct_path` — path backtracking and output format | MATCH | Any successful A* path of length ≥2 | gamemd: walks parent-chain from goal → start, writes direction bytes (0=S, 1=SW, 2=W, 3=NW, 4=N, 5=NE, 6=E, 7=SE) into a direction-byte buffer, appends 0xFFFFFFFF sentinel, writes path length + first-node X to global struct at `DAT_0089a2d8` (fn-astar_reconstruct_path.md §Behavioral analysis) | Rust: `reconstruct_path_dual` walks `came_from` arrays from goal → start via `decode_from`, reverses to produce `Vec<LayeredPathStep>` of (rx, ry, layer) — no direction bytes; consumers compute direction at the movement layer from cell pairs (core.rs:677-731) | INTERNAL-ONLY: both produce a complete ordered cell-sequence from start to goal; direction-byte encoding is an intermediate format that gamemd's smoothing functions consume; Rust uses direct cell pairs which are equivalent information. Observable output (unit moves along same cells) is identical. Parity of the sequence depends on compare-path_smooth_corners and compare-path_optimize_straight_segments. | gamemd: fn-astar_reconstruct_path.md §Behavioral analysis; Rust: core.rs:677-731 `reconstruct_path_dual` |
| `AStar_reconstruct_path` — direction encoding (0-based-South convention) | MATCH | Path step where unit moves from cell to adjacent cell in any of 8 directions | gamemd: `g_DirTable @ 0x00818760` maps `dy*3+dx` → direction byte: 0=S, 1=SW, 2=W, 3=NW, 4=N, 5=NE, 6=E, 7=SE (fn-astar_reconstruct_path.md §Direction encoding, verified via `read_memory 0x00818750`) | Rust: no direction-byte encoding in `reconstruct_path_dual`; direction is implicit in the cell sequence and consumed downstream. The movement system iterates `NEIGHBORS` in order N=0…NW=7 (core.rs:384-395) — a different convention (0=N). The direction byte convention mismatch is only relevant if direction bytes from the path buffer are consumed externally. | MATCH (scoped): `reconstruct_path_dual` output is cell coordinates, not direction bytes. The downstream `Path_smooth_corners` and `Path_optimize_straight_segments` are separate compare tasks. The direction-byte convention in gamemd is an internal encoding consumed within the smoothing pipeline; Rust does not expose direction bytes at this layer. | gamemd: fn-astar_reconstruct_path.md §Direction encoding `g_DirTable @ 0x00818750`; Rust: core.rs:384-395 `NEIGHBORS`, 677-731 `reconstruct_path_dual` |
| `PathfinderClass__Constructor` — singleton struct and buffer allocation | MATCH | Game startup: PathfinderClass instance is created once for the session | gamemd: allocates global singleton at `0x0087e8b8`; pre-allocates 5 large heap buffers (closed-path 1.5MB, open list 1MB, closed list 65536 cells, node pool 10000 nodes, secondary 160000 bytes); 3 sub-structs at `+0x74/+0x8c/+0xa4`; 3 path-record arrays (fn-pathfinder_constructor.md §Heap buffer inventory) | Rust: thread-local `PathfindWorkspace` with 6 `Vec` fields (ground_g, bridge_g, ground_from, bridge_from, ground_closed, bridge_closed); no fixed-size pre-allocation — Vecs grow to `total_cells` on first use (core.rs:44-68). No separate sub-structs or path-record arrays. | INTERNAL-ONLY: both initialize per-search state once before use. Rust's thread-local `Vec` approach is functionally equivalent to gamemd's pre-allocated pools for a single-threaded sim. Observable output (paths found) is identical. The 10000-node pool capacity in the constructor is consistent with the `local_34 != 10000` iteration limit noted in compare-astar_main_loop. | gamemd: fn-pathfinder_constructor.md §Heap buffer inventory; Rust: core.rs:34-95 `PathfindWorkspace` |
| `PathfinderClass__Constructor` — cost_multiplier and urgency defaults | MATCH | Any path request using the freshly-constructed singleton | gamemd: `Pathfinder+0x04` (cost_multiplier) = 1.0f; `Pathfinder+0x3C` (urgency) = 0 (fn-pathfinder_constructor.md §Struct field accesses) | Rust: `AStarOptions::urgency` defaults to 0; `STEP_COST = 1000` is the base cost that callers multiply by (core.rs:101, zone_search.rs `urgency: 0` default in call sites) | INTERNAL-ONLY: both start with urgency=0 and no cost multiplier beyond base. Pathfinder+0x04 multiplier maps to STEP_COST scaling in Rust — same effect (scale=1.0 means no extra multiplier). | gamemd: fn-pathfinder_constructor.md `+0x04` = 1.0f, `+0x3C` = 0; Rust: core.rs:101 `STEP_COST`, zone_search.rs urgency default |
| `PathfinderClass__Reset` — per-search state clear (epoch vs full refill) | DRIFT | Back-to-back path requests within the same match session | gamemd: resets pools via epoch increment (`Pathfinder+0x28 += 1`); old closed-list values persist — they are implicitly ignored by epoch mismatch; full zeroing only on uint32 overflow (every 2^32 resets) (fn-pathfinder_reset.md §Step 3) | Rust: `PathfindWorkspace::reset` calls `refill` on every reset — sets all 6 Vec entries to sentinel values (i32::MAX or false); no epoch; always-full reinit (core.rs:56-68) | INTERNAL-ONLY: gamemd's epoch trick is an optimization to avoid full array zeroing; Rust always zeros. For A* search correctness, both approaches produce identical closed-list behavior for each search. The epoch trick is purely a performance optimization — not observable in path output. | gamemd: fn-pathfinder_reset.md §Step 3 epoch increment; Rust: core.rs:56-68 `PathfindWorkspace::reset` |
| `PathfinderClass__UpdateBridgePassability` — peer-path search marker toggle | MISSING | Unit pathing when another unit nearby has a queued movement path | gamemd: before A*, XOR-toggles `CellClass+0x140 bit 0x40000` on cells along peer units' queued paths (fn-pathfinder_update_bridge_pass.md §Peer scan); this makes peer-path cells 4× more expensive in A*; cancelled by a second call after search; gated by `PathfinderClass+0x03 != 0` and `PathfinderClass+0x3C != 0` | Rust: no call to `UpdateBridgePassability` equivalent; `SearchMarkerOverlay` is the mechanism for the 0x40000 marker, but it is not populated from peer unit paths in any wiring in `movement_path.rs` | DRIFT: gamemd biases A* away from cells that nearby units are moving through (4× cost on peer path cells); Rust never applies this bias. In dense traffic (multiple units moving simultaneously), Rust produces paths that ignore peer traffic — units may choose the same cells and collide/block more than in gamemd. Fires whenever two or more units are moving in proximity, which is common in any skirmish. | gamemd: fn-pathfinder_update_bridge_pass.md §When it is invoked, §Peer scan; Rust: movement_path.rs (no UpdateBridgePassability call), core.rs:208-216 `SearchMarkerOverlay` (unwired for peer paths) |
| `PathfinderClass__UpdateBridgePassability` — 5×5 occupied-cell toggle | MISSING | Unit pathing when probe cell has occupied neighbors but no eligible peer paths | gamemd: if no eligible peer path found and `PathfinderClass+0x3C == 1` → clears flag and returns; else runs 5×5 scan around probe cell: XOR-toggles occupied (`CellClass+0x124 != 0`) cells in the 5×5 neighborhood + unconditionally toggles probe cell (fn-pathfinder_update_bridge_pass.md §5x5 fallback) | Rust: no equivalent 5×5 occupied-cell scan; no probe-cell toggle | DRIFT: the 5×5 fallback biases A* away from crowded areas even when no peer has a queued path (static occupancy penalty); Rust produces unbiased paths through occupied 5×5 neighborhoods. Fires whenever a unit paths near occupied cells without a peer queued path — common around buildings, choke points, waiting units. | gamemd: fn-pathfinder_update_bridge_pass.md §5x5 fallback; Rust: movement_path.rs (no equivalent) |
| `Path_smooth_corners` — diagonal-only zigzag anchor rule | MATCH | Zigzag pattern starting with a cardinal step (e.g. N then E) | gamemd: cardinal steps reset anchor to `0xFFFFFFFF`; only odd-index (diagonal) directions persist as anchor; cardinal→cardinal 90° turns never smoothed (fn-path_smooth_corners.md §Key invariants verified claim 1) | Rust: `if !is_diagonal_dir(d0) { i += 1; continue; }` — skips smoothing when anchor step is cardinal (path_smooth.rs:110-113) | MATCH: same diagonal-anchor invariant; same effect on path shape | gamemd: fn-path_smooth_corners.md §Invariant 1; Rust: path_smooth.rs:106-113 |
| `Path_smooth_corners` — direction-8 (tube) exclusion from smoothing | MATCH | Path through an underground tube segment (direction 8 step) | gamemd: `uVar7 != 8 && uVar2 != 8` — both anchor and next step must be non-tube to enter zigzag mode (fn-path_smooth_corners.md §Key invariants verified claim 2) | Rust: `d0 == DIR_INVALID || d1 == DIR_INVALID` check plus no tube-direction handling; direction 8 would produce `DIR_INVALID` from `direction_between` since it's not in `DIR_DELTAS` (path_smooth.rs:100-103). Bridge/tube steps pass through smoothing unchanged. | MATCH: tube steps never participate in zigzag smoothing on either side; result is that tube path segments are never shortened by smoothing | gamemd: fn-path_smooth_corners.md §Invariant 2; Rust: path_smooth.rs:100-103 `DIR_INVALID` |
| `Path_smooth_corners` — corner-cutting passability check | MATCH | Diagonal shortcut through a corner where one adjacent cardinal is blocked | gamemd: `Path_smooth_single_segment` verifies both cardinal neighbors of the diagonal shortcut are walkable before accepting the shortcut (fn-path_smooth_corners.md §Callees `Path_smooth_single_segment @ 0x0042B420`) | Rust: `if is_diagonal_dir(shortcut_dir)` → checks both cardinal neighbors `card_a` and `card_b` are walkable (path_smooth.rs:129-144) | MATCH: both require that both adjacent cardinals are walkable for any diagonal shortcut; prevents cutting through impassable corners | gamemd: fn-path_smooth_corners.md `Path_smooth_single_segment` callees; Rust: path_smooth.rs:127-144 |
| `Path_optimize_straight_segments` — 20-step window limit | MATCH | Path longer than 20 steps | gamemd: `if (0x13 < iVar13) break` — loop body never processes step index 20 or beyond; at most 20 steps are analyzed per call (fn-path_optimize_straight_segments.md §Key invariants claim 1) | Rust: `MAX_OPTIMIZE_STEPS: usize = 20`; `steps_to_check = (result.len()-1).min(MAX_OPTIMIZE_STEPS)` (path_smooth.rs:245, 267) | MATCH: same 20-step hard limit on both sides | gamemd: fn-path_optimize_straight_segments.md §Invariant 1 (`0x13 < iVar13`); Rust: path_smooth.rs:245 `MAX_OPTIMIZE_STEPS = 20` |
| `Path_optimize_straight_segments` — drift detection algorithm | DRIFT | Path with a mild curve that stays within Chebyshev bounds but has high cross-product drift | gamemd: Chebyshev regression — fires when `max(abs(cum_x), abs(cum_y))` decreases vs previous step (path curves back toward anchor); triggers on any decrease in the running max (fn-path_optimize_straight_segments.md §Core algorithm `abs_new_x < local_70 || abs_new_y < local_6c`) | Rust: cross-product drift — fires when `(cum_dx × ideal_dy - cum_dy × ideal_dx)² > ideal_dist² × DRIFT_THRESHOLD` (path_smooth.rs:388-393); a different geometric criterion | DRIFT: the two algorithms detect different path shapes as "needing reroute." A path where Chebyshev decreases but cross-product is small would trigger gamemd but not Rust; conversely, a path with large cross-product but non-decreasing Chebyshev would trigger Rust but not gamemd. Both pass 2 implementations affect the same set of long paths — the observable difference is which specific curved paths get straightened. Fires on any path long enough to show Chebyshev regression. | gamemd: fn-path_optimize_straight_segments.md §Core algorithm Chebyshev regression; Rust: path_smooth.rs:358-397 `find_drift_segment` |
| `Path_optimize_straight_segments` — direction-8 tube reset | MATCH | Path segment containing a tube/bridge jump | gamemd: direction 8 resets all drift state (`local_78`, `local_4c`, etc.) to 0; restarts tracking from tube exit coordinate (fn-path_optimize_straight_segments.md §Invariant 2) | Rust: tube direction 8 does not appear in `DIR_DELTAS`; `direction_between` returns `DIR_INVALID` for non-adjacent pairs; drift tracking naturally resets at `DIR_INVALID` steps since the cross-product computation only operates on valid consecutive cell pairs | MATCH: tube/bridge steps are never smoothed by pass 2 on either side; segment tracking resets after a tube jump | gamemd: fn-path_optimize_straight_segments.md §Invariant 2 direction-8 reset; Rust: path_smooth.rs:350-397 `find_drift_segment` (no direction-8 in valid pairs) |
| `Path_optimize_straight_segments` — compaction of deleted entries | MATCH | Path with entries marked as deleted by rerouting (0xFFFFFFFE) | gamemd: final compaction pass removes all `0xFFFFFFFE` entries in-place, fills tail with `0xFFFFFFFF`, updates `param_2[2]` to compacted length (fn-path_optimize_straight_segments.md §Compaction pass) | Rust: `result.splice(seg_start..=seg_end, replacement)` in-place replacement — no sentinel-delete cycle; deleted entries are replaced immediately with the rerouted path (path_smooth.rs:280-284) | INTERNAL-ONLY: both produce a compacted path with no gaps; gamemd uses a two-phase approach (mark-delete then compact), Rust replaces immediately. Observable output is same path length after optimization. | gamemd: fn-path_optimize_straight_segments.md §Compaction pass; Rust: path_smooth.rs:280-284 `result.splice` |
| `PathfinderClass__UpdateHierarchicalEdges` — soft-split exclusion (flood-fill branch) | DRIFT | A* fails and the current cell is locally reachable (zero return from flood-fill); neighbor zones differ from current zone | gamemd: calls `ZoneMap__FloodFillReachableZones` to detect soft-split; for each neighbor zone reachable by flood that differs from current zone, appends a packed undirected edge `min<<16|max` to per-level exclusion vectors at `PathfinderClass+0x7c/0x94/0xac` (fn-pathfinder_update_hier_edges.md §Zero-return branch) | Rust: no flood-fill call; `exclude_corridor_edges` appends consecutive zone pairs from the failed corridor path to `BTreeSet<ZoneEdge>` (zone_search.rs:458-460, 698-708) — only edges along the failed path are excluded, not all edges reachable from the blocked cell via flood | DRIFT: gamemd excludes all edges between the current zone and any flood-reachable neighbor zone (potentially many exclusions per call); Rust excludes only the consecutive zone pairs in the failed corridor path. On a zone graph where the blocked cell's zone has multiple neighbors, gamemd may exclude more edges per failure than Rust, causing different corridor choices on subsequent retries. Fires on any hierarchical A* failure where the cell is not completely blocked (soft-split). | gamemd: fn-pathfinder_update_hier_edges.md §Zero-return branch (`ZoneMap__FloodFillReachableZones`); Rust: zone_search.rs:458-460, 698-708 `exclude_corridor_edges` |
| `PathfinderClass__UpdateHierarchicalEdges` — hard-split branch (InvalidateZoneEdge) | MISSING | A* fails and the current cell is fully blocked (nonzero return from flood-fill) | gamemd: calls `PathfinderClass__InvalidateZoneEdge(this, current_zone, level)` which reads the stored `Zone_precheck` path for this level from `PathfinderClass+0xbc + level*1000`; appends the specific adjacent stored-path edge; may clear `PathfinderClass+0x38` (hierarchy-valid flag) if no actionable path edge exists — this makes the next retry skip hierarchical mode entirely (fn-pathfinder_update_hier_edges.md §Nonzero-return branch) | Rust: no hard-split branch and no hierarchy-valid flag; `exclude_corridor_edges` runs the same exclusion logic regardless of whether the cell is completely blocked; the retry loop terminates by exhausting `MAX_CORRIDOR_RETRIES`, not by clearing a per-unit HS flag | DRIFT: when the A* goal is genuinely unreachable at a given hierarchy level (hard split), gamemd falls back to flat A* by clearing `+0x38`; Rust keeps running corridor retries until the retry budget is exhausted. This means Rust runs more A* attempts than gamemd for truly unreachable destinations. Observable effect: unit takes longer to give up on unreachable goals (5 retries in Rust vs fewer in gamemd after HS is disabled). | gamemd: fn-pathfinder_update_hier_edges.md §Nonzero-return branch (`PathfinderClass__InvalidateZoneEdge`); Rust: zone_search.rs:428-464 (no hard-split branch, no hierarchy-valid flag) |
| `PathfinderClass__UpdateHierarchicalEdges` — 3-level hierarchy iteration | MATCH | Any hierarchical A* failure that triggers UpdateHierarchicalEdges | gamemd: outer loop `iVar10 = 0..2` iterates all 3 hierarchy levels; per-level exclusion vectors at `+0x7c`, `+0x94`, `+0xac` (stride 0x18) (fn-pathfinder_update_hier_edges.md §Level loop) | Rust: `find_zone_corridor` takes `adjacency: &ZoneAdjacency` which encodes all hierarchy levels; `excluded_edges: BTreeSet<ZoneEdge>` is flat across all levels but zone IDs implicitly encode level membership | INTERNAL-ONLY: both update exclusion state for all 3 levels per failure; Rust's flat `BTreeSet` structure is equivalent for corridor pathfinding purposes since zone IDs don't repeat across levels | gamemd: fn-pathfinder_update_hier_edges.md §Level loop; Rust: zone_search.rs:628-695 `find_zone_corridor` |
| `PathfinderClass__InvalidateZoneEdge` — direct edge append from stored path | MISSING | Hard-split: A* fails on a zone and stored `Zone_precheck` path has ≥2 zones | gamemd: scans stored zone path at `PathfinderClass+0xBC + level*1000` for `current_zone`; selects adjacent edge `(path[i-1], path[i])` or `(path[i], path[i+1])`; appends canonicalized `min<<16|max` to per-level exclusion vector (fn-pathfinder_invalidate_zone_edge.md §Step 3–4, `FUN_0042D830`) | Rust: `exclude_corridor_edges` appends all consecutive zone pairs from the failed corridor path (zone_search.rs:698-708); no scanning of a stored `Zone_precheck` path; no concept of "stored zone precheck path" at `PathfinderClass+0xBC` | DRIFT: gamemd selects the specific adjacent edge in the stored zone path that crosses the blocked zone; Rust excludes all consecutive edges in the failed corridor. The specific edge selected by gamemd is a subset of what Rust excludes — gamemd's exclusion is more surgical (excludes fewer edges per call) but requires a stored path reference. Fires on hard-split hierarchical failures. | gamemd: fn-pathfinder_invalidate_zone_edge.md §Step 3–4; Rust: zone_search.rs:698-708 `exclude_corridor_edges` |
| `PathfinderClass__InvalidateZoneEdge` — common-neighbor edges append | MISSING | Hard-split with a direct edge whose endpoints share common neighbors in the zone graph | gamemd: after the direct edge append, iterates backward over `late_endpoint`'s adjacency; for each common neighbor of both `early` and `late` endpoints (a common-neighbor scan), appends `(early_endpoint, candidate)` to the exclusion vector (fn-pathfinder_invalidate_zone_edge.md §Step 5; key detail: only `early,candidate` is appended, not `late,candidate`) | Rust: no common-neighbor scan; `exclude_corridor_edges` only appends consecutive zone pairs from the path — no graph adjacency traversal | DRIFT: gamemd's common-neighbor appends prevent future corridor routes from threading through the blocking zone via any adjacent shortcut; Rust has no equivalent pruning. After a hard-split failure, Rust may retry corridors that still route through a common-neighbor zone that gamemd would have excluded. Fires whenever a hard-split zone has graph neighbors. | gamemd: fn-pathfinder_invalidate_zone_edge.md §Step 5 (common-neighbor scan, `DAT_0087F878`); Rust: zone_search.rs:698-708 (no graph neighbor scan) |
| `PathfinderClass__InvalidateZoneEdge` — hierarchy-valid flag clear (PathfinderClass+0x38) | MISSING | Stored zone path count < 2 OR current zone not found in stored path | gamemd: sets `PathfinderClass+0x38 = 0` (clears hierarchy-valid flag); caller `UpdateHierarchicalEdges` re-reads `+0x38` after this call; `AStar_pathfind_search` skips hierarchical mode if `+0x38 == 0` on next retry (fn-pathfinder_invalidate_zone_edge.md §Step 1–2, §Step 6 "Exclusion lifetime") | Rust: no hierarchy-valid flag; zone corridor mode is determined per-call from zone precheck; there is no persistent flag that `exclude_corridor_edges` or the retry loop can clear to disable hierarchy for subsequent retries | DRIFT: gamemd gracefully degrades from hierarchical to flat A* when zone path is unavailable; Rust continues running corridor-mode retries even when the zone structure has no useful stored path. Units facing unreachable destinations may behave differently — gamemd abandons hierarchy sooner, Rust exhausts all retries in corridor mode. | gamemd: fn-pathfinder_invalidate_zone_edge.md §Step 1 (`+0x38 = 0`), §Exclusion lifetime; Rust: zone_search.rs:428-464 (no hierarchy-valid flag) |
| `Path_walk_directions_to_cell` — prior-path endpoint pre-pass (Run_AStar caller) | MISSING | Any repath call where a prior 24-step direction buffer exists at FootClass+0x5E0 | gamemd: `FootClass__Run_AStar` calls `Path_walk_directions_to_cell` before each A* invocation to decode the existing direction buffer into the prior path's endpoint MapCoord (fn-path_walk_directions_to_cell.md §Callers); this endpoint is used as contextual start-node hint or waypoint for the new search | Rust: no pre-pass; prior path is discarded on repath; `find_move_path` starts A* directly from the unit's current cell with no direction-buffer decode step (movement_path.rs:199-329) | DRIFT: in gamemd, the prior path's computed endpoint feeds back into the new A* search (e.g. influences zone precheck start or node initialization); Rust always starts from a cold slate. Impact is most visible during partial-path repaths: gamemd may use the buffered endpoint to bias the new corridor; Rust cannot because the buffer is not decoded. Fires on every repath after the initial path. | gamemd: fn-path_walk_directions_to_cell.md §Callers (Run_AStar pre-pass); fn-run_astar.md; Rust: movement_path.rs:199-329 (no direction-buffer decode) |
| `Path_walk_directions_to_cell` — direction 0–7 step loop | MISSING | n_steps > 0 direction array of compass directions (0–7) | gamemd: iterates each direction entry; adds `g_DirectionOffsets[dir].dx/dy` (signed short, stride 4, base 0x0089F688) to packed MapCoord (low word = x, high word = y); produces destination cell after n_steps (fn-path_walk_directions_to_cell.md §Full decompile) | Rust: no `walk_directions_to_cell` function exists; the direction-delta table is accessed only inside `astar_search` neighbor expansion (core.rs NEIGHBORS array), not as a standalone utility | MISSING: no standalone function to replay a direction buffer to an endpoint cell. The NEIGHBORS constant in core.rs is structurally equivalent to `g_DirectionOffsets` (same 8-direction dx/dy deltas) but is not exposed as a replayable walk utility. | gamemd: fn-path_walk_directions_to_cell.md §Full decompile, §Globals (g_DirectionOffsets @ 0x0089F688); Rust: core.rs `NEIGHBORS` (not a separate walk function) |
| `Path_walk_directions_to_cell` — direction 8 tube jump | DRIFT | n_steps buffer containing direction value == 8 at current position | gamemd: calls `MapClass__Get_CellClass(&local_4)` → reads `CellClass+0x116` (short `tube_index`) → if `tube_index == -1` sets pos=(0,0); else `pos = g_TubeArray[tube_index]->field_0x28` (exit MapCoord) (fn-path_walk_directions_to_cell.md §Tube jump behavior) | Rust: direction-8 tube jumps in A* use `explicit_tube_edge` which calls `terrain.tube_at_cell(x,y)?.exit` directly (core.rs:621-630); no `tube_index == -1` → pos=(0,0) defensive fallback; zero-length auto-shell tubes are filtered by `tube.path_len() == 0` guard not by sentinel check | DRIFT: defensive fallback `tube_index == -1 → pos=(0,0)` is absent in Rust; in gamemd a missing-tube cell during direction-buffer replay silently resets position to MapCoord (0,0), causing the unit to appear at the map origin; Rust would not produce this fallback (the tube lookup returns `None` and the step is skipped). The direction-walk utility is itself MISSING so this only fires if/when the pre-pass is implemented. | gamemd: fn-path_walk_directions_to_cell.md §Tube jump behavior (tube_index == -1 fallback, g_TubeArray @ 0x008B413C); Rust: core.rs:621-630 `explicit_tube_edge` (no -1 sentinel) |
| `Path_walk_directions_to_cell` — early exit (n_steps < 1) | MATCH | n_steps == 0 | gamemd: if `param_3 < 1` immediately writes `*param_2` (start) to `*param_1` and returns; output = start unchanged (fn-path_walk_directions_to_cell.md §Early exit) | Rust: no direct equivalent; but the Rust path code never calls a direction-walk utility — when a path has zero steps remaining, the unit is considered at its goal and triggers a repath from current position (movement_path.rs step-advance logic) | INTERNAL-ONLY: the early-exit branch is a guard on the direction-walk utility itself, which is MISSING in Rust. The observable effect — unit stays at start when no steps are given — is reproduced by Rust's movement state machine reaching end-of-path. | gamemd: fn-path_walk_directions_to_cell.md §Early exit; Rust: no equivalent call site |
| `Zone_precheck` — slope cost contribution | DRIFT | Unit with non-zero slope speed factor; zone edge spanning a slope zone | gamemd: calls `FootClass__Get_Slope_Speed_Factor(param_5)`, fetches `FootClass+0x21C` (slope table ptr); if speed_factor > 1e-5, adds `Math__ftol(Zone_Estimate_Slope_Cost(slope_table, level, zone, neighbor))` to each candidate edge cost (fn-zone_precheck.md §Slope check initialization) | Rust: `zone_precheck_flat` has zero slope contribution — doc comment states "Slope contribution is zero in this slice"; `Zone_Estimate_Slope_Cost` is excluded from scope (fn-zone_precheck.md callee list); no `FootClass+0x21C` slope table is accessed (zone_hierarchy.rs:329-371) | DRIFT: zone-level path cost excludes slope for all units; on maps with significant slope zones, gamemd's zone precheck finds a different (higher-slope-avoiding) corridor than Rust's flat precheck. Units may be routed through steeper zone paths in Rust, visibly different from gamemd on ramp-heavy maps. Fires whenever a unit paths across a zone boundary with slope terrain. | gamemd: fn-zone_precheck.md §Slope check initialization, §Callees (Zone_Estimate_Slope_Cost @ 0x00585F40, FootClass__Get_Slope_Speed_Factor @ 0x004DC760); Rust: zone_hierarchy.rs:329-335 (slope omitted) |
| `Zone_precheck` — bridge-edge tiebreak penalty | DRIFT | Zone edge with `edge.byte4 != 0` (bridge/special edge flag) | gamemd: adds `0.001002` (float at `0x007E3818`) to candidate_cost when `edge.byte4 != 0`; this is a floating-point tiebreaker applied before converting to the zone node heap (fn-zone_precheck.md §Slope cost penalty) | Rust: adds `i32::from(edge.flag != 0)` = integer 1 when `edge.flag != 0` (zone_hierarchy.rs:439); bridge-flagged edges cost +1 over non-flagged edges | DRIFT: gamemd uses a fractional float penalty (0.001002) to tiebreak without affecting non-bridge route selection across most cost ranges; Rust uses a full integer +1 which can override non-bridge differences in ZONE_BASE_COSTS ranges. Any zone graph where a bridge-flagged edge has base_cost equal to an adjacent non-bridge edge will produce a different routing choice. Fires whenever src and dst span a bridge-flagged zone edge. | gamemd: fn-zone_precheck.md §Slope cost penalty (0x007E3818 = 0.001002); Rust: zone_hierarchy.rs:439 `edge_flag_cost = i32::from(edge.flag != 0)` |
| `Zone_precheck` — base cost table (float vs integer) | DRIFT | Any zone edge with non-zero cost class | gamemd: `edge_cost_base = DAT_007E3794[edge.cost_class]` — per-cost-class float array; exact values not read in this session (fn-zone_precheck.md §Decompilation analysis: "Float array: edge cost per cost_class") | Rust: `ZONE_BASE_COSTS: [i32; 8] = [1000, 0, 0, 1000, 1000, 0, 1000, 1000]` — integer table; values are: clear/beach/water/building=1000, crushable/wall/impassable/outside=0 (zone_hierarchy.rs:28-29) | DRIFT: gamemd's float cost table values at `DAT_007E3794` are unverified; the Rust integer table is an approximation. Any divergence in relative costs between cost classes changes which zone corridor is selected. Until `DAT_007E3794` is read in a live Ghidra session, this is unresolvable DRIFT — the scale and distribution differ from the Rust table. | gamemd: fn-zone_precheck.md §Globals (DAT_007E3794 @ 0x007E3794, YELLOW — actual values not read); Rust: zone_hierarchy.rs:28-29 `ZONE_BASE_COSTS` |
| `Zone_precheck` — passability gate | MATCH | SpeedType for unit attempting to cross a zone edge of a given cost class | gamemd: `(&g_PassabilityMatrix)[param_4 * 8 + edge.cost_class] == 1`; same 8×N passability matrix as cell-level A* (fn-zone_precheck.md §Passability gate) | Rust: `passability::is_passable_for_zone(record.zone_type, movement_zone)` looks up `PASSABILITY_MATRIX[zone_layer][zone_type] == 1`; matrix is the same 13×8 table with same structure (passability.rs:115-143, 184-190) | MATCH: both index the same passability matrix by movement zone and terrain type; Rust accurately reproduces the gamemd gate. The INTERNAL-ONLY difference is `SpeedType → zone_layer` mapping vs `MovementZone → zone_layer` — semantically equivalent since `zone_search.rs` maps SpeedType to MovementZone before calling precheck. | gamemd: fn-zone_precheck.md §Passability gate; Rust: passability.rs:115-143 `PASSABILITY_MATRIX`, zone_hierarchy.rs:433 `is_passable_for_zone` |
| `Zone_precheck` — secondary heap + zone chain storage | INTERNAL-ONLY | Any cross-zone path search | gamemd: uses secondary heap at `PathfinderClass+0x68` (same min-heap struct as primary, reset between levels); zone chain stored to `Pathfinder+0xBC + level*1000` (short array) and `Pathfinder+0xC74 + level*4` (chain length) (fn-zone_precheck.md §Heap used, §Zone chain recording) | Rust: uses `BinaryHeap<PrecheckQueueEntry>` allocated locally per `search_precheck_level` call; result returned as `ZonePrecheckResult.paths[level]: Vec<ZoneId>` (zone_hierarchy.rs:399-413, 287-291) | INTERNAL-ONLY: both implement a min-priority-queue zone-level A* with path backtracking; output (zone chain per level) is functionally identical. Storage mechanism differs (global singleton buffer vs stack-local Vec). Observable output — which zone corridor is selected — is the same given identical costs. | gamemd: fn-zone_precheck.md §Heap used, §Zone chain recording; Rust: zone_hierarchy.rs:399-413 BinaryHeap, 287-291 ZonePrecheckResult |
| `FootClass__Find_Nearby_Passable_Cell` — candidate pool + selection | DRIFT | Goal cell is blocked; unit must find nearest passable cell (move goal redirect) | gamemd: collects up to 24 candidates from expanding diamond rings (`abs(dx)+abs(dy)==r`); if `param_14 == null → g_CurrentFrameCounter % count` random pick; if `param_14` is real target → `Sqrt_Approx` Euclidean closest pick; separate direct vs indirect pools (fn-find_nearby_passable_cell.md §Step 4–5) | Rust `nearest_walkable_any_layer`: returns the **first** passable cell encountered in expanding Chebyshev square rings (`max(abs(dx),abs(dy))==r`); no candidate pool; no random/closest selection; always returns the lexicographically-first passable cell in the iteration order (core.rs:1519-1555) | DRIFT: (1) Chebyshev square ring vs diamond ring — at r≥1 Rust checks more cells per ring (16 at r=2 vs 8 for diamond). (2) No randomization — same blocked goal always redirects to the same cell; gamemd varies with frame counter. (3) No closest-to-target selection. Combined: redirected move targets differ visibly on any blocked goal. `miner_dock_sequence::find_nearby_passable_cell_with_index` implements the correct diamond ring + modulo pick but is used only for miner dock, not for general move-goal redirect. Fires on every blocked-destination move command. | gamemd: fn-find_nearby_passable_cell.md §Step 3–5; Rust: core.rs:1519-1555 `nearest_walkable_any_layer` (square ring, first-found); movement_path.rs:78-118 `nearest_move_goal` |
| `FootClass__Find_Nearby_Passable_Cell` — search radius | DRIFT | Any passable-cell search | gamemd: `search_radius = min(FootClass+0xF4 + FootClass+0xF8, 32)` = unit's cached Speed + SightRange, capped at 32 (fn-find_nearby_passable_cell.md §Step 2) | Rust `resolve_requested_move_goal`: calls `nearest_move_goal` with `max_radius` passed by the caller; no FootClass Speed/SightRange-derived radius; `max_radius` varies by call site (movement_path.rs:120-142) | DRIFT: gamemd's radius is per-unit (faster/more-sighted units search farther); Rust's radius is call-site-fixed, not per-unit attributes. Units with small Speed+SightRange may fail to find a redirect cell that gamemd would find. Fires on any blocked destination with a redirected goal search. | gamemd: fn-find_nearby_passable_cell.md §Step 2; Rust: movement_path.rs:120-142 `resolve_requested_move_goal` (caller-supplied max_radius) |
| `FootClass__Find_Nearby_Passable_Cell` — passability filters (height/occupancy/bridge) | DRIFT | Multi-parameter passability checks: height match, occupant check, bridge cell rejection | gamemd: 6 sequential candidate filters: on-screen, `CellRect__CheckPassability`, height match (`|cand_height - origin_height| < 2`, if param_11), occupant check (if param_12), bridge cell rejection (if param_13==0), occupancy rect (if param_16) (fn-find_nearby_passable_cell.md §Step 3) | Rust `nearest_walkable_any_layer`: single check `self.is_any_layer_walkable(cx, cy)` — walkability only, no height match, no occupant check, no bridge rejection, no on-screen test; all 4 conditional filters are absent (core.rs:1519-1555) | DRIFT: missing occupant and height checks cause Rust to redirect move goals into occupied or height-mismatched cells that gamemd would skip. Units may be sent to cells another unit is already standing in. Height-mismatch rejection affects bridge approaches. Bridge-rejection param (param_13) is absent entirely. Fires on any blocked-destination redirect where cell occupancy or height differs from origin. | gamemd: fn-find_nearby_passable_cell.md §Step 3 (filters 1–6); Rust: core.rs:1527-1529 `is_any_layer_walkable` (no height/occupant/bridge filters) |
| `FootClass__Find_Nearby_Passable_Cell` — origin height and bridge height adjustment | DRIFT | Origin cell is a bridge structural cell with `CellClass+0x140 & 0x100` set | gamemd: reads `CellClass+0x11B` (signed byte) as origin height; if bridge-aware mode AND bridge structural flag, raises height by +4 to prevent false mismatches (fn-find_nearby_passable_cell.md §Step 1) | Rust: no origin height tracking; `nearest_walkable_any_layer` is purely walkability-based with no height field; bridge-structural flag never read in redirect path (core.rs:1519-1555, movement_path.rs:78-118) | DRIFT: on bridge cells, gamemd's +4 height adjustment prevents false "height mismatch" rejections that would exclude adjacent bridge deck cells; Rust has no height logic at all, so bridge-adjacent cell selection differs from gamemd on any ramp/bridge map. Fires on any redirect from a bridge origin cell. | gamemd: fn-find_nearby_passable_cell.md §Step 1 (CellClass+0x11B, CellClass+0x140 & 0x100); Rust: no equivalent |
| `Pathfinding_update_continued` (MapCoord_StepByDir_GetCell) — direction step (0–7) | MATCH | Any caller stepping a cell coord one step in direction 0–7 via `g_DirectionOffsets` | gamemd: reads `+0x24` packed coord (low=x, high=y), adds `g_DirectionOffsets[dir]` dx/dy deltas, calls `MapClass__Get_CellClass` on result (fn-pathfinding_update_continued.md §Direction stepping) | Rust: `direction_delta(dir)` in `util/direction.rs:77-79` returns `(dx, dy)` from same 8-direction table; `step_coord_by_direction` applies the delta and bounds-checks (resolved_terrain.rs:302-316) | MATCH: both use the same 8-direction table (N/NE/E/SE/S/SW/W/NW with identical (dx,dy) values per fn-pathfinding_update_continued.md §Globals and direction.rs:103-120 test fixtures). CellClass* return is INTERNAL-ONLY — Rust callers access cell data through terrain grid, not a returned pointer. | gamemd: fn-pathfinding_update_continued.md §Direction stepping (g_DirectionOffsets @ 0x0089F688); Rust: direction.rs:77-79 `direction_delta`, resolved_terrain.rs:302-316 `step_coord_by_direction` |
| `Pathfinding_update_continued` — direction 8 no-op guard | DRIFT | Direction value == 8 passed to `Pathfinding_update_continued` | gamemd: guard `if (param_2 < 8)` — direction 8 causes the function to return immediately; stored cell coord at `+0x24` is NOT modified and `MapClass__Get_CellClass` is NOT called (fn-pathfinding_update_continued.md §Behavioral analysis) | Rust: `step_coord_by_direction` treats direction 8 as a tube jump (`is_tube_step_direction(8) == true`); returns `tube.exit` or `(0,0)` (resolved_terrain.rs:303-308) — the coord IS changed for direction 8 | DRIFT: gamemd's `Pathfinding_update_continued` is a no-op for direction 8; Rust's `step_coord_by_direction` jumps to tube exit. Callers that use `Pathfinding_update_continued` for zone/map flood-fill stepping (e.g. `ZoneMap__FloodFillReachableZones`) may encounter different behavior at tube-connected cells during zone partitioning. However, `Path_walk_directions_to_cell` (direction-buffer replay) does handle dir==8 as a tube jump in gamemd via a different code path. | gamemd: fn-pathfinding_update_continued.md §Behavioral analysis (guard `< 8`); Rust: resolved_terrain.rs:303-308 `is_tube_step_direction` |
| `MinHeap__SiftDown` — heap sift-down algorithm | INTERNAL-ONLY | Zone-level A* heap-pop in `Zone_precheck` | gamemd: custom 1-based array binary min-heap; sift-down compares float f-cost at `node+0x08`; tie-break: left child wins on `<=`, right child wins only on strict `<` (fn-minheap_siftdown.md §Algorithm, §Comparison operators) | Rust: `std::collections::BinaryHeap<PrecheckQueueEntry>` — standard max-heap via `Ord` reversed (min-heap by cost inversion with secondary tie-break by `sequence`); no manual sift-down; `BinaryHeap::pop()` / `push()` maintain heap internally (zone_hierarchy.rs:308-327, 399-453) | INTERNAL-ONLY: both implement a priority queue for zone-level A*; the same set of nodes is expanded in priority order; observable output (zone corridor selected) is determined by cost values not by heap implementation details. Tie-break semantics differ (gamemd left-child preference vs Rust sequence counter) but this is already covered by the Zone_precheck bridge-edge-penalty DRIFT row which changes costs before they reach the heap. | gamemd: fn-minheap_siftdown.md §Algorithm; Rust: zone_hierarchy.rs:308-327 `PrecheckQueueEntry` ordering, 399-453 `search_precheck_level` BinaryHeap |
| `PathfinderClass__EstimateZoneCost` — zone chain base estimate formula | DRIFT | Src and dst in different zones, Zone_precheck finds a zone chain of length N | gamemd: `estimate = Pathfinder+0xC74 * 2 - 2` = `zone_chain_len * 2 - 2`; same-zone path (len=1) → estimate=0; cross-zone path (len=2) → estimate=2; each additional hop adds 2 (fn-fn_42d170.md §Step 6) | Rust `zone_cost_estimate`: `chebyshev(start, start_center)` + sum of `chebyshev(from_center, to_center)` per corridor hop + `chebyshev(goal_center, goal)` — Chebyshev distances between zone centers, not a fixed-per-hop formula (zone_search.rs:769-779) | DRIFT: gamemd's formula uses a fixed +2 per zone-chain hop regardless of zone size or actual distance; Rust uses actual Chebyshev distances between zone centroids. On maps where zones have varying sizes or where start/goal are far from their zone centers, the estimate differs substantially. Affects whether alternate blocked-destination candidates are accepted or rejected. Fires on every blocked-goal path request that calls this estimate. | gamemd: fn-fn_42d170.md §Step 6 (`Pathfinder+0xC74 * 2 - 2`); Rust: zone_search.rs:769-779 `zone_cost_estimate` (centroid-based Chebyshev sum) |
| `PathfinderClass__EstimateZoneCost` — bridge endpoint adjustment | MISSING | Src or dst endpoint is on a bridge cell (`from_bridge_flag` or `to_bridge_flag` nonzero) | gamemd: adds Chebyshev from original endpoint to bridge-adjacent zone-chain entry (via `FUN_00583820`); same-bridge-record short-circuit returns `direct_chebyshev` immediately (fn-fn_42d170.md §Step 7–8) | Rust `zone_cost_estimate`: no bridge endpoint adjustment; no `FindBridgeRecord` short-circuit; endpoints are used directly without bridge-aware coordinate resolution (zone_search.rs:724-779) | MISSING: on bridge-adjacent move requests, gamemd's estimate accounts for the approach distance to the bridge entry cell; Rust ignores this. The same-bridge short-circuit means gamemd may return a very small estimate (just direct Chebyshev) while Rust computes a full zone-chain estimate — changing whether an alternate candidate is accepted. Fires on any path request where src or dst is on or adjacent to a bridge. | gamemd: fn-fn_42d170.md §Step 7–8 (`FUN_00583820`, `MapClass__FindBridgeRecord`); Rust: zone_search.rs:724-779 (no bridge endpoint logic) |
| `PathfinderClass__EstimateZoneCost` — alternate acceptance threshold | MATCH | Caller checks `estimate <= chebyshev(original, alternate) + 6` | gamemd: `FootClass__Find_Path` uses this exact threshold to accept FNPC alternate (fn-fn_42d170.md §Callers: "Accept FNPC alternate if result `<= cheb(original, alternate) + 6`") | Rust: `accepts_blocked_destination_alternate` returns `helper_result != i32::MAX && helper_result <= chebyshev(original, alternate) + BLOCKED_DESTINATION_ALTERNATE_MARGIN` where `BLOCKED_DESTINATION_ALTERNATE_MARGIN = 6` (zone_search.rs:41, 783-790) | MATCH: the +6 margin for blocked-destination alternate acceptance is correctly replicated. Note: estimate formula (above row) is DRIFT, so the comparison may produce different accept/reject outcomes even though the threshold is identical. | gamemd: fn-fn_42d170.md §Callers (threshold +6); Rust: zone_search.rs:41 `BLOCKED_DESTINATION_ALTERNATE_MARGIN = 6`, zone_search.rs:783-790 `accepts_blocked_destination_alternate` |
| `PathfinderHeapVec__Clear` (FUN_0042D540) — exclusion-vector clear | INTERNAL-ONLY | PathfinderClass initialization; per-search exclusion vector reset | gamemd: zeros count field (`+0x10`), conditionally frees heap buffer (`+0x04`) if ownership flag (`+0x0D`) is set, then zeros capacity (`+0x08`); custom heap-vec clear for per-level exclusion list storage (fn-fn_42d540.md §Clear sequence) | Rust: `ZonePrecheckExclusions::default()` creates fresh per-search exclusion state as stack-local struct with `BTreeSet<ZoneEdgeKey>` and `Vec<ZoneEdgeKey>` per level; per-search reset is implicit by constructing fresh exclusions each call, not by explicitly clearing buffers (zone_hierarchy.rs:244-248, zone_search.rs:287-293) | INTERNAL-ONLY: both clear/reset the zone exclusion list before each search; Rust uses idiomatic Rust ownership (fresh struct per search) vs gamemd's manual heap-buffer management. No observable behavioral difference for the exclusion mechanism itself — differences in the exclusions actually appended are covered by the `InvalidateZoneEdge` rows. | gamemd: fn-fn_42d540.md §Clear sequence; Rust: zone_hierarchy.rs:244-248 `ZonePrecheckExclusions`, zone_search.rs:287-293 `ZonePrecheckExclusions::default()` |
| `U16Vec__Constructor` (FUN_0042DD60) — per-level temp vector init | INTERNAL-ONLY | `UpdateHierarchicalEdges` initializes a temp u16 vector with capacity 10 to collect flood-fill neighbor zone IDs | gamemd: constructs a heap-allocated `u16[]` vector struct (vtable + data_ptr + capacity + owns-flag) via `operator_new(capacity * 2)` (fn-fn_42dd60.md §Behavioral analysis); used as a temporary storage for the 10-capacity neighbor zone ID list during flood-fill | Rust: `UpdateHierarchicalEdges` equivalent (`exclude_corridor_edges` in zone_search.rs) uses `BTreeSet<ZoneEdge>` and `Vec<ZoneId>` from the Rust standard library — no custom vector type; `Vec::new()` or `ZonePrecheckExclusions::default()` replaces the custom allocator | INTERNAL-ONLY: both allocate temporary per-search zone storage; memory model differs (custom heap-vec vs standard Rust Vec) but the behavioral role — collecting zone IDs for exclusion list building — is the same. Observable output differences are in `UpdateHierarchicalEdges` rows (flood-fill vs no-flood-fill), not in the vector allocation mechanism. | gamemd: fn-fn_42dd60.md §Behavioral analysis; Rust: zone_search.rs `exclude_corridor_edges`, zone_hierarchy.rs `ZonePrecheckExclusions` |
| `PathfinderHeapVec__Push` (FUN_0042D830) — exclusion-edge append | INTERNAL-ONLY | `InvalidateZoneEdge` appends a packed zone-edge key to the per-level exclusion vector | gamemd: plain append with no duplicate suppression; grows capacity via virtual if full; returns 1 on success, 0 on failure (fn-fn_42d830.md §Append path, §No duplicate suppression) | Rust: `ZonePrecheckExclusions::append_producer_edge` uses `BTreeSet<ZoneEdgeKey>` (dedup) + `Vec<ZoneEdgeKey>` (ordered); appends to both; duplicates are silently dropped by the set (zone_hierarchy.rs:261-272) | DRIFT: gamemd's vector allows duplicate entries (same edge appended twice appears twice); Rust silently deduplicates via `BTreeSet::insert`. When `InvalidateZoneEdge` inlines a common-neighbor append after the direct-edge append, if the same edge appears as both direct and common-neighbor, gamemd stores it twice while Rust stores it once. This could affect the Zone_precheck exclusion check (`exclusions.contains`) — but since both gamemd and Rust check "is this edge excluded?" and both would find it in either case, the DRIFT is effectively INTERNAL-ONLY for the exclusion mechanism. Only relevant if duplicate entries affect the ordered iteration in `fn-pathfinder_update_hier_edges.md §Zero-return exclusion append` — the ordered list output used by UpdateHierarchicalEdges. | gamemd: fn-fn_42d830.md §No duplicate suppression; Rust: zone_hierarchy.rs:261-272 `append_producer_edge` (BTreeSet dedup) |
| `PathfinderHeapVec__Init` (FUN_0042DC50) — exclusion/heap vector initialization | INTERNAL-ONLY | `PathfinderClass__Constructor` initializes per-level exclusion and heap-vec structs | gamemd: `PathfinderHeapVec__Init` sets vtable pointer (`PTR_FUN_007e37ec`), writes capacity/ownership/init_flag; allocates `capacity × 4` bytes via `operator_new` when no external buffer is provided; ownership_flag=1 gates the later `PathfinderHeapVec__Clear` conditional free (fn-fn_42dc50.md §Initialization paths, §Decompilation, verified via `decompile_function 0x0042DC50`) | Rust: no `PathfinderHeapVec` struct; `PathfindWorkspace` fields (`ground_g`, `bridge_g`, `ground_from`, `bridge_from`, `ground_closed`, `bridge_closed`) are standard `Vec<T>` initialized via `Default::default()` — zero allocation at construction; capacity grows lazily on first `reset(total_cells)` call via `Vec::resize` (core.rs:44-68) | INTERNAL-ONLY: both produce an empty, ready-to-use storage container before pathfinding begins. The C++ vtable + manual ownership machinery in gamemd is replaced by Rust's `Vec<T>` RAII ownership. No observable behavioral difference — both sides start each search with cleared/sentinel-filled buffers. The 3-mode init distinction (zero-cap, external buffer, internal alloc) has no Rust equivalent because `Vec::resize` handles reuse transparently. | gamemd: fn-fn_42dc50.md §Initialization paths, §Struct Field Accesses (`decompile_function 0x0042DC50`); Rust: core.rs:44-68 `PathfindWorkspace` fields + `Default::default()` |
| `PathfinderClass__FindNearbyBridgePeer` (FUN_0042B080) — 5×5 bridge peer scan | MISSING | `UpdateBridgePassability` needs to find a bridge-occupying unit when the probe cell's object list is null | gamemd: scans 25 cells in ±2 dx/dy square around center; selects ground/bridge object list per cell based on `abs(cell.level - requested_height) < 3`; walks linked list, gates by `obj+0x14 bit 2`, calls subobject vtable `+0xA0` acceptance predicate with center leptons; returns first matching object (fn-fn_42b080.md §Full algorithm) | Rust: `UpdateBridgePassability` is entirely absent in Rust (covered by compare-pathfinder_update_bridge_pass MISSING row); therefore this 5×5 peer-scan helper has no Rust equivalent | MISSING: the entire bridge passability XOR-marker system is absent in Rust. This helper is a leaf node of that missing system; its absence does not need a separate fix beyond implementing `UpdateBridgePassability` and its `SearchMarkerOverlay` population. Impact scope is the same as the parent MISSING row. | gamemd: fn-fn_42b080.md §Summary; Rust: no equivalent (UpdateBridgePassability system entirely absent) |
| `Path_smooth_single_segment` — midpoint direction formula | DRIFT | Path segment with diagonal endpoints e.g. NW(7) and NE(1) | gamemd: `midpoint = (dir_a + dir_b) >> 1` (arithmetic right-shift floor division); NW(7)+NE(1) → `(7+1)>>1 = 4` (South) (fn-path_smooth_single_segment.md §Step 1) | Rust `midpoint_dir(a,b)`: if `hi-lo==2` → `lo+1`; else `(hi+1)%8`; NW(7)+NE(1) → lo=1, hi=7, hi-lo=6≠2 → `(7+1)%8 = 0` (North) (path_smooth.rs:60-72) | DRIFT: for endpoint-direction pairs that differ by more than 2 on the wheel (wrap-around cases), gamemd and Rust compute opposite midpoint directions. The path smoothing produces a step in the wrong compass direction on wrap-around segments. Fires on any path that includes an NW→NE (or NE→NW, SW→SE, etc.) wrap-around corner pair during smoothing. | gamemd: fn-path_smooth_single_segment.md §Step 1 (`(dir_a + dir_b) >> 1`); Rust: path_smooth.rs:60-72 `midpoint_dir` |
| `Path_smooth_single_segment` — slope cost rejection | DRIFT | Candidate smoothed cell is steep (slope_cost × slope_speed_factor >= 1.0) | gamemd: per-step check `MapClass__Get_Slope_Cost_At_Cell * slope_speed_factor >= 1.0` → aborts smoothing for this segment if true; result: steep diagonal cells are never introduced by smoothing (fn-path_smooth_single_segment.md §Step 4) | Rust `smooth_path`: no slope cost check; walkability is the only per-cell filter (`walkable(sx, sy)` boolean); steep cells that are walkable are freely substituted as smoothed diagonals (path_smooth.rs:122-125) | DRIFT: Rust may smooth a path through steep terrain that gamemd would leave unsmoothed, producing a shorter but steeper diagonal path. Visible as unit taking an unexpected steep route instead of the original corner path. Fires on any path through hilly/ramp terrain that contains smoothable segments. | gamemd: fn-path_smooth_single_segment.md §Step 4 (slope threshold 1.0); Rust: path_smooth.rs:122-125 `walkable(sx, sy)` (no slope check) |
| `Path_smooth_single_segment` — A* cost marker rejection | DRIFT | Candidate smoothed cell has `CellClass+0x140 & 0x40000` set (bridge-approach marker) | gamemd: per-step check `CellClass+0x140 & 0x40000 != 0` → aborts smoothing; prevents smoothing into bridge-approach cells flagged by `UpdateBridgePassability` (fn-path_smooth_single_segment.md §Step 4) | Rust `smooth_path`: no A* cost marker check; bridge-approach cells (if walkable) are freely substituted as smoothed diagonals; Rust does not read `CellClass+0x140` at all in the smoothing loop (path_smooth.rs:86-162) | DRIFT: Rust may smooth paths into bridge-approach cells that gamemd protects from smoothing. On maps with bridges, smoothed paths may diagonally cut into bridge ramps in ways gamemd prevents. Fires on any map with a bridge when the smoothed diagonal intersects a bridge-approach cell. | gamemd: fn-path_smooth_single_segment.md §Step 4 (`CellClass+0x140 & 0x40000`); Rust: path_smooth.rs:86-162 (no cost-marker check) |
| `Path_smooth_single_segment` — tube direction bypass | INTERNAL-ONLY | Path segment contains direction 8 (tunnel transition) | gamemd: explicit guard `if (dir_a == 8 \|\| dir_b == 8)` — walks all `param_4 + param_5` steps without smoothing; returns `param_4 + param_5` (fn-path_smooth_single_segment.md §Step 2) | Rust `smooth_path`: operates on cell-coordinate pairs, not direction indices; direction 8 tube transitions never appear as input (tube connectivity is modeled as a cell-list edge in the Rust path representation, not as direction index 8) (path_smooth.rs:86-162) | INTERNAL-ONLY: both sides skip smoothing across tube transitions. Rust's approach changes how tubes appear in the path representation (as a cell jump rather than direction 8), but the observable result — no diagonal inserted across a tube — is the same. No path-output difference. | gamemd: fn-path_smooth_single_segment.md §Step 2 (direction 8 guard); Rust: path_smooth.rs (cell-list representation, no direction index 8) |
| `Path_Find_Split_Anchor` — backward-scan valley-then-peak inflection detection | MISSING | `Path_optimize_straight_segments` fails to reroute a segment in one pass and needs a natural subdivision point | gamemd: scans direction array backwards from high-index to low-index; accumulates Chebyshev displacement; detects first valley-then-new-peak inflection point (where distance first decreases then exceeds prior max); returns that index and coordinate as the split anchor for recursive subdivision (fn-path_find_split_anchor.md §Full algorithm) | Rust `optimize_path` / `find_drift_segment` / `reroute_segment`: drift detection via cross-product magnitude (perpendicular deviation); when reroute fails, `seg_start += 1` — no recursive subdivision, no split-anchor search; `find_drift_segment` and `reroute_segment` have no analog for inflection-based splitting (path_smooth.rs:271-294, 352-398, 402-494) | MISSING: Rust has no recursive segment subdivision via inflection points. On complex paths where a single straight-line reroute fails, gamemd subdivides and recurses; Rust simply advances by one step. The visible difference: gamemd may successfully optimize a segment that Rust leaves as-is after one failed reroute attempt. Fires on any path with a segment too curved for a single reroute pass (relatively uncommon in open terrain, more likely in constrained corridors). | gamemd: fn-path_find_split_anchor.md §Full algorithm; Rust: path_smooth.rs:271-294 `find_drift_segment`, 402-494 `reroute_segment` (no split-anchor, no recursion) |
| `Path_Reroute_Straight_Line` — per-cell passability check (vtable+0x1AC + cost marker) | DRIFT | Candidate straight-line cell is impassable for the unit's movement type, or has `CellClass+0x140 & 0x40000` set | gamemd: per-cell check via `FootClass vtable+0x1AC` (direction+height-aware virtual call) AND `CellClass+0x140 & 0x40000` A* cost marker check → reject cell if either fails (fn-path_reroute_straight_line.md §Step 3) | Rust `reroute_segment`: per-cell check is `walkable(nx, ny)` boolean closure (no direction, no height, no cost marker); diagonal corner-cutting check `walkable(cx+ddx, cy)` and `walkable(cx, cy+ddy)` is present but no `CellClass+0x140` check (path_smooth.rs:472-481) | DRIFT: Rust allows rerouting into cells that gamemd's vtable+0x1AC would reject (direction-specific or height-specific impassability) and into bridge-approach cells flagged with `0x40000`. A rerouted segment may traverse cells gamemd would have blocked. Fires on any path through direction-restricted terrain or near bridges during optimize pass. | gamemd: fn-path_reroute_straight_line.md §Step 3; Rust: path_smooth.rs:472-481 `walkable(nx,ny)` (no vtable call, no cost marker) |
| `Path_Reroute_Straight_Line` — slope strictness flag and steep-cell tolerance | DRIFT | Candidate straight-line path crosses steep cells (slope_cost × slope_speed_factor >= threshold) | gamemd: counts steep cells per candidate walk; `param_7 == 0` (strict): aborts if any steep cell; `param_7 != 0` (lenient): aborts only if `>= 4` steep cells; slope check gated on `slope_speed_factor > _DAT_007e3810` threshold (fn-path_reroute_straight_line.md §Step 2–4) | Rust `reroute_segment`: no slope check, no strictness flag, no steep-cell counter; only walkability and diagonal corner-cutting are checked (path_smooth.rs:460-481) | DRIFT: Rust will accept a rerouted straight segment through steep terrain that gamemd (in strict mode) would reject after the first steep cell. Units rerouted by Rust's optimize pass may climb slopes that gamemd would route around. Fires on any rerouted segment through hilly terrain. | gamemd: fn-path_reroute_straight_line.md §Step 2–4 (param_7 strictness, steep counter, slope threshold); Rust: path_smooth.rs:460-481 (no slope check) |
| `Path_Reroute_Straight_Line` — retry with swapped direction order | DRIFT | First direction ordering (diagonal-first) fails passability checks | gamemd: on first-pass failure, swaps diagonal direction ↔ straight direction and their step counts, then retries the validation loop; returns success if second ordering passes (fn-path_reroute_straight_line.md §Step 5) | Rust `reroute_segment`: no retry; returns `None` immediately when any cell fails the walkability check; the Bresenham-style interleaving in the step loop is fixed and non-retrying (path_smooth.rs:446-491) | DRIFT: gamemd may find a valid reroute via the straight-first ordering when Rust gives up after the diagonal-first failure. Some rerouted segments that gamemd accepts will appear as non-optimized in Rust, leaving a slightly more curved path. Fires when the diagonal-first ordering hits an obstacle but the straight-first ordering would clear it. Relatively uncommon but fires in corridor-adjacent terrain. | gamemd: fn-path_reroute_straight_line.md §Step 5 (retry with swapped pair); Rust: path_smooth.rs:446-491 `reroute_segment` (no retry) |
| `MapClass__Get_Slope_Cost_At_Cell` — 2D slope cost table lookup | MISSING | Path-smoothing functions (`Path_smooth_single_segment`, `Path_Reroute_Straight_Line`) check if a candidate cell is too steep to traverse | gamemd: reads 2 signed 16-bit slope components from `CellClass+0x24` (packed low/high words); applies signed floor-divide by 4 to each to get table indices; looks up `int` cost from 2D table at `param_2+0x59F0`, stride 0x82; returns slope cost used by callers as `slope_cost * slope_speed_factor >= 1.0` rejection test (fn-mapclass_get_slope_cost_at_cell.md §Full algorithm) | Rust path smoothing (`path_smooth.rs`): no slope cost function; `smooth_path` / `reroute_segment` use a boolean `walkable` closure only; no `CellClass+0x24` slope-component read; no 2D table lookup anywhere in `path_smooth.rs`; A* core uses `CLIFF_COST_MULTIPLIER=4` for height transitions during search, not during smoothing (path_smooth.rs:86-494, core.rs:116) | MISSING: the entire per-cell slope cost infrastructure used during path smoothing is absent in Rust. Callers of `MapClass__Get_Slope_Cost_At_Cell` in gamemd (`Path_smooth_single_segment`, `Path_Reroute_Straight_Line`) each have a DRIFT row above noting the missing slope check. This row records the absence of the underlying function. No Rust equivalent of the `CellClass+0x24` slope field or 2D cost table has been identified. | gamemd: fn-mapclass_get_slope_cost_at_cell.md §Full algorithm (`CellClass+0x24`, table at `+0x59F0`); Rust: path_smooth.rs (no slope lookup), core.rs:116 `CLIFF_COST_MULTIPLIER` (different mechanism, A* only) |
| `PathfinderClass` struct — per-unit vs thread-local allocation | INTERNAL-ONLY | Any pathfinding call | gamemd: `PathfinderClass` (~0xC80 bytes) is allocated once per `FootClass` unit (embedded field or heap ptr); each unit has its own closed/open arrays and state; `epoch` field at `+0x28` enables amortized per-search reset without full array clear (struct-pathfinder_class.md §Field Table: `+0x0C` 0x180004-byte bridge closed list, `+0x10` 0x100004-byte ground closed list, `+0x28` epoch counter) | Rust: single thread-local `PathfindWorkspace` (PATHFIND_WORKSPACE) shared across all units; no per-unit allocation; `reset(total_cells)` does a full `Vec::clear() + Vec::resize()` per search call — no epoch amortization; `BinaryHeap::clear()` for open set (core.rs:44-95) | INTERNAL-ONLY: both provide cleared search buffers at the start of each pathfinding call. The per-unit vs shared distinction affects only the implementation model, not the path computed. The epoch optimization in gamemd is an amortization detail; the observable contract (fresh search state per call) is identical. No path-output difference. | gamemd: struct-pathfinder_class.md §Field Table `+0x28` epoch, `+0x0C`/`+0x10` closed lists; Rust: core.rs:44-95 `PathfindWorkspace` thread-local |
| `PathfinderClass` struct — zone path arrays and hierarchy_valid flag | INTERNAL-ONLY | Cross-zone path requests; `InvalidateZoneEdge` / `UpdateHierarchicalEdges` writes | gamemd: `hierarchy_valid` flag at `+0x38`; zone path arrays `zone_path[level]` at `+0xBC + level*1000` (500 ushorts per level × 3 levels); zone path lengths at `+0xC74 + level*4`; all persisted in struct between calls within a search episode (struct-pathfinder_class.md §Field Table `+0x38`, `+0xBC`, `+0xC74`) | Rust: `zone_precheck_flat` returns `ZonePrecheckResult { paths: [Vec<ZoneId>; 3] }` — zone paths are stack-allocated and returned per call, not stored in the workspace; `hierarchy_valid` has no persistent flag field; the equivalent is whether `zone_precheck_flat` returned `Some(ZonePrecheckResult)` vs `None` (zone_hierarchy.rs:287-291 `ZonePrecheckResult`, core.rs:44-68 `PathfindWorkspace`) | INTERNAL-ONLY: both record which zone corridor was selected and whether it is valid. Rust returns this per-call vs gamemd persisting it in the struct. The observable outcome — which zone corridor guides the A* search — is identical given the same zone costs. Zone-state persistence differences matter only if gamemd reuses a cached zone result across ticks without recomputing; that case is covered by the `InvalidateZoneEdge` / `UpdateHierarchicalEdges` rows which already capture the DRIFT in edge invalidation timing. | gamemd: struct-pathfinder_class.md `+0x38` hierarchy_valid, `+0xBC` zone_path, `+0xC74` zone_path_count; Rust: zone_hierarchy.rs:287-291 `ZonePrecheckResult`, core.rs:44-68 `PathfindWorkspace` |
