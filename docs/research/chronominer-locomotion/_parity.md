# chronominer-locomotion — Parity Report

Rust-comparer appends one row per per-symbol decode finding. Format:

| Symbol | Verdict | Input | gamemd output | Rust output | Visibility | Citations |
|--------|---------|-------|---------------|-------------|------------|-----------|

Default verdict for any formula/mechanism/ordering/field difference is **DRIFT**
unless positive concrete evidence of observable equivalence is cited inline.
Per CLAUDE.md parity bar and decode-system HARD-GATE.

<!-- ROWS BEGIN -->

<!-- compare-fn-constructor (task #35) -->
| Constructor / struct size and COM vtables | DRIFT | Any TeleportLocomotionClass instantiation | gamemd: allocates 76-byte COM object with 3 vtable pointers (IUnknown +0x00, ILocomotion +0x04, IPiggyback +0x18); COM interface plumbing is the identity of the locomotor instance | Rust: no TeleportLocomotionClass struct; locomotor state is held in `entity.teleport_state: Option<TeleportState>` and `entity.locomotor: Option<LocomotorState>` — no COM identity, no vtable pointers | Any system that holds a TeleportLocomotionClass* (e.g., piggyback queries via IPiggyback vtable) cannot be exercised; COM QI fails entirely | gamemd @ 0x00718000 per fn-constructor.md; Rust @ src/sim/movement/teleport_movement.rs:48 (TeleportState struct, no COM) |
| Constructor / dual destination-cache initialization | DRIFT | Any TeleportLocomotionClass instantiation | gamemd: initializes two separate 3-field (X,Y,Z) destination caches at +0x1c..+0x24 and +0x28..+0x30 to the g_NullCoord sentinel (0,0,0); Process reads both | Rust: `TeleportState` has only `target_rx: u16, target_ry: u16` — single 2D destination, no sentinel triple, no Z | Sentinel-based "no destination" detection and two-cache lookup logic in Process are entirely absent; warp commands that should be rejected (no valid dest) are not detectable | gamemd @ 0x00718000 param_1[7..12] init per fn-constructor.md:27-32; Rust @ src/sim/movement/teleport_movement.rs:48-55 (TeleportState, no dual cache) |
| Constructor / frame counter snapshot at construction | DRIFT | Any TeleportLocomotionClass instantiation | gamemd: captures `g_CurrentFrameCounter` into `TeleportLocomotionClass+0x3c` at construction time; this seeds the frame-stamp-based timer used by TimerCheck | Rust: no frame counter captured at issue_teleport_command time; being_warped_ticks is a raw decrement counter initialized at warp issue, not at locomotor construction | Timer drift compounds over the lifetime of the locomotor object when game pauses or tick rates vary | gamemd @ 0x00718000 `param_1[0xf] = g_CurrentFrameCounter` per fn-constructor.md:39; Rust: no frame snapshot in issue_teleport_command or TeleportState |
| Constructor / flag bytes at +0x35 and +0x36 | DRIFT | Any TeleportLocomotionClass instantiation | gamemd: zeroes two flag bytes at TeleportLocomotionClass+0x35 and +0x36 at construction | Rust: no equivalent flag bytes in TeleportState or LocomotorState | Any behavior gated on these flags is absent; flags are referenced in state transitions (YELLOW — not yet decoded in remaining tasks) | gamemd @ 0x00718000 per fn-constructor.md:34-35; Rust: no +0x35/+0x36 equivalent |

<!-- compare-fn-process (task #36) -->
| Process / mission gating (MISSION_ENTER=0xf) | DRIFT | Unit with teleport locomotor not in MISSION_ENTER | gamemd: Process dispatches to warp-dock path (LAB_00718ce9) only when TechnoClass mission == 0xf (MISSION_ENTER); all other missions use the drive path or no-op | Rust: `tick_teleport_movement` processes all entities with `teleport_state != None` unconditionally, regardless of mission state | Unit can warp while in the wrong mission state (e.g., ATTACK, GUARD) — player sees unprompted teleports during combat | gamemd @ 0x00718b70 branch at `iVar6 != 0xf` per fn-process.md:86; Rust @ src/sim/movement/teleport_movement.rs:153 (no mission check) |
| Process / radio-link gate (states 1, 2, 6) for dock commit | DRIFT | Unit in MISSION_ENTER approaching a building dock | gamemd: before committing warp to destination, reads TechnoClass+0x5a4 (radio-linked building), checks radio state 1/2/6 and cell-match against building NW cell; sets dock-commit flag | Rust: no radio-link check; teleport destination committed unconditionally | Unit teleports into a building dock without confirming the radio link is active — mis-warp into occupied docks, broken harvester dock sequencing | gamemd @ 0x00718b70 LAB_00718ce9 per fn-process.md:132-157; Rust: no radio-link handling in teleport_movement.rs |
| Process / infantry scatter on warp dispatch | DRIFT | Infantry unit in MISSION_ENTER | gamemd: if unit IsInfantry() (vtable+0x200) and in MISSION_ENTER, scatters infantry at destination (vtable+0x1ec = Scatter()) before placement | Rust: no infantry scatter call | Infantry at destination cell are not scattered before warp arrival — unit spawns on top of existing infantry, breaking occupancy and pathing | gamemd @ 0x00718b70 IsInfantry/Scatter per fn-process.md:121-124; Rust: no scatter call |
| Process / Find_Nearby_Passable_Cell fallback | DRIFT | Destination cell occupied or impassable | gamemd: if primary destination cell is blocked (occupancy check at vtable+0x1ac), calls FootClass__Find_Nearby_Passable_Cell (0x0056dc20) and retries CellClass__PlaceInfantryInCell | Rust: no passable-cell fallback; if target cell is occupied the unit warps on top of occupants | Unit warps into occupied cells with no fallback — visual overlap, broken occupancy grid, pathing corruption | gamemd @ 0x00718b70 Find_Nearby_Passable_Cell per fn-process.md:176,209; Rust: no passable-cell fallback |
| Process / bridge Z-height check at destination | DRIFT | Destination cell has bridge overlay (CellClass+0x140 & 0x100) | gamemd: checks whether unit's current Z > CellClass__GetGroundHeight + g_BridgeZOffset×3; sets bridge flag (uVar11) for PlaceInfantryInCell | Rust: no bridge height check in tick_teleport_movement; position Z is not adjusted for bridge cells | Unit arriving at bridge-overlay cells gets wrong Z — renders below bridge, Z-order error visible | gamemd @ 0x00718b70 bridge-Z block per fn-process.md:70-82; Rust: no bridge check in teleport_movement.rs |
| Process / ChronoInTransit flag check in drive path | DRIFT | Unit in drive-path (mission ≠ ENTER) with cached destination | gamemd: if TechnoClass+0x27c (ChronoInTransit) clear, calls Find_Nearby_Passable_Cell + PlaceInfantryInCell; if set, calls locomotion move directly | Rust: no ChronoInTransit check in tick_teleport_movement; always performs direct position snap | Drive-path passability is bypassed for non-ENTER missions — unit snaps into impassable cells | gamemd @ 0x00718b70 Path B ChronoInTransit per fn-process.md:196-199; Rust @ src/sim/movement/teleport_movement.rs:175-188 (direct position write) |
| Process / sentinel dest-cache "no destination" path | DRIFT | Locomotor has no pending destination (cache = g_NullCoord 0,0,0) | gamemd: if both dest caches match sentinel, reads TechnoClass current Location and returns 0 — no action this tick | Rust: `teleport_state` is either Some (active) or None (inactive); no sentinel check; once issue_teleport_command sets a destination it is always non-null | No silent no-op path in Rust — once a teleport is issued it always executes; gamemd can be in a "locomotor present, no dest" steady state that Rust cannot represent | gamemd @ 0x00718b70 Path A per fn-process.md:191-194; Rust @ src/sim/movement/teleport_movement.rs (None = no teleport; no sentinel state) |
| Process / secondary mission values (8, 9, 7, 0x19) triggering radio gate | DRIFT | Unit with secondary mission state 8, 9, 7, or 0x19 | gamemd: vtable+0x184 returns secondary state value; if 8/9/7/0x19, jumps to LAB_00718ce9 (radio-link gate) | Rust: no secondary mission check | Secondary mission states that should gate warp into dock are ignored — unit can warp at wrong mission phase | gamemd @ 0x00718b70 vtable+0x184 check per fn-process.md:127-128; Rust: no secondary mission handling |

<!-- compare-fn-post-warp-validation (task #40) -->
| PostWarpValidation / water-terrain death check | DRIFT | Teleport destination is water cell (CellClass+0xec==2), unit is not aircraft, not Teleporter-type, no bridge overlay | gamemd: sets TechnoClass+0x3cd (dying flag) = 1, calls vtable+0x3a0 (Die), triggers kill-credit chain (FUN_006b0ae0 with TechnoClass+0x428/+0x42c) | Rust: no post-warp terrain validation; unit arrives at any cell without dying | Player can teleport chrono units to water — they survive instead of dying; major exploit | gamemd @ 0x007187a0 phase-4 death path per fn-post-warp-validation.md:110-133; Rust: no PostWarpValidation call anywhere in teleport_movement.rs |
| PostWarpValidation / blocked-cell bridge-overlay death | DRIFT | Teleport destination is impassable (Can_Enter_Cell returns 7) AND has bridge overlay AND unit is not teleporter-type | gamemd: second death path — sets dying flag, calls Die, kill-credit chain | Rust: no impassable check; unit lands on any cell | Unit survives landing on blocked bridge-overlay cells that should kill it | gamemd @ 0x007187a0 phase-5 per fn-post-warp-validation.md:154-170; Rust: no validation |
| PostWarpValidation / aircraft power-surplus exemption | DRIFT | Aircraft (TechnoType+0x67c==3) with Powered=yes and owner house has no power surplus teleporting to water | gamemd: revokes aircraft water-death exemption when `TechnoType+0x410 (Powered)` set and house has no power surplus — aircraft dies on water if unpowered | Rust: no aircraft/power check | Unpowered aircraft survive water-destination teleports incorrectly | gamemd @ 0x007187a0 phase-3 per fn-post-warp-validation.md:86-104; Rust: no power/aircraft check |
| PostWarpValidation / occupant warp-notification at destination | DRIFT | Teleport destination cell has occupants with warpable flag (vtable+0x160 returns true) | gamemd: walks CellClass+0xe4 occupant list, calls vtable+0x16c on each warpable occupant with Rules+0xfa8 arg (warp-anim displacement) | Rust: no occupant notification; existing occupants at destination are undisturbed | Occupants at destination receive no warp-displacement — visual stacking, broken sub-cell occupancy | gamemd @ 0x007187a0 phase-1 per fn-post-warp-validation.md:57-71; Rust: no occupant walk at arrival |
| PostWarpValidation / kill-credit attribution + TechnoClass+0x2d8 clear | DRIFT | Any post-warp death (water or blocked bridge) | gamemd: calls FUN_006b0ae0(TechnoClass+0x428, TechnoClass+0x42c) for kill credit, clears TechnoClass+0x2d8 | Rust: no death → no kill credit issued; TechnoClass+0x2d8 not cleared | Player gets no kill credit for teleporting enemies to water; source ptrs left dirty | gamemd @ 0x007187a0 death paths per fn-post-warp-validation.md:122-128; Rust: no kill attribution |
| PostWarpValidation / function entirely absent | DRIFT | Every completed teleport | gamemd: calls PostWarpValidation unconditionally from state-5 (when TechnoClass+0x280==0) | Rust: no PostWarpValidation call at any phase of tick_teleport_movement or issue_teleport_command | All five observable behaviors of PostWarpValidation (terrain death, aircraft check, power check, occupant notify, kill credit) are missing | gamemd @ 0x007192f0 state-5 `TeleportLocomotionClass__PostWarpValidation(...)` per fn-state-machine-tick.md:179; Rust @ src/sim/movement/teleport_movement.rs (no equivalent) |

<!-- compare-fn-phase0-set-warping-out (task #41) -->
| Phase0_SetWarpingOut / 60-tick WarpOut warm-up before teleport | DRIFT | Any non-instant teleport initiation (non-harvester, non-Teleporter-flag) | gamemd: sets TechnoClass+0x270 (BeingWarped=1), arms timer to 60 ticks (`Locomotor+0x40 = 0x3c`), advances state to 1 — unit is visible at departure for 60 ticks (2 seconds at 30fps) before teleporting | Rust: `TeleportPhase::Relocate` executes in a single tick; no 60-tick warm-up; no BeingWarped flag | Unit disappears 60 ticks too early — player never sees the WarpOut shimmer warm-up; teleport appears instantaneous | gamemd @ 0x007197d0 per fn-phase0-set-warping-out.md:28-34; Rust @ src/sim/movement/teleport_movement.rs:163 (Relocate: single tick) |
| Phase0_SetWarpingOut / BeingWarped flag (TechnoClass+0x270 = 1) | DRIFT | Any warp initiation | gamemd: sets TechnoClass+0x270 (BeingWarped) = 1 in Phase0_SetWarpingOut | Rust: no BeingWarped flag; `teleport_state = Some(...)` is the only signal | Systems reading TechnoClass+0x270 (combat system, damage immunity, render translucency) see flag never set; unit is damageable during warp warm-up and not shown translucent | gamemd @ 0x007197d0 `*(TechnoClass+0x270) = 1` per fn-phase0-set-warping-out.md:28; Rust: no BeingWarped flag in teleport_movement.rs |

<!-- compare-fn-clear-pending-warp-phase (task #42) -->
| ClearPendingWarpPhase / cancel anim on no-destination abort | MISSING | Warp initiated but destination becomes invalid (unit moved, cell occupied) before departure | gamemd: spawns WarpOut cancel anim (AnimClass at departure cell) + clears TechnoClass+0x280 (WarpState=0) | Rust: no cancel path; once `teleport_state = Some(...)`, Relocate always executes in the next tick — no destination re-validation before relocation | Player sees no cancel anim when warp is aborted mid-setup; unit teleports to stale/invalid destination instead | gamemd @ 0x00719790 per fn-clear-pending-warp-phase.md:62-64; Rust: no equivalent path (teleport_state always executes once set) |

<!-- compare-fn-timer-check (task #43) -->
| TimerCheck / timer formula — frame-stamp vs decrement | DRIFT | All timer-based waits (states 1 and 6), including ChronoDelay | gamemd: `g_CurrentFrameCounter - start >= duration`; start=-1 means instant expiry; correctly handles pauses, frame-rate changes | Rust: `being_warped_ticks -= 1` per tick unconditionally; no frame-stamp; pause/fast-forward diverge immediately | Delay duration drifts when tick rate varies — unit dematerializes too early or too late relative to gamemd timing | gamemd @ 0x00719bf0 timer formula per fn-timer-check.md:36-41; Rust @ src/sim/movement/teleport_movement.rs:204 |
| TimerCheck / warp shimmer gate clear (TechnoClass+0x271=0) on expiry | DRIFT | Timer expiry (end of ChronoDelay in states 1 and 6) | gamemd: clears TechnoClass+0x271 (warp anim/shimmer gate) = 0 on timer expiry — signals full materialization | Rust: sets `entity.teleport_state = None` only; no TechnoClass+0x271 clear | Shimmer/translucency gate never cleared — any system reading +0x271 sees permanent warp-in-progress after teleport completes | gamemd @ 0x00719bf0 `*(TechnoClass+0x271) = 0` per fn-timer-check.md:49; Rust @ src/sim/movement/teleport_movement.rs:216 (entity.teleport_state = None only) |
| TimerCheck / weapon re-engagement via Passive_Target_Acquire on expiry | DRIFT | Timer expiry with TechnoClass+0x2b4==0 (targeting inactive) | gamemd: calls FUN_0070f770 + TechnoClass__Passive_Target_Acquire; if no target, calls vtable+0x484(0,1) to resume locomotion | Rust: no weapon re-engagement after ChronoDelay ends; locomotor is restored but no targeting pass or resume call | Unit completes warp but does not auto-re-acquire targets — player sees delayed weapon engagement or unit sitting idle after teleport | gamemd @ 0x00719bf0 targeting block per fn-timer-check.md:52-58; Rust @ src/sim/movement/teleport_movement.rs:216-222 (loco.end_override only) |
| TimerCheck / warp-count counter increment at +0x38 | DRIFT | Timer expiry when Locomotor+0x38 > 0 | gamemd: increments Locomotor+0x38 (warp count/cycle counter) on each timer expiry | Rust: no warp count field; TeleportState is dropped on completion | Warp count tracking absent — any system reading Locomotor+0x38 for cooldown, stats, or exploit prevention sees 0 | gamemd @ 0x00719bf0 `*(param_1+0x38) += 1` per fn-timer-check.md:62-64; Rust: no warp count field |

<!-- compare-fn-initiate-warp (task #38) -->
| InitiateWarp / distance computation — 3D vs 2D | DRIFT | Any teleport initiation where source or destination has non-zero Z (bridge, cliff) | gamemd: Euclidean 3D distance `sqrt(dX²+dY²+dZ²)` in leptons from `GetCoords()` (foundation center) to destination | Rust: Euclidean 2D distance `sqrt(dx²+dy²)` in leptons from `entity.position.rx/ry × 256` to destination, no Z component | ChronoDelay ticks differ for units on elevated terrain (bridges, cliffs) — unit on bridge teleporting to ground computes shorter delay in gamemd than Rust | gamemd @ 0x00719400 step-2 Sqrt_Approx per fn-initiate-warp.md:60-66; Rust @ src/sim/movement/teleport_movement.rs:107-110 (dx/dy only) |
| InitiateWarp / distance source — GetCoords vs cell position | DRIFT | Any teleport initiation for a building or multi-cell unit | gamemd: uses `TechnoClass vtable+0x48` (`GetCoords()`) which returns foundation center in leptons; Rust uses `entity.position.rx * 256 + 0` — NW-cell edge, not center | Rust starts distance from cell NW corner, gamemd from foundation center (+128 leptons offset per cell axis for 1×1 buildings, larger for bigger foundations) | Distance is systematically off by up to `(width-1)×128` leptons — delay ticks differ for large-foundation buildings | gamemd @ 0x00719400 step-2 `piVar2 = GetCoords()` per fn-initiate-warp.md:60; Rust @ src/sim/movement/teleport_movement.rs:107 (`entity.position.rx as i32 * 256`) |
| InitiateWarp / elapsed-time adjustment (step 5) | DRIFT | Subsequent InitiateWarp calls when timer already armed | gamemd: subtracts `CurrentFrame - frame_start` from computed delay before clamping — if timer was already running, the remaining ticks are shorter | Rust: no elapsed-time subtraction; `being_warped_ticks` always set to full computed delay | Second warp initiated while timer running gets full unsubtraced delay in Rust vs. shorter remaining delay in gamemd — visible as longer-than-expected chrono delay on back-to-back warps | gamemd @ 0x00719400 step-5 per fn-initiate-warp.md:99-104; Rust @ src/sim/movement/teleport_movement.rs:112 (no elapsed subtraction) |
| InitiateWarp / ChronoMinimumDelay clamp direction (`<=` vs `<`) | DRIFT | Computed delay exactly equals ChronoMinimumDelay (default 16 ticks) | gamemd: clamps when `delay <= ChronoMinimumDelay` — delay equal to minimum still triggers clamp | Rust: clamps when `delay < chrono_minimum_delay` — delay equal to minimum passes through without re-clamping | Off-by-one: when computed delay == 16, gamemd re-clamps (and may reset timer start frame), Rust does not — frame-stamp vs countdown diverge for this specific value | gamemd @ 0x00719400 step-6 `if (iVar3 <= ...)` per fn-initiate-warp.md:110-118; Rust @ src/sim/movement/teleport_movement.rs:70 (`if delay < rules.chrono_minimum_delay`) |
| InitiateWarp / departure anim (WarpOut at source) | DRIFT | Any teleport initiation (conditional on guard `in_EAX != unaff_EBX`) | gamemd: spawns AnimClass at TechnoClass+0x9c (source Location leptons) using Rules+0x33c (WarpOut AnimType) | Rust: no anim spawned at departure | WarpOut departure shimmer absent — player sees no visual effect when teleport begins | gamemd @ 0x00719400 step-1 per fn-initiate-warp.md:44-47; Rust @ src/sim/movement/teleport_movement.rs (no anim) |
| InitiateWarp / departure sound (WarpOut at source) | DRIFT | Any teleport with TechnoType+0x578 or Rules+0x21c sound configured | gamemd: plays departure sound via `VocClass__PlayAt` at source coords | Rust: no sound (sim/ correctly cannot call audio/) | WarpOut departure sound absent | gamemd @ 0x00719400 step-11 sound check per fn-initiate-warp.md:171-172; Rust: no audio in teleport_movement.rs |
| InitiateWarp / WarpingOut flag set at warp initiation | DRIFT | Any teleport initiation (non-harvester) | gamemd: sets `TechnoClass+0x271` (WarpingOut) = 1 in step 8 | Rust: no WarpingOut flag — only `teleport_state: Some(...)` is set | Systems gating on WarpingOut (damage immunity, render translucency) never activate — unit takes full damage during warp | gamemd @ 0x00719400 step-8 per fn-initiate-warp.md:137; Rust @ src/sim/movement/teleport_movement.rs:119-133 (no flag) |
| InitiateWarp / harvester instant-warp dispatch: GetTypeID+TechnoType flag vs is_harvester bool | DRIFT | Any chrono harvester (CMIN) or Teleporter-flagged infantry | gamemd: checks `GetTypeID == 1` (infantry type) AND `TechnoType+0xe0e` (Teleporter bool) — two conditions, verified at runtime | Rust: `is_harvester: bool` passed by caller at call site — computed upstream by miner system, not by the locomotor itself | If the caller passes wrong `is_harvester` value, or a non-CMIN Teleporter infantry is issued via the wrong code path, the instant-warp gate fires/skips incorrectly; the locomotor cannot self-correct without the flag lookup | gamemd @ 0x00719400 step-9 per fn-initiate-warp.md:143-155; Rust @ src/sim/movement/teleport_movement.rs:100-103 (is_harvester param) |
| InitiateWarp / WarpAttach detach (step 10) | DRIFT | Unit with active WarpAttach (TechnoClass+0x694 != 0) | gamemd: calls `WarpAttachClass__Detach` if TechnoClass+0x694 non-null | Rust: no WarpAttach detach | WarpAttach link persists through teleport — ChronoSphere beam stays attached to teleporting unit | gamemd @ 0x00719400 step-10 per fn-initiate-warp.md:160-164; Rust: no WarpAttach handling |
| InitiateWarp / arrival anim (WarpOut at destination with extra args) | DRIFT | Any teleport completion | gamemd: spawns second AnimClass at destination coords with persistent-anim args `(type, coords, 0, 1, 0x600, 0, 0)` | Rust: no arrival anim | Arrival-cell anim (persistent indicator) absent | gamemd @ 0x00719400 step-12 per fn-initiate-warp.md:196; Rust: no arrival anim |
| InitiateWarp / CrateClass__PickupDispatch at destination | DRIFT | Any teleport where destination cell has a crate | gamemd: calls `CrateClass__PickupDispatch` at destination after arrival | Rust: no crate pickup | Units teleporting onto crate cells do not collect the crate | gamemd @ 0x00719400 step-12 per fn-initiate-warp.md:193; Rust: no crate pickup |
| InitiateWarp / arrival sound (WarpIn at destination) | DRIFT | Any teleport with TechnoType+0x574 or Rules+0x218 sound | gamemd: plays arrival sound via VocClass__PlayAt at destination | Rust: no sound | WarpIn arrival sound absent | gamemd @ 0x00719400 step-11 arrival sound per fn-initiate-warp.md:178-179; Rust: no audio |
| InitiateWarp / bridge flag update at destination (step 11) | DRIFT | Teleport to bridge-overlay destination cell | gamemd: checks `CellClass+0x140 & 0x100`; sets `TechnoClass+0x8c = 1` if bridge overlay present | Rust: no bridge flag write in issue_teleport_command | Unit arrives at bridge cell without bridge flag — Z-order rendering error | gamemd @ 0x00719400 step-11 per fn-initiate-warp.md:174-176; Rust: no bridge flag write |

<!-- compare-fn-update-position (task #39) -->
| Update_Position / bridge Z offset added to dest Z | DRIFT | Teleport destination on a bridge cell (CellClass+0x140 & 0x100 set) when `TechnoClass+0x8c == 0` | gamemd: adds `g_BridgeZOffset_Teleport` (0x00b0ec2c, value=0 in stock YR) to dest Z and sets TechnoClass+0x8c=1 | Rust: no bridge Z addition; no TechnoClass+0x8c flag | Value is 0 at runtime so Z is numerically equal, but TechnoClass+0x8c bridge flag is never set in Rust — systems reading the bridge flag see wrong state. Required evidence for INTERNAL-ONLY: prove all consumers of TechnoClass+0x8c are absent in Rust. | gamemd @ 0x00718260 mode-A bridge block per fn-update-position.md:79-84; Rust @ src/sim/movement/teleport_movement.rs:178-187 (no bridge Z, no bridge flag) |
| Update_Position / infantry scatter at destination (mode B cell walk) | DRIFT | Teleport destination cell occupied by infantry | gamemd: walks `CellClass+0xe4` (or `+0xe8` for bridge layer) object list; calls Unlimbo on blocking infantry via `vtable+0x16c` with `Rules+0xfa8` scatter radius | Rust: no occupant walk; no Unlimbo call | Infantry stacks at teleport destination instead of scattering — visual overlap, occupancy corruption | gamemd @ 0x00718260 mode-B loop per fn-update-position.md:41-48; Rust @ src/sim/movement/teleport_movement.rs:181-192 (occupancy.move_entity only) |
| Update_Position / FootClass__Find_Nearby_Passable_Cell fallback (mode B, param_4!=0) | DRIFT | Destination cell impassable or occupied and passable-cell search enabled | gamemd: calls FootClass__Find_Nearby_Passable_Cell, writes adjusted coords into TechnoClass+0x288/+0x28c/+0x290 | Rust: no passable-cell search; always warps to exact target cell | Unit warps into impassable/occupied cells with no fallback — occupancy violation, possible stuck state | gamemd @ 0x00718260 mode-B param_4 block per fn-update-position.md:53-60; Rust: no fallback |
| Update_Position / sub-cell placement via CellClass__PlaceInfantryInCell vs CELL_CENTER_LEPTON | DRIFT | Any teleport with infantry or units requiring sub-cell placement | gamemd: uses `CellClass__PlaceInfantryInCell` which determines exact sub-cell (NE/NW/SE/SW) based on occupancy | Rust: forces `sub_x = sub_y = CELL_CENTER_LEPTON` (always cell center, no sub-cell awareness) | Infantry placed at always-center sub-cell instead of occupancy-aware placement — multiple infantry in same cell stack at center instead of distributing to sub-cells | gamemd @ 0x00718260 PlaceInfantryInCell via Process per fn-update-position.md; Rust @ src/sim/movement/teleport_movement.rs:182-183 (sub_x/sub_y = CELL_CENTER_LEPTON) |
| Update_Position / ground height Z applied to destination | DRIFT | Any teleport where dest cell ground height differs from input Z | gamemd: calls `CellClass__GetGroundHeight` and overwrites dest-cache-1 Z with ground height | Rust: no ground height lookup; Z remains at `position.z` (not updated during teleport) | Unit arrives at wrong Z if terrain height at destination differs — can render below or above terrain surface | gamemd @ 0x00718260 mode-A `uVar11 = CellClass__GetGroundHeight` per fn-update-position.md:76-77; Rust @ src/sim/movement/teleport_movement.rs:178-188 (no Z update) |
| Update_Position / coord roundtrip via vtable+0xf4/+0xf0 | DRIFT | Any teleport — mode A commit | gamemd: pushes dest to owner via `vtable+0xf4` (Move_To), reads back applied coords from TechnoClass+0x288/+0x28c/+0x290 into locomotor cache; then calls `vtable+0xf0` | Rust: direct assignment `entity.position.rx = teleport.target_rx` — no vtable dispatch, no coord read-back | Any clamping or adjustment applied by vtable+0xf4 (map boundary clamp, impassable-tile rejection) is skipped — teleport can place unit outside valid position space | gamemd @ 0x00718260 mode-A vtable+0xf4/+0xf0 per fn-update-position.md:70-86; Rust @ src/sim/movement/teleport_movement.rs:177-183 (direct position write) |

<!-- compare-fn-state-machine-tick (task #37) -->
| StateMachineTick / State-0 WarpingOut short-circuit | DRIFT | State==0 AND TechnoClass+0x271 (WarpingOut) set AND TechnoClass+0x280==0 | gamemd dispatches TimerCheck vtable slot and returns immediately — re-entry guard prevents double-tick | Rust has no WarpingOut flag; tick_teleport_movement processes every tick unconditionally | Unit can be double-ticked during active warp animation — state corruption visible as premature relocation or skipped delay | gamemd @ 0x007192f0 entry block per fn-state-machine-tick.md; Rust @ src/sim/movement/teleport_movement.rs:153 (no guard) |
| StateMachineTick / State-0 WarpState fast-forward | DRIFT | State==0 AND TechnoClass+0x280 non-zero (external caller set pending warp) | gamemd copies TechnoClass+0x280 into state counter and returns — external caller can skip directly to any state | Rust has no WarpState copy mechanism; no external fast-forward path | External warp commands (ChronoSphere targeting a unit) cannot skip the state machine — affected unit stays in wrong state | gamemd @ 0x007192f0 state-0 WarpState-copy branch per fn-state-machine-tick.md; Rust @ src/sim/movement/teleport_movement.rs (no equivalent) |
| StateMachineTick / State-0 body — 60-tick warm-up before warp | DRIFT | ChronoInTransit gate (TechnoClass+0x270) set, state==0 | gamemd: sets ChronoInTransit flag, arms 60-tick timer (`param_1[0x10] = 0x3c`), advances to state 1 — unit is visible at departure for 60 ticks before disappearing | Rust: Relocate executes in exactly 1 tick with no warm-up period | Unit disappears from departure cell 60 ticks (2 seconds at 30fps) too early — player sees teleporter vanish instantly instead of after a brief visible delay | gamemd @ 0x007192f0 state-0-body per fn-state-machine-tick.md:108-112; Rust @ src/sim/movement/teleport_movement.rs:170 (single-tick Relocate) |
| StateMachineTick / State-0 — WarpOut anim at departure cell | DRIFT | Any teleport initiation | gamemd: spawns AnimClass at TechnoClass+0x9c (departure Location) using Rules+0x33c (WarpOut anim type) | Rust: spawns no warp animation at departure cell | WarpOut departure shimmer is missing — player sees no visual effect when unit begins teleporting | gamemd @ 0x007192f0 state-0 AnimClass spawn per fn-state-machine-tick.md:72-73; Rust @ src/sim/movement/teleport_movement.rs (no anim) |
| StateMachineTick / State-0 — WarpOut departure sound | DRIFT | Any teleport initiation with TechnoType+0x578 sound configured | gamemd: plays departure sound via VocClass__PlayAt at departure coords using TechnoType+0x578 (WarpOut sound) vs Rules+0x218 fallback | Rust: no sound played (sim/ correctly cannot call audio/) | WarpOut departure sound is absent — player hears silence when unit starts teleporting | gamemd @ 0x007192f0 state-0 VocClass__PlayAt per fn-state-machine-tick.md:76-77; Rust: no audio pathway in sim/ |
| StateMachineTick / State-0 — BulletClass retargeting on warp | DRIFT | Any teleport initiation with bullets targeting the warping unit | gamemd: scans g_BulletClass_Array, calls BulletClass__UpdateTarget for each bullet targeting this unit | Rust: no bullet retargeting sweep | Bullets targeting a warping unit remain aimed at the old position and still track/hit — player sees projectiles bend toward vanished unit | gamemd @ 0x007192f0 state-0 BulletClass scan per fn-state-machine-tick.md:68-69; Rust @ src/sim/movement/teleport_movement.rs (no bullet scan) |
| StateMachineTick / State-0 — WarpAttachClass__Detach on warp | DRIFT | Unit with active WarpAttach (TechnoClass+0x694 != 0) at warp initiation | gamemd: calls WarpAttachClass__Detach if TechnoClass+0x694 non-null | Rust: no WarpAttach detach call | WarpAttach link (used by ChronoSphere temporal shrink beam) persists through teleport — visual corruption and logic errors for units teleporting while being targeted by ChronoSphere | gamemd @ 0x007192f0 state-0 WarpAttachClass__Detach per fn-state-machine-tick.md:74-75; Rust: no WarpAttach handling |
| StateMachineTick / State-0 — bridge flag set at departure | DRIFT | Teleport to destination cell with bridge overlay (CellClass+0x140 & 0x100) | gamemd: sets TechnoClass+0x8c (bridge flag) based on destination cell bridge overlay check | Rust: no bridge flag written during teleport | Unit arriving at bridge cell lacks bridge-Z lift — renders at wrong height, Z-ordering errors visible on bridge tiles | gamemd @ 0x007192f0 state-0 bridge-flag write per fn-state-machine-tick.md:80-81; Rust: no bridge flag in teleport_movement.rs |
| StateMachineTick / State-1 — timer wait before teleport | DRIFT | After state-0 body executes (ChronoInTransit path) | gamemd: stays in state 1 for up to 60 ticks (set by state-0), dispatching TimerCheck each tick; unit still visible at departure | Rust: no state-1 equivalent; Relocate is single-tick | Unit teleports away 60 ticks (≈2 seconds) too early; all departure-side visibility is absent | gamemd @ 0x007192f0 state-1 per fn-state-machine-tick.md:118-123; Rust @ src/sim/movement/teleport_movement.rs:170 (Relocate, no wait) |
| StateMachineTick / State-2 — WarpIn anim at departure cell | DRIFT | Transition from state-1 to state-2 | gamemd: spawns AnimClass at TechnoClass+0x9c (departure coords, before teleport) using Rules+0x33c (WarpIn anim) | Rust: spawns no departure-side WarpIn anim | Second departure-cell visual effect (WarpIn shimmer at source) is absent | gamemd @ 0x007192f0 state-2 AnimClass spawn per fn-state-machine-tick.md:128-130; Rust: no anim spawning |
| StateMachineTick / State-2 — WarpIn arrival sound | DRIFT | Transition from state-1 to state-2 with TechnoType+0x578 (WarpIn sound) | gamemd: plays WarpIn sound via VocClass__PlayAt at TechnoClass+0x27/+0x28/+0x29 (destination coords) | Rust: no sound | WarpIn arrival sound absent at departure side | gamemd @ 0x007192f0 state-2 sound per fn-state-machine-tick.md:130-131; Rust: no audio |
| StateMachineTick / State-2 — WarpingOut flag set | DRIFT | State-2 execution (teleport-to-destination tick) | gamemd: sets TechnoClass+0x271 (WarpingOut) = 1 before teleport move | Rust: no WarpingOut flag written | Systems that gate on WarpingOut (e.g., damage immunity during warp, render translucency) see flag never set — unit takes full damage during warp | gamemd @ 0x007192f0 state-2 per fn-state-machine-tick.md:131; Rust: no WarpingOut flag in teleport_movement.rs |
| StateMachineTick / State-2 — bridge flag clear | DRIFT | State-2 execution | gamemd: clears TechnoClass+0x8c (bridge flag) in state 2 | Rust: no bridge flag cleared | Bridge height state not restored if unit teleported away from a bridge cell — Z-ordering corruption on subsequent moves | gamemd @ 0x007192f0 state-2 per fn-state-machine-tick.md:133; Rust: no bridge flag |
| StateMachineTick / State-3 — ChronoDelay loaded from Rules each tick | DRIFT | State-3 tick while unit moves to destination | gamemd: writes `TechnoClass+0x284 = Rules+0xbec (ChronoDelay)` unconditionally each tick in state 3, so the post-arrival delay is taken from the live Rules value at the moment of arrival | Rust: `being_warped_ticks` is computed once from distance formula at issue time; if Rules values change mid-warp the Rust value is stale | ChronoDelay reflects old Rules value if rules reloaded mid-warp; more importantly the delay source is fundamentally different (distance-based vs flat Rules value for non-Chrono-Trigger case) | gamemd @ 0x007192f0 state-3 per fn-state-machine-tick.md:153; Rust @ src/sim/movement/teleport_movement.rs:65-79 (compute_chrono_delay, set once) |
| StateMachineTick / State-4 — explicit Mark/Place/Unlimbo vtable calls | DRIFT | State-4 execution (final placement) | gamemd: calls vtable+0x1b4 (Mark occupation), vtable+0x1cc (Place unit), vtable+0x124 (Unlimbo) in state 4 | Rust: occupancy is swapped in the Relocate tick via occupancy.move_entity; no explicit Unlimbo or Place vtable calls | Unlimbo sequencing (which triggers health-bar spawn, targeting updates, rally-point assignment) fires at wrong tick relative to destination-cell confirmation — visible as brief missing unit state | gamemd @ 0x007192f0 state-4 per fn-state-machine-tick.md:161-165; Rust @ src/sim/movement/teleport_movement.rs:181-192 (occupancy.move_entity in Relocate) |
| StateMachineTick / State-5 — PostWarpValidation drop-on-water kill chain | DRIFT | Arrival destination is impassable/water AND TechnoClass+0x280==0 | gamemd: calls TeleportLocomotionClass__PostWarpValidation(dest_x, dest_y, dest_z) which kills the unit if the destination is invalid (water, impassable) | Rust: no PostWarpValidation call — unit arrives at any destination without validation | Unit teleported to water/impassable cell survives instead of dying — player can use teleport to bypass impassable terrain | gamemd @ 0x007192f0 state-5 PostWarpValidation per fn-state-machine-tick.md:179; Rust: no post-warp validation anywhere in teleport_movement.rs |
| StateMachineTick / State-5 — WarpIn anim at arrival cell | DRIFT | Arrival (state-5 execution, after unit teleported to destination) | gamemd: spawns AnimClass at TechnoClass+0x9c (arrival Location, after teleport) | Rust: no arrival anim spawned | WarpIn shimmer at destination cell is absent — player sees no visual confirmation of unit materialization at refinery pad or target cell | gamemd @ 0x007192f0 state-5 AnimClass spawn per fn-state-machine-tick.md:185-186; Rust: no anim spawning |
| StateMachineTick / State-5 — arrival sound (WarpIn at destination) | DRIFT | Arrival with TechnoType+0x574 (WarpIn sound) | gamemd: plays WarpIn sound at arrival cell | Rust: no sound | Arrival sound absent — player hears nothing when unit materializes | gamemd @ 0x007192f0 state-5 sound per fn-state-machine-tick.md:174-175; Rust: no audio |
| StateMachineTick / State-5 — in-playfield check clears flag | DRIFT | Unit arrives outside playfield bounds | gamemd: calls MapClass__Is_Cell_In_Playfield; if false, clears TechnoClass+0x3d5 | Rust: no playfield check during teleport | Unit teleporting to map-edge cells retains in-playfield flag incorrectly — may cause render or targeting bugs for out-of-bounds arrivals | gamemd @ 0x007192f0 state-5 playfield check per fn-state-machine-tick.md:177; Rust: no playfield check |
| StateMachineTick / State-5 — source building ptrs cleared | DRIFT | Arrival after any teleport that originated from a building (rally-point teleport) | gamemd: clears TechnoClass+0x428 and TechnoClass+0x42c (source building pointers) after arrival | Rust: no source building ptr cleanup | Source building pointers remain set after teleport completion — unit may attempt to return to or interact with a non-existent source building reference | gamemd @ 0x007192f0 state-5 per fn-state-machine-tick.md:181; Rust: no building ptr cleanup |
| StateMachineTick / State-6 — post-arrival ChronoDelay from Rules | DRIFT | After arrival (state-6), non-harvester unit | gamemd: timer armed in state-5 from `TechnoClass+0x284` (written from `Rules+0xbec` = ChronoDelay) — the post-arrival hold is the flat INI value, not distance-based | Rust: `being_warped_ticks` for the ChronoDelay phase was computed at issue time from distance formula (`compute_chrono_delay`) | For non-Chrono-Trigger case: gamemd post-arrival delay = `Rules.ChronoDelay` (default 60 ticks); Rust uses distance-based formula result — different tick counts, wrong post-arrival translucency duration | gamemd @ 0x007192f0 state-6 / state-5 timer arm per fn-state-machine-tick.md:183-184; Rust @ src/sim/movement/teleport_movement.rs:65-79 |
| StateMachineTick / State-7 — BeingWarped flag clear | DRIFT | End of warp sequence (state-7) | gamemd: clears TechnoClass+0x271 (BeingWarped/WarpingOut) = 0, then resets state machine to 0 | Rust: no BeingWarped flag; TeleportState is simply set to None | Any system that polls TechnoClass+0x271 to detect warp completion never sees the clear | gamemd @ 0x007192f0 state-7 per fn-state-machine-tick.md:209-210; Rust @ src/sim/movement/teleport_movement.rs:216-222 (entity.teleport_state = None) |
| StateMachineTick / State-7 — WarpState reset | DRIFT | End of warp sequence | gamemd: clears `TechnoClass+0x280` (WarpState) = 0 and `TeleportLocomotionClass+0x30` (Z dest cache) = 0 | Rust: clears only `TeleportState` struct — no WarpState or cached-coord reset | WarpState remains non-zero after warp — next tick's state-0 guard reads stale WarpState and fast-forwards incorrectly | gamemd @ 0x007192f0 state-7 per fn-state-machine-tick.md:213-215; Rust @ src/sim/movement/teleport_movement.rs:216 |
| StateMachineTick / Timer mechanism — frame-stamp vs countdown | DRIFT | All timer-based waits (states 1 and 6) | gamemd: arms timer as `(frame_start = CurrentFrame, duration = N_ticks)`; timer fires when `CurrentFrame - frame_start >= duration` — immune to tick-rate variation and handles pauses correctly | Rust: decrements `being_warped_ticks` once per `tick_teleport_movement` call, which fires every `advance_tick()` regardless of `tick_ms` value (tick_ms == 0 guard exists but non-zero tick_ms with different durations are treated identically) | If tick rate changes (slow-mode, fast-forward) the Rust countdown diverges from gamemd frame-stamp check: Rust undercounts ticks in fast-forward, overcounts in slow | gamemd @ 0x007192f0 state-1/state-6 TimerCheck dispatch per fn-state-machine-tick.md:118,205; Rust @ src/sim/movement/teleport_movement.rs:204 (being_warped_ticks -= 1) |

<!-- compare-fn-accessors (task #44) -->
| Accessors / Is_Moving — armed flag byte | DRIFT | Any locomotion-framework poll of Is_Moving | gamemd: `Is_Moving` reads `TeleportLocomotionClass+0x34` byte == 1; HeadToCoord arms it, Stop_Moving clears it; the flag is separate from whether a dest cache is populated | Rust: no armed-flag byte; "is moving" is implied by `entity.teleport_state.is_some()` — no separate arming step | Any caller that checks Is_Moving between HeadToCoord and the first Process tick sees the wrong state in Rust; also no flag to clear on warp cancel | gamemd @ 0x00718080 param_1+0x30 per fn-accessors.md:44-46; Rust @ src/sim/movement/teleport_movement.rs:48-55 (TeleportState, no armed flag) |
| Accessors / Destination — HeadToCoord cache vs current location | DRIFT | Any system reading the locomotor's destination coord (e.g., pathfinding, UI waypoint display) | gamemd: `Destination` returns the 3D HeadToCoord cache (validated warp target) if Is_Moving==1, else current TechnoClass Location leptons | Rust: no `Destination` accessor; no HeadToCoord cache; target stored only as `(target_rx, target_ry)` in 2D cell coords without Z | Systems querying locomotor destination get incorrect coords or cannot query at all; Z component always absent | gamemd @ 0x007180a0 param_1[6..8] per fn-accessors.md:61-63; Rust: no Destination() equivalent |
| Accessors / HeadToCoord — 4-gate flag check before arming | DRIFT | Unit in a deploy/busy/boarding state issued a teleport command | gamemd: `HeadToCoord` checks 4 TechnoClass vtable gate flags (+0x37c, +0x380, +0x1d4, +0x1d8) before arming; if any set, clears TechnoClass+0x5a4 and aborts | Rust: `issue_teleport_command` arms unconditionally — no gate flag checks | Teleport is issued to a deploying or busy unit that gamemd would reject; unit warps mid-deploy | gamemd @ 0x00718100 gate checks per fn-accessors.md:88-96; Rust @ src/sim/movement/teleport_movement.rs:117 (unconditional arm) |
| Accessors / HeadToCoord — NullCoord rejection with event broadcast | DRIFT | Destination validated by Process returns NullCoord sentinel (no valid cell found) | gamemd: if Process returns NullCoord in dest-cache-0, calls `vtable+0x480(0,1)` (dest-rejected broadcast) and returns without arming | Rust: no NullCoord check after destination validation; no dest-rejected broadcast | Player issues an invalid teleport command; gamemd cancels silently with a broadcast; Rust arms and warps to a garbage or zero cell | gamemd @ 0x00718100 NullCoord check per fn-accessors.md:108-110; Rust: no rejection path in issue_teleport_command |
| Accessors / Stop_Moving — warp cancel path absent | MISSING | Any caller invoking Stop_Moving to cancel an in-progress teleport | gamemd: `Stop_Moving` clears HeadToCoord cache (all three coords to NullCoord), clears IsMoving=0, clears `full_object+0x36` extra flag | Rust: no Stop_Moving implementation; no way to cancel a teleport once `issue_teleport_command` has set `entity.teleport_state` | Teleport cannot be cancelled mid-flight; any system that calls Stop_Moving (e.g., damage interrupt, player command) has no effect | gamemd @ 0x00718230 per fn-accessors.md:141-145; Rust: no Stop_Moving equivalent in teleport_movement.rs |
| Accessors / Stop_Moving — full_object+0x36 extra flag clear | DRIFT | Stop_Moving call on any teleport locomotor | gamemd: Stop_Moving additionally clears `TeleportLocomotionClass+0x36` (byte flag, identity in struct-decode); its clear is part of the stop contract | Rust: no +0x36 flag exists in TeleportState — flag never cleared | Any behavior gated on +0x36 persists incorrectly after a stop event (identity TBD from struct-decode #12) | gamemd @ 0x00718230 `*(param_1+0x32)=0` per fn-accessors.md:145 (full_object+0x36); Rust: no +0x36 equivalent |
| Accessors / Mark_All_Occupation_Bits — RateTimer stub vs occupancy | DRIFT | Any locomotion-framework call to Mark_All_Occupation_Bits (e.g., after unit placement) | gamemd: `Mark_All_Occupation_Bits` calls `RateTimer__Set(&param_2)` — a stub that sets a rate timer instead of marking occupation bits | Rust: no Mark_All_Occupation_Bits; occupation bits written directly in `occupancy.move_entity` during the Relocate tick | If any caller expected the RateTimer side-effect (e.g., timing a placement-settle window), that side-effect is absent; conversely Rust's direct occupancy.move_entity fires at a different point in the tick than gamemd's locomotor-framework callback | gamemd @ 0x007192c0 RateTimer__Set per fn-accessors.md:156-163; Rust @ src/sim/movement/teleport_movement.rs:190-199 (occupancy.move_entity in Relocate tick) |

<!-- compare-fn-com-stubs (task #58) -->
| COM stubs / QueryInterface — IPiggyback QI | DRIFT | Any COM caller calling QI on TeleportLocomotionClass for IID_IPiggyback | gamemd: QueryInterface checks IID_IPiggyback via 4-DWORD GUID comparison; on match returns `param_1+0x18` (IPiggyback vtable pointer) and calls AddRef | Rust: no COM QI; locomotor interfaces replaced by `LocomotorState::begin_override`/`end_override`; no interface pointer dispatch | Any C++ caller using COM QI to obtain IPiggyback cannot function; the entire COM identity of the locomotor is absent | gamemd @ 0x00719e30 IID_IPiggyback path per fn-com-stubs.md:28-34; Rust @ src/sim/movement/locomotor.rs (begin_override, no COM) |
| COM stubs / Begin_Piggyback — double-stack guard | DRIFT | Second Begin_Piggyback call on an already-piggybacked locomotor | gamemd: `Begin_Piggyback` returns `E_FAIL (0x80004005)` if the piggyback slot at base+0x30 is already occupied — prevents double-stacking | Rust: `loco.begin_override(OverrideKind::Teleport)` has no double-stack guard visible in this module; behavior on re-issue is not validated here | Second teleport issued to a unit already piggybacking may silently overwrite or stack incorrectly instead of being rejected | gamemd @ 0x00719e90 E_FAIL guard per fn-com-stubs.md:59-62; Rust @ src/sim/movement/teleport_movement.rs:121-123 (begin_override, no guard) |
| COM stubs / End_Piggyback — kill-credit ptr clear (TechnoClass+0x428/+0x42c) | DRIFT | Teleport warp completes via End_Piggyback (normal clean termination) | gamemd: `End_Piggyback` clears `TechnoClass+0x428` (source building ptr) and `TechnoClass+0x42c` (source house ptr) — kill-credit attribution cleanup | Rust: `loco.end_override()` at teleport completion (tick_teleport_movement cleanup loop) does not clear any TechnoClass kill-credit pointers | Kill-credit source pointers remain set after warp — unit continues to credit kills to the source building/house that dispatched the teleport; wrong kill attribution visible in score | gamemd @ 0x00719ee0 +0x428/+0x42c clear per fn-com-stubs.md:93-95; Rust @ src/sim/movement/teleport_movement.rs:217-222 (end_override, no ptr clear) |
| COM stubs / Is_Ok_To_End — ChronoInTransit gate (TechnoClass+0x27c) | DRIFT | Teleport nominally complete but WarpIn anim still playing (ChronoInTransit set) | gamemd: `Is_Ok_To_End` gates piggyback release on `TechnoClass+0x27c == 0` (ChronoInTransit must be clear); if warp anim still playing, locomotor is not released | Rust: `loco.is_overridden()` is the only check; no ChronoInTransit guard; override released as soon as `being_warped_ticks == 0` | Drive locomotor restored while WarpIn anim still playing — unit resumes ground movement before the player sees it fully materialize; visible jitter at arrival | gamemd @ 0x00719f30 TechnoClass+0x27c condition per fn-com-stubs.md:148; Rust @ src/sim/movement/teleport_movement.rs:218-221 (is_overridden check only) |
| COM stubs / Is_Ok_To_End — IsDeploying gate (TechnoClass+0x6ad) | DRIFT | Unit is in deploy animation when warp would complete | gamemd: `Is_Ok_To_End` gates piggyback release on `TechnoClass+0x6AD == 0` (IsDeploying must be false); warp cannot complete if unit is mid-deploy | Rust: no IsDeploying check in cleanup path | Drive locomotor restored mid-deploy — unit may warp and redeploy simultaneously, breaking deploy state machine | gamemd @ 0x00719f30 TechnoClass+0x6AD condition per fn-com-stubs.md:151; Rust @ src/sim/movement/teleport_movement.rs:218-221 (no deploy check) |
| COM stubs / Is_Ok_To_End — Is_Moving check at end-of-warp | DRIFT | Warp nominally complete (being_warped_ticks==0) but locomotor IsMoving flag still set | gamemd: `Is_Ok_To_End` requires `Is_Moving() == false` as first condition — piggyback not released if locomotor is still armed | Rust: cleanup fires as soon as `being_warped_ticks == 0` with no Is_Moving equivalent check | If any code path re-arms the teleport between tick and cleanup, Rust releases the override anyway; gamemd would hold it | gamemd @ 0x00719f30 Is_Moving() condition per fn-com-stubs.md:131; Rust @ src/sim/movement/teleport_movement.rs:216 (teleport_state=None check only) |
| COM stubs / ILocomotion_QI_Thunk — this-adjustment | INTERNAL-ONLY | COM callers using ILocomotion vtable slot to QI for other interfaces | gamemd: 9-byte thunk adjusts `this` by -0x04 before calling QueryInterface (ILocomotion base at +0x04 → IUnknown base at +0x00) | Rust: no COM vtable dispatch; LocomotorState is accessed directly | The this-adjustment is COM plumbing only — no observable behavior difference. Algebraic proof: both produce the same interface pointer for any valid IID, just via different dispatch paths | gamemd @ 0x0071a160 per fn-com-stubs.md:163-170; Rust: no vtable thunk |

<!-- compare-struct-teleport-locomotion-class (task #45) -->
| Struct / TechnoClass+0x271 warp_anim_gate field absent | DRIFT | Any event that clears warp_anim_gate (TimerCheck expiry, state-7) | gamemd: TechnoClass+0x271 (`warp_anim_gate`) is set at warp start and cleared by TimerCheck when timer expires — this is the signal that the WarpOut shimmer animation has ended and the unit is visible again | Rust: no warp_anim_gate field in GameEntity; shimmer state implied only by `being_warped_ticks > 0` in TeleportState | Any system that polls +0x271 to show/hide the unit (translucency, targeting, selection bracket) sees wrong state — player may be able to target a unit that should still be shimmering | gamemd struct per struct-teleport-locomotion-class.md:139; Rust: no +0x271 equivalent in GameEntity |
| Struct / TechnoClass+0x27c chrono_in_transit field absent | DRIFT | End of warp (Is_Ok_To_End gate, ChronoInTransit check) | gamemd: TechnoClass+0x27c (`chrono_in_transit`) must be clear for Is_Ok_To_End to release the piggyback and for the drive path in Process to proceed; set during warp, cleared when complete | Rust: no chrono_in_transit field in GameEntity; locomotor override released unconditionally when being_warped_ticks==0 | Drive locomotor restored before ChronoInTransit clears — unit can move while still flagged as in-transit from gamemd's perspective; also Process drive-path guard is absent | gamemd struct per struct-teleport-locomotion-class.md:140; Rust: no +0x27c equivalent |
| Struct / TechnoClass+0x280 warp_state field absent | DRIFT | State-0 guard, state-7 reset, ClearPendingWarpPhase | gamemd: TechnoClass+0x280 (`warp_state`) gates state-0 WarpingOut short-circuit and is reset to 0 in state-7; ClearPendingWarpPhase clears it on cancel | Rust: no warp_state field; state machine replaced by TeleportPhase enum with no equivalent pending-warp counter | State-0 WarpingOut path and warp cancel path are both unimplementable without this field | gamemd struct per struct-teleport-locomotion-class.md:141; Rust: no +0x280 equivalent |
| Struct / TechnoClass+0x284 chrono_delay_countdown field absent | DRIFT | State-3 set, state-6 expiry (non-harvester post-arrival delay from Rules.ChronoDelay) | gamemd: TechnoClass+0x284 holds the Rules+0xBEC (ChronoDelay, default 60) countdown written in state-5, consumed in state-6; this is a flat INI-value delay distinct from the distance-based InitiateWarp delay | Rust: no chrono_delay_countdown field; post-arrival delay merged into `being_warped_ticks` and computed distance-based at issue time, not from flat Rules.ChronoDelay | Post-arrival translucency duration differs: gamemd uses flat 60-tick Rules.ChronoDelay; Rust uses distance-scaled compute_chrono_delay — duration diverges for all non-trivial distances | gamemd struct per struct-teleport-locomotion-class.md:142; Rust @ src/sim/movement/teleport_movement.rs:65-79 |
| Struct / TechnoClass+0x288/+0x28c/+0x290 dest coords absent | DRIFT | State-2 teleport to stored destination (TechnoClass Location write) | gamemd: TechnoClass+0x288/+0x28c/+0x290 hold the warp destination in leptons; state-2 reads these and writes to TechnoClass+0x9c/+0xa0/+0xa4 (Location) | Rust: destination stored as `(target_rx, target_ry)` cell coords in TeleportState, not as TechnoClass fields; Z component absent entirely | Z destination always 0 in Rust; any teleport to elevated terrain (ramps, cliffs, bridges) places the unit at ground level | gamemd struct per struct-teleport-locomotion-class.md:143-145; Rust @ src/sim/movement/teleport_movement.rs:48-55 (2D cell, no Z) |
| Struct / TechnoClass+0x3cd falling_dying_flag absent | DRIFT | PostWarpValidation water/impassable landing death trigger | gamemd: PostWarpValidation sets TechnoClass+0x3cd to 1 when the unit lands on water or impassable terrain, triggering the kill chain | Rust: no falling_dying_flag in GameEntity; PostWarpValidation has no Rust equivalent | Unit teleporting to water survives; no death on invalid terrain — already covered as MISSING row but the field absence is the root cause | gamemd struct per struct-teleport-locomotion-class.md:148; Rust: no +0x3cd equivalent |
| Struct / TechnoClass+0x694 warp_attach_ptr field absent | DRIFT | InitiateWarp WarpAttach detach step | gamemd: TechnoClass+0x694 holds a `WarpAttachClass*`; InitiateWarp reads it, calls Detach() before warp, then clears it | Rust: no warp_attach_ptr in GameEntity; WarpAttachClass detach step is entirely absent | WarpAttach objects (which track ChronoSphere-applied warp state) are never cleaned up when a teleport is issued — WarpAttach persists, possibly conflicting with subsequent warps | gamemd struct per struct-teleport-locomotion-class.md:152; Rust: no WarpAttachClass integration |
| Struct / TechnoClass+0x2b4 targeting_active field absent | DRIFT | TimerCheck expiry — weapon re-engagement after warp | gamemd: TimerCheck reads TechnoClass+0x2b4; if 0 (no active target), calls Passive_Target_Acquire to re-engage weapons after warp completes | Rust: no targeting_active field; no weapon re-engagement after teleport completes | After warp the unit does not auto-acquire targets; player must manually command attack — already covered under timer-check rows but the field absence is the root cause | gamemd struct per struct-teleport-locomotion-class.md:146; Rust: no +0x2b4 equivalent |
| Struct / TeleportLocomotionClass+0x40 field_40 unknown — DRIFT | DRIFT | Unknown — field not written by constructor and identity unresolved (YELLOW in struct doc) | gamemd: `TeleportLocomotionClass+0x40` exists in the 76-byte object; not initialised by constructor; referenced in IsOkToEnd check `param_1+0x20` (which may resolve to +0x38 or +0x40 depending on this-pointer base) | Rust: no field_40 equivalent in TeleportState | Default DRIFT per parity bar: unknown field, unknown behavior, no proof of equivalence | gamemd struct per struct-teleport-locomotion-class.md:54 (YELLOW); Rust: TeleportState has no +0x40 equivalent |
| Struct / TeleportLocomotionClass+0x48 field_48 unknown — DRIFT | DRIFT | Unknown — possible alt piggyback slot (YELLOW in struct doc) | gamemd: `TeleportLocomotionClass+0x48` cleared by constructor; may be the actual End_Piggyback storage slot if IPiggyback-this+0x30 resolves to full-obj+0x48 | Rust: no field_48 equivalent; piggyback stored in LocomotorState.override_kind | Default DRIFT per parity bar: unresolved dual-slot conflict; if End_Piggyback reads +0x48 and Begin_Piggyback stores to +0x30, piggyback would fail silently in gamemd — unknown observable effect | gamemd struct per struct-teleport-locomotion-class.md:56 (YELLOW); Rust: no +0x48 equivalent |

<!-- compare-fn-warp-attach-detach (task #81) -->
| WarpAttachClass__Detach / entire function absent | MISSING | Any warp initiated on a unit that has an active WarpAttachClass (TechnoClass+0x694 != null) | gamemd: InitiateWarp calls WarpAttachClass__Detach (0x0062a4a0) unconditionally before warp sequence; detach clears warp state, fires relocation or early-exit, cleans timer fields | Rust: no WarpAttachClass system at all; issue_teleport_command has no detach call | Unit with an active WarpAttach (e.g., partially-teleported unit from ChronoSphere, or a unit mid-warp-cycle) warps again without cleanup — WarpAttach persists, timer fields stale, subsequent behavior undefined | gamemd @ 0x007195cf (InitiateWarp caller site) per fn-warp-attach-detach.md:13; Rust @ src/sim/movement/teleport_movement.rs:104-142 (no detach call) |
| WarpAttachClass__Detach / TechnoClass+0x6c warp_state_local not cleared | DRIFT | WarpAttach detach on any path (early or full) | gamemd: clears TechnoClass+0x6c to 0 on both early and full detach paths | Rust: no TechnoClass+0x6c field in GameEntity | Any system gating on warp_state_local sees stale non-zero value indefinitely | gamemd @ 0x0062a4a0 param_1[9][0x1b]=0 per fn-warp-attach-detach.md:81; Rust: no +0x6c equivalent |
| WarpAttachClass__Detach / TechnoClass timer fields (+0x6a0/+0x6a4/+0x6a8) not written | DRIFT | WarpAttach detach on any path | gamemd: writes TechnoClass+0x6a0 (timer_start = g_CurrentFrameCounter), +0x6a4 (timer_duration = rate-timer value), +0x6a8 (timer_delay = 0 or iVar2×3) on both paths | Rust: no TechnoClass timer fields in GameEntity; no rate-timer integration | Rate-timer-based movement pacing after warp is entirely absent; unit may visually pop to destination with no locomotor settle window | gamemd @ 0x0062a4a0 piVar6[0x1a8/0x1a9/0x1aa] writes per fn-warp-attach-detach.md:82-84; Rust: no timer fields |
| WarpAttachClass__Detach / TechnoClass+0x432 hide_cameo_flag not cleared | DRIFT | WarpAttach full-detach path when owner is human player | gamemd: clears TechnoClass+0x432 (byte flag) and calls vtable+0x14c if owner is human player — this un-hides the unit's sidebar cameo after warp | Rust: no hide_cameo_flag in GameEntity; no sidebar cameo management | Sidebar cameo remains hidden after warp completes — player loses the unit from sidebar view | gamemd @ 0x0062a4a0 TechnoClass+0x432 clear per fn-warp-attach-detach.md:86; Rust: no cameo visibility management |
| WarpAttachClass__Detach / passable-cell fallback for impassable destination | DRIFT | WarpAttach destination cell is impassable | gamemd: CellClass__CheckCellPassability → if impassable, FUN_00703590 (Find_Nearby_Passable_Cell variant) finds alternate landing cell | Rust: no passable-cell fallback in issue_teleport_command or tick_teleport_movement | Unit directed to impassable cell by WarpAttach teleports into blocked cell instead of finding nearby valid cell | gamemd @ 0x0062a4a0 Phase 4 per fn-warp-attach-detach.md:153-155; Rust: no passable-cell fallback |
| WarpAttachClass__Detach / aircraft direction-offset nudge absent | DRIFT | Warp with WarpAttach on an aircraft unit (TechnoType+0xcce set) | gamemd: aircraft path reads g_DirectionOffsets table (8-entry direction nudge) and offsets destination cell by direction bits from rate-timer | Rust: no aircraft distinction in issue_teleport_command; no direction-offset table | Aircraft arriving from WarpAttach-based warp land at wrong cell (no directional nudge) — player sees aircraft appear at adjacent cell rather than intended arrival cell | gamemd @ 0x0062a4a0 Phase 1/3 aircraft path per fn-warp-attach-detach.md:93-98,141-147; Rust: no aircraft handling |
| WarpAttachClass__Detach / full relocation sequence (Limbo+vtable chain) absent | MISSING | WarpAttach full-detach path (timer elapsed + CanPlaceAtTarget succeeds) | gamemd: fires TechnoClass__SetGhostCell → vtable+0x3c8 (Limbo) → vtable+0x480 → vtable+0x484 → vtable+0x488(0,0,0,0,0); timer_delay set to iVar2×3 from locomotor field | Rust: no relocation sequence; position written directly in Relocate tick with no Limbo/Unlimbo, no ghost-cell, no vtable chain | Unit placed at destination without full re-registration in cell occupancy, map, and targeting systems — visible desync between unit position and game-state cell occupancy | gamemd @ 0x0062a4a0 Phase 5 vtable chain per fn-warp-attach-detach.md:169-183; Rust @ src/sim/movement/teleport_movement.rs:170-200 (direct occupancy.move_entity only) |
| WarpAttachClass__Detach / iVar2×3 locomotor-speed timer delay absent | DRIFT | WarpAttach full-detach path (timer elapsed) | gamemd: timer_delay = locomotor_speed_field × 3; locomotor speed read from `*(locomotor_ptr+0xb0)` — scales post-detach settle window to unit speed | Rust: no timer_delay calculation; no locomotor speed query at warp time | Post-warp settle window has wrong duration (0) for all units; unit re-engages at wrong tick | gamemd @ 0x0062a4a0 iVar2*3 per fn-warp-attach-detach.md:178; Rust: no locomotor speed query |
| WarpAttachClass__Detach / anim listener deregistration absent | DRIFT | WarpAttach with active anim object (param_1[0x11] != null, listener-registered flag set) | gamemd: clears listener-registered flag, removes from g_AnimClass_RemoveListeners array before clearing anim ptr | Rust: no anim system, no listener list management | Animation listener dangling pointer — no visible effect if anim system absent, but anim-remove callbacks would reference freed memory | gamemd @ 0x0062a4a0 Phase 6 listener cleanup per fn-warp-attach-detach.md:182; Rust: no anim listener system |

<!-- compare-string-chrono-rules-keys (task #46) -->
| ChronoRules / ChronoMinimumDelay clamp direction off-by-one | DRIFT | Any warp where computed delay equals ChronoMinimumDelay exactly | gamemd: `if (timer_ticks <= min_delay)` clamp — activates when computed delay is EQUAL TO OR LESS THAN ChronoMinimumDelay | Rust: `if delay < rules.chrono_minimum_delay` — activates only when strictly less than; when delay == minimum, Rust leaves it unchanged while gamemd would re-arm the timer with a fresh g_CurrentFrameCounter snapshot | When distance exactly produces `delay == ChronoMinimumDelay`, gamemd re-arms the timer start frame (resetting elapsed) while Rust does not — timer starts at slightly different reference frame, producing up to ChronoMinimumDelay ticks of visible drift in shimmer duration | gamemd @ 0x00719400 `if (timer_ticks <= min_delay)` per string-chrono-rules-keys.md:86; Rust @ src/sim/movement/teleport_movement.rs:74 (`if delay < rules.chrono_minimum_delay`) |
| ChronoRules / elapsed-time subtraction absent in Rust | DRIFT | Any warp issued on a unit where `timer_start != -1` (locomotor had a prior timer started) | gamemd: subtracts `elapsed = g_CurrentFrameCounter - timer_start` from computed `timer_ticks` if `timer_start != -1`; this corrects for time already spent in a prior timer phase | Rust: no elapsed-time subtraction in `compute_chrono_delay`; delay computed fresh from distance with no reference to prior timer state | Unit that re-warps shortly after a previous warp (timer partially expired) gets a full unshortened delay in Rust; gamemd would give the residual reduced delay | gamemd @ 0x00719400 elapsed subtraction per string-chrono-rules-keys.md:78-81; Rust @ src/sim/movement/teleport_movement.rs:65-79 (no elapsed adjustment) |
| ChronoRules / ChronoDelay (Rules+0xBEC) used for post-arrival flat hold, not distance-based delay | DRIFT | Post-arrival phase (state 3/6) for non-harvester unit when ChronoTrigger=yes | gamemd: `ChronoDelay` (stock 60 frames) is written to `TechnoClass+0x284` in state 3 and used as the WarpIn hold timer duration in state 5 — this is a FLAT post-arrival hold separate from the distance-based InitiateWarp delay | Rust: `compute_chrono_delay` computes a single distance-based delay that covers both the warp-out hold AND the post-arrival hold; no separate flat ChronoDelay value applied post-arrival | Post-arrival translucency duration diverges: gamemd = flat 60 frames (ChronoDelay); Rust = distance-based formula result (varies with destination) — a unit warping 20 cells away has different post-arrival shimmer duration in each engine | gamemd @ 0x007192F0 state-3 `TechnoClass+0x284 = Rules+0xBEC` per string-chrono-rules-keys.md:125; Rust @ src/sim/movement/teleport_movement.rs:65-79 (single distance-based delay) |
| ChronoRules / warp-out hold 60 frames (state-0B timer_duration=0x3c) absent in Rust | DRIFT | Warp-out shimmer phase (state 1, 60-frame wait) | gamemd: state 0B arms `timer_duration = 0x3c (60)` for the warp-out wait; this 60-frame value is the warp-out phase duration, not the distance formula result | Rust: `TeleportPhase::Relocate` executes in a single tick with no warp-out hold — the 60-frame warp-out shimmer before relocation is entirely missing | Player sees the unit shimmer for 60 frames before it teleports in gamemd; Rust relocates instantly with no pre-warp shimmer window | gamemd @ 0x007192F0 state-0B `param_1[0x10] = 0x3c` per enum-state-machine-states.md:65; Rust @ src/sim/movement/teleport_movement.rs:163-200 (Relocate is instant, no warp-out hold) |

<!-- compare-enum-state-machine-states (task #47) -->
| Enum / State 0 sub-branch A — warp_state interrupt-resume absent | DRIFT | Unit at state 0 with TechnoClass+0x280 (warp_state) non-zero (externally interrupted mid-warp) | gamemd: state 0 first checks if TechnoClass+0x280 is non-zero; if so, copies it directly to the locomotor state byte (+0x34) — resumes the state machine at the interrupted state | Rust: no warp_state resume path; TeleportPhase enum has only Relocate and ChronoDelay — no mid-warp interrupt recovery | A unit interrupted mid-warp (e.g. by damage) and later resumed will re-enter Rust's Relocate unconditionally; gamemd would resume at the correct interrupted state (e.g. state 5 if interrupted mid-validation) | gamemd @ 0x007192F0 state-0 sub-branch A per enum-state-machine-states.md:50-54; Rust @ src/sim/movement/teleport_movement.rs:159 (no interrupt state) |
| Enum / State 0 sub-branch B — ChronoInTransit 60-tick warp-out gate absent | DRIFT | Unit with TechnoClass+0x27C (ChronoInTransit) set entering state 0 | gamemd: sub-branch B detects ChronoInTransit set at state 0 and arms a 60-frame timer (BeingWarped=1, timer_duration=0x3c) before advancing to state 1 | Rust: no ChronoInTransit check at all; warp begins immediately when TeleportState is set | 60-frame pre-warp shimmer gate is bypassed; unit teleports without the warp-out animation window | gamemd @ 0x007192F0 state-0 sub-branch B per enum-state-machine-states.md:62-66; Rust: no ChronoInTransit gate |
| Enum / State 2 — WarpAnimGate (+0x271) set to 1 absent | DRIFT | State 2 (WARP_RELOCATE) execution | gamemd: state 2 sets TechnoClass+0x271 (WarpAnimGate) = 1 before committing teleport position | Rust: no WarpAnimGate field; render system receives no flag that the unit is mid-warp-anim | Unit at destination during chrono delay is not flagged as in-warp-anim — any system gating on +0x271 (e.g., translucency, targeting exclusion) does not apply | gamemd @ 0x007192F0 state-2 `TechnoClass+0x271 = 1` per enum-state-machine-states.md:101; Rust: no +0x271 equivalent |
| Enum / State 4 — occupation bit mark/unmark cycle absent | DRIFT | State 4 (OCCUPATION_CLEAR) after unit placed at destination | gamemd: state 4 calls vtable+0x1B4 (Mark occupation bits) then immediately vtable+0x1CC (Unmark) — a cycle that refreshes the unit's presence in the cell occupancy grid | Rust: `occupancy.move_entity` in the Relocate tick handles occupancy transfer but no mark/unmark cycle fires after placement | Post-placement occupancy grid may have stale entries; any system that reads occupancy between the mark and unmark steps sees transient double-occupancy in gamemd; Rust produces no such window | gamemd @ 0x007192F0 state-4 vtable+0x1B4/+0x1CC per enum-state-machine-states.md:118-119; Rust @ src/sim/movement/teleport_movement.rs:190-199 (move_entity only, no mark/unmark) |
| Enum / State 5 — TechnoClass+0x90 gate for post-arrival advance absent | DRIFT | State 5 (POST_WARP_VALIDATE) with TechnoClass+0x90 == 0 | gamemd: state 5 checks TechnoClass+0x90 (purpose YELLOW — possibly IsDying/IsInAir) before arming state-6 timer and advancing; if gate fails, state machine may not advance | Rust: no TechnoClass+0x90 check; ChronoDelay countdown begins unconditionally after Relocate | Unit in a state where +0x90 would block state-6 advance in gamemd continues to ChronoDelay in Rust — observable as translucency applied when it should not be | gamemd @ 0x007192F0 state-5 `cVar2 = TechnoClass+0x90` gate per enum-state-machine-states.md:128; Rust @ src/sim/movement/teleport_movement.rs:202-210 (ChronoDelay unconditional) |
| Enum / State 7 — dest_cache_1_z (+0x30) clear absent | DRIFT | State 7 (CLEANUP) at end of warp cycle | gamemd: state 7 clears `TeleportLocomotionClass+0x30` (dest_cache_1_z byte) to 0 as part of reset | Rust: no dest_cache_1_z field; TeleportState is set to None — full struct dropped | No observable effect if dest_cache_1_z is never re-read before drop, but if the warp sequence is re-entered before cleanup (interrupt resume path), gamemd would see a stale non-zero value that Rust cannot represent | gamemd @ 0x007192F0 state-7 `param_1[0xc]=0` per enum-state-machine-states.md:144; Rust @ src/sim/movement/teleport_movement.rs:216 (teleport_state=None) |

<!-- compare-fn-house-has-power-surplus (task #82) -->
| HouseClass__HasPowerSurplus / power gate in PostWarpValidation absent | MISSING | Aircraft unit warping to destination when owning house has power deficit | gamemd: PostWarpValidation calls HasPowerSurplus (HouseClass+0x2d8 > 0); aircraft units (SpeedType==3) survive invalid terrain ONLY if the owning house has power surplus — no surplus means death even for aircraft | Rust: no PostWarpValidation, no HouseClass power check; aircraft warp to any destination regardless of house power level | Aircraft units survive warp to water/impassable terrain during power outage in Rust; in gamemd they die — player can exploit power-out to bypass the aircraft warp-death gate | gamemd @ 0x0050e1b0 called from PostWarpValidation 0x007188a6 per fn-house-has-power-surplus.md:13; Rust: no PostWarpValidation or power gate |

<!-- compare-fn-cell-has-bridge-overlay (task #83) -->
| CellClass__HasBridgeOverlay / theater-tile-range bridge detection absent | MISSING | Warp destination cell with bridge overlay (any theater) | gamemd: HasBridgeOverlay checks CellClass+0x38 (overlay tile index) against 5 theater-INI-populated tile ranges (shore pieces + 4 bridge ramp ranges); called by PostWarpValidation to adjust Z-height for bridge arrivals | Rust: no CellClass overlay tile check; no theater-INI tile range tables; bridge detection uses no equivalent mechanism in tick_teleport_movement | Unit teleporting to bridge-overlay cells: (a) gets wrong Z (bridge-height adjustment skipped), (b) PostWarpValidation death check skips bridge exemption — unit may die on bridge tiles that gamemd would allow | gamemd @ 0x004865d0 called from PostWarpValidation 0x00718aa9 per fn-cell-has-bridge-overlay.md:12; Rust: no overlay tile check, no bridge Z adjustment via tile ranges |

<!-- compare-fn-0070f770 (task #84) -->
| TechnoClass__ResetAutoFireTimer / post-warp auto-fire timer re-arm absent | MISSING | Every warp completion for any teleporting unit | gamemd: TimerCheck (state 1 or 6) calls FUN_0070f770 (`TechnoClass__ResetAutoFireTimer`) after clearing TechnoClass+0x271 (warp-shimmer gate); the function reads remaining ticks on TechnoClass+0x180/+0x184/+0x188 (3-field auto-fire timer block) and if >10 frames remain AND g_MapEditorMode==0, re-randomizes the interval to 4–8 frames — re-arming the unit's auto-fire check cycle immediately after warp | Rust: no equivalent; `tick_teleport_movement` on completion only calls `loco.end_override()` and sets `teleport_state = None`; no TechnoClass auto-fire timer fields exist | Unit finishes warp but auto-fire cycle is not re-armed: first attack opportunity after warp is delayed until the pre-existing timer naturally expires (potentially 10+ ticks); gamemd re-arms immediately on every warp — observable as delayed first-shot response post-warp | gamemd @ 0x00719c29 UNCONDITIONAL_CALL from TimerCheck per fn-0070f770.md:27-31; Rust @ src/sim/movement/teleport_movement.rs:215-222 (no ResetAutoFireTimer call) |

<!-- compare-fn-techno-passive-target-acquire (task #85) -->
| TechnoClass__Passive_Target_Acquire / post-warp passive target scan absent | MISSING | Warp completion when unit has no active target (TechnoClass+0x2B4 == 0) | gamemd: TimerCheck calls Passive_Target_Acquire (0x00709480) after warp-delay expiry when TechnoClass+0x2B4 == 0; the function: runs gate FUN_00709290 (mission/house/targeting-permission checks), timestamps TechnoClass+0x4FC with current frame, calls GetCoords (vtable+0x48) + target-scan dispatcher (vtable+0x39C), and if new target found sets TechnoClass+0x50C (firing_flag) = 1; if target found → locomotion stays locked; if not → Resume called to restart locomotion | Rust: no Passive_Target_Acquire call; on `being_warped_ticks == 0` Rust unconditionally calls `loco.end_override()` and removes TeleportState — no targeting gate, no scan, no Resume-vs-lock branch | Unit completes warp with no active target: gamemd immediately scans for targets and may fire before resuming locomotion; Rust always resumes locomotion — the post-warp aggressive target acquisition and the Resume-vs-lock gate are both missing, meaning units are less responsive to new targets after warping | gamemd @ 0x00719C31 UNCONDITIONAL_CALL from TimerCheck per fn-techno-passive-target-acquire.md:18-19; Rust @ src/sim/movement/teleport_movement.rs:215-222 (no targeting call) |

<!-- compare-string-warp-anim-keys (task #86) -->
| WarpIn= INI key parsed but warp shimmer animations never spawned by teleport SM | DRIFT | Every teleporting unit at departure (states 0/2) and arrival (state 5) | gamemd: StateMachineTick states 0, 2, and 5 each call `AnimClass__Constructor(*(Rules+0x33C), ...)` to spawn the WarpIn shimmer animation at the unit's current location; state 5 spawns the arrival shimmer | Rust: `GeneralRules.warp_in` and `GeneralRules.warp_away` are parsed from `WarpIn=`/`WarpAway=` and loaded into the sprite atlas, but `tick_teleport_movement` and `issue_teleport_command` never spawn any animation — no `AnimClass` equivalent is created at departure or arrival | Unit teleports visually silently: no warp-shimmer SHP plays at departure cell or arrival cell; gamemd plays WARPIN at both; shimmer is player-visible every time a chrono unit teleports — every match with chrono units | gamemd states 0/2/5 per fn-state-machine-tick.md, WarpIn anim ptr at Rules+0x33C per string-warp-anim-keys.md:35; Rust `warp_in` parsed @ src/rules/ruleset.rs:964-966 but not consumed in src/sim/movement/teleport_movement.rs |
| WarpAway= INI key parsing MATCH — key parsed, anim loaded | MATCH | INI parsing only | gamemd: `WarpAway` (Rules+0x340) is parsed by RulesClass__ReadGeneral; consumed by ChronoSphere weapon system, not TeleportLocomotionClass (doc YELLOW) | Rust: `GeneralRules.warp_away` is parsed from `WarpAway=` (default "WARPAWAY") and loaded into sprite atlas — parsing matches gamemd | Both parse the key and store the result; neither TeleportLocomotionClass in gamemd nor Rust's teleport SM consume WarpAway | gamemd @ string-warp-anim-keys.md:36-37 (YELLOW on locomotor consumption); Rust @ src/rules/ruleset.rs:972-974 |

<!-- compare-string-warpable (task #87) -->
| Warpable= per-unit flag absent in Rust | MISSING | Any ChronoSphere targeting check or PostWarpValidation warpable gate | gamemd: `Warpable` is read per-unit into TechnoTypeClass+0xD3A (byte); consumed via vtable+0x160 warpable check in PostWarpValidation Phase 1 to gate whether a unit can be targeted/affected by chrono weapons | Rust: no `warpable` field exists anywhere in `src/` (confirmed by grep) — not parsed in `ObjectType`, not in `GameEntity`, not checked in any teleport code path | All units in Rust are implicitly warpable; units marked `Warpable=no` in stock rules (e.g., buildings set to false, some specific units) will be incorrectly allowed as warp targets — observable any time the player uses a ChronoSphere or any chrono-targeting weapon | gamemd TechnoTypeClass+0xD3A per string-warpable.md:57; Rust: no match found in src/ |

<!-- compare-string-chrono-harv-too-far (task #88) -->
| ChronoHarvTooFarDistance= INI key and consumption MATCH | MATCH | Chrono Miner return-to-refinery path when refinery is far away | gamemd: `ChronoHarvTooFarDistance` (Rules+0xD7C, stock 50) consumed by harvester mission SM to decide whether to warp or drive when returning | Rust: `chrono_harv_too_far_distance: i32` parsed from `ChronoHarvTooFarDistance=` (default 50) in `GeneralRules`; consumed as `config.too_far_threshold_chrono` in `return_exceeds_too_far_threshold()` with strict `>` comparison — both INI parse and behavioral use match gamemd | MATCH — INI key, default value, and too-far threshold gate all correctly implemented | Rust @ src/rules/ruleset.rs:1014, src/sim/miner/mod.rs:230, src/sim/miner/miner_system.rs:923-931; gamemd Rules+0xD7C per string-chrono-harv-too-far.md:52 |

<!-- ROWS END -->
